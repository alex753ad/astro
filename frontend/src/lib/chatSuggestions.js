/**
 * chatSuggestions.js — приветствие Аристеи и примеры вопросов.
 *
 * ⚠️ **Общий файл для веба и приложения, и вторых копий этих строк быть не
 * должно.** Тот же приём и по той же причине, что у `lib/sectionStream.js` и
 * `mobile/lib/ruDeclension.js`: третий такой случай начнут искать здесь, а не
 * копированием готового массива в новый компонент. Фразы жили в `RagChat.jsx`
 * локальной константой `SUGGESTIONS` — при переносе чата в приложение это
 * ровно тот момент, когда копия и заводится.
 *
 * ⚠️ Слоя проактивности (`decodeTopic`, разбор `?astrea=<topic>` из веб-пуша)
 * здесь НЕТ намеренно: он привязан к пушам, которых в приложении нет вовсе, и
 * переносить его было бы работой над несуществующей задачей. Он остаётся
 * локальным для `RagChat.jsx`.
 */

/**
 * Примеры вопросов. Показываются, когда разговора ещё нет.
 *
 * Пять штук — верхняя граница того, что помещается на телефоне, не выдавливая
 * поле ввода за экран. Формулировки от первого лица («моя карта», «мои
 * отношения»): человек нажимает их как СВОЙ вопрос, и в истории диалога они
 * так и останутся.
 */
export const CHAT_SUGGESTIONS = [
  'Что говорит моя карта о карьере?',
  'Как Сатурн влияет на мои отношения?',
  'Какой период сейчас для финансовых решений?',
  'Почему мне сложно с дисциплиной?',
  'Что означает мой Асцендент?',
];

/**
 * Первая реплика Аристеи в пустом чате.
 *
 * ⚠️ Это НЕ сообщение диалога: на сервер оно не уходит, в историю не пишется и
 * в промпт не подмешивается. Чисто экранный текст — иначе первая же реплика
 * съедала бы одну из десяти позиций серверной истории, ничего не добавляя
 * модели (она и так знает, кто она).
 *
 * Тон — тот же, что в системном промпте: «навигатор решений», без гадания и
 * без обещаний. Поэтому «что делать и когда», а не «расскажу твою судьбу».
 */
export const CHAT_GREETING =
  'Я смотрю на вашу карту целиком — характер, периоды, что сейчас активно. ' +
  'Спрашивайте своими словами: про работу, отношения, деньги, здоровье или ' +
  'про конкретное решение. Отвечу по вашей карте, а не вообще.';

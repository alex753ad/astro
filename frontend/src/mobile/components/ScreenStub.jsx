/**
 * ScreenStub.jsx — заглушка вкладки.
 *
 * Общий рендерер для Feed/Chart: только заголовок, без контента. «Ещё»
 * (MoreScreen.jsx) больше не заглушка — там теперь переключатель темы,
 * поэтому у неё свой макет с заголовком сверху, а не по центру.
 *
 * flex: 1 вместо minHeight: '100%' — сознательный выбор, не стилистический.
 * Проценты по высоте резолвятся только относительно содержащего блока с
 * ОПРЕДЕЛЁННОЙ высотой; в цепочке из нескольких вложенных флекс-контейнеров
 * это легко ломается на конкретном движке рендеринга непредсказуемо. flex:1
 * этой зависимости не имеет вовсе — раскладку считает сам флекс-алгоритм,
 * без резолва процентов через N уровней вложенности. Ровно с такой поломкой
 * заголовок вкладки прижимался к статус-бару, хотя верхний safe-area отступ
 * стоял в родителе (TabShell.jsx) правильно.
 */

import React from 'react';

export default function ScreenStub({ title }) {
  return (
    <div
      style={{
        flex: 1,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <span
        style={{
          fontFamily: 'var(--font-display)',
          fontWeight: 700,
          fontSize: 22,
          color: 'var(--text-primary)',
        }}
      >
        {title}
      </span>
    </div>
  );
}

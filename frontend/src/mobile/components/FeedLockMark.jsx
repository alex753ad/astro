/**
 * FeedLockMark.jsx — признак «расшифровка закрыта» у события планера.
 *
 * ⚠️ Это исключение из правила «помечается ОТКРЫТОЕ, а не закрытое»
 * (FeedEventCard.jsx, решение владельца 05.09.2026), и оно не отменяет его.
 * Там пометка ставилась на закрытые транзиты и оказывалась на 689 карточках
 * из 718 — то есть не сообщала ничего. Здесь наоборот: у проходов Луны по
 * домам закрытых МЕНЬШИНСТВО (замер 16.09.2026 на боевом стенде: 31 из 53 на
 * free, 22 из 53 на платном — прошлое открыто всем), и значок несёт
 * сведения, а не шум.
 *
 * Своей высоты значок не добавляет: 12×12 в строке 13px. Вид события от
 * тарифа не зависит — меняется только эта метка, поэтому лента одинаково
 * читается у всех (решение владельца 16.09.2026).
 *
 * `currentColor` не берём: цвет строки меняется между обычным и приглушённым
 * видом, а метка обязана оставаться подписью, а не заголовком.
 */

import React from 'react';

export default function FeedLockMark({ size = 12 }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 12 12"
      fill="none"
      aria-label="Расшифровка закрыта"
      role="img"
      style={{ flexShrink: 0, color: 'var(--text-secondary)', opacity: 0.75 }}
    >
      <rect x="2.5" y="5.25" width="7" height="5.25" rx="1.2" stroke="currentColor" strokeWidth="1.1" />
      <path d="M4.25 5.25V3.9a1.75 1.75 0 1 1 3.5 0v1.35" stroke="currentColor" strokeWidth="1.1" strokeLinecap="round" />
    </svg>
  );
}

/**
 * onboardingCopy.js — все тексты онбординга одним файлом (SPEC_ONBOARDING.md §5).
 *
 * По компонентам не раскладывать. Причина записана историей проекта:
 * `ruDeclension.js` заведён 06.09.2026 ровно потому, что до него склонение
 * копировали из компонента в компонент и копии разъезжались. Реплики
 * Аристеи — третий такой случай подряд, и он хуже двух предыдущих:
 * формулировки выверяет владелец, а выверять их по нескольким файлам сразу
 * — это гарантированный разъезд с первой же правкой.
 *
 * ⚠️ Здесь только данные: константы, никаких функций и никакой логики
 * показа. Логика живёт в компонентах, которые их читают.
 *
 * ⚠️ ВЫЗОВОВ LLM В ОНБОРДИНГЕ НЕТ НИ ОДНОГО, и это решение, а не
 * недоделка (SPEC_ONBOARDING.md §4). Аристея здесь — персонаж с заранее
 * написанными репликами. Три независимые причины:
 *   1. чат Аристеи гейтится тарифом (`features.rag_chat`, Лира и Орион), а
 *      приветствие обязано работать для анонима, у которого нет ни тарифа,
 *      ни аккаунта;
 *   2. генерация на каждого входящего тратит общий дневной `BudgetTracker`
 *      (он один на все три AI-контура проекта) до регистрации и до оплаты —
 *      то есть деградировала бы платную интерпретацию, которая при
 *      исчерпании бюджета принудительно уходит в шаблон;
 *   3. приветствие обязано быть одинаковым у всех и проверяемым на приёмке:
 *      сгенерированный текст нельзя ни выверить заранее, ни воспроизвести в
 *      чек-листе.
 * Практическое следствие: приветствие работает целиком без сети.
 *
 * ⚠️ Астрологических значков (♃ ♄ ☉ ☽) в этих строках быть не должно.
 * Системный шрифт на Android этот блок Unicode либо не покрывает (тофу),
 * либо подменяет цветным emoji-шрифтом, который игнорирует `color`. Для них
 * подключено отдельное семейство `AstroSymbols` (mobile.css), потребители
 * берут его через `lib/feedGlyphs.js`. На десктопе разница не видна вовсе.
 */

/** Три экрана приветствия, по порядку (SPEC_ONBOARDING.md §3, §12). */
export const WELCOME_SCREENS = [
  {
    key: 'who',
    title: 'Аристея',
    text: 'Я считаю твою натальную карту и показываю, что по ней происходит сейчас.',
  },
  {
    key: 'what',
    title: 'Что здесь есть',
    text: 'Карта рождения — положение планет в момент твоего рождения. '
      + 'Лента — события этой карты по дням: что начинается, что заканчивается, '
      + 'что идёт прямо сейчас.',
  },
  {
    key: 'need',
    title: 'Что нужно для расчёта',
    // Обещание «это можно отметить» проверено по коду веб-формы 07.09.2026:
    // чекбокс «Я не знаю точного времени (расчёт от 12:00 космограммы)»
    // существует (BirthForm.jsx), при нём birth_time уходит null. Если
    // чекбокс когда-нибудь уберут — править и эту строку, иначе приложение
    // будет обещать несуществующее.
    text: 'Дата, время и место рождения. Время влияет на дома и восходящий знак; '
      + 'если точное неизвестно, это можно отметить — расчёт будет короче.',
  },
];

export const WELCOME_BUTTONS = {
  next: 'Далее',
  skip: 'Пропустить',
  // Ведёт на веб-форму во внешнем браузере (решение владельца 07.09.2026):
  // формы ввода данных рождения в приложении нет, она живёт на сайте.
  build: 'Построить карту',
  haveAccount: 'У меня уже есть аккаунт',
};

/**
 * Адрес веб-формы ввода данных рождения. Маршрут /home (App.jsx; корень `/`
 * — лендинг, не форма — этим адресом ошибочно вела кнопка «Постройте её на
 * сайте» до 07.09.2026, см. MoreCardsList.jsx).
 *
 * Живёт здесь, а не в MoreCardsList.jsx, хотя используется и там: это
 * единственный адрес формы во всём мобильном приложении, и держать его
 * одной константой было условием этой правки — раньше он был хардкожен
 * дважды с двумя разными (и один раз неверным) значениями.
 */
export const BIRTH_FORM_URL = 'https://aristeatime.ru/home';

/**
 * Страница тарифов. Ведёт наружу, в системный браузер, — так можно:
 * публикуемся в RuStore, он монетизацию вне своих платёжных SDK разрешает
 * (CLAUDE.md, «Из мобильного приложения можно вести на веб-оплату»).
 *
 * Здесь, а не по месту использования, по той же причине, что и адрес формы
 * выше: с появлением второго потребителя (отказ 403 при исчерпанных слотах
 * карт, SPEC_CHART_CREATE.md §8) он был бы хардкожен дважды.
 */
export const PRICING_URL = 'https://aristeatime.ru/pricing';

/** Страница карты на сайте: `/chart/:chartId` (App.jsx:457). Оттуда
  * работает PDF-отчёт, недоступный из приложения — почему, написано у
  * самой ссылки в MoreScreen.jsx. */
export const WEB_CHART_URL = 'https://aristeatime.ru/chart';

/**
 * Подсказки экрана «Карта» (SPEC_ONBOARDING.md §10). Порядок — от общего к
 * частному; жест последним, он бесполезен, пока непонятно, на что смотреть.
 *
 * `anchor` — какой элемент подсвечивать; экран сопоставляет эти имена своим
 * ref'ам. Шаги 1-3 указывают на одно и то же колесо намеренно, см. разбор в
 * ChartScreen.jsx рядом с HINT_ANCHORS.
 */
export const CHART_HINTS = [
  {
    key: 'wheel',
    anchor: 'wheel',
    text: 'Круг — небо в момент твоего рождения. Планеты стоят там, где они были в этот момент.',
  },
  {
    key: 'houses',
    anchor: 'wheel',
    text: 'Двенадцать секторов — дома. Они делят карту по областям жизни: работа, отношения, здоровье, деньги.',
  },
  {
    key: 'aspects',
    anchor: 'wheel',
    text: 'Линии внутри круга — аспекты, углы между планетами. Зелёные — спокойные, красные — напряжённые.',
  },
  {
    key: 'zoom',
    anchor: 'zoomHint',
    // Формулировка обязана совпадать со строкой под колесом в ChartScreen.jsx
    // («Сведи пальцы для зума · двойной тап — сброс»): подсказка её
    // подсвечивает, а не пересказывает другими словами — иначе два текста
    // разъедутся при первой правке одного из них.
    text: 'Сведи пальцы, чтобы приблизить. Двойное касание возвращает масштаб.',
  },
];

/** Подсказки экрана «Лента» (SPEC_ONBOARDING.md §11). */
export const FEED_HINTS = [
  {
    key: 'timeline',
    anchor: 'timeline',
    text: 'Лента идёт по дням сверху вниз и открывается на сегодняшнем.',
  },
  {
    key: 'houseChips',
    anchor: 'houseChips',
    // Подпись «Планеты сейчас в домах» над блоком уже стоит (закрыт п. 3
    // отложенного списка CLAUDE.md, 07.09.2026). Подсказка отвечает на
    // следующий вопрос: почему этот блок вынесен из потока наверх.
    text: 'Здесь — дома, по которым сейчас идут медленные планеты. '
      + 'Такой период держится месяцами и годами, поэтому он вынесен наверх, '
      + 'а не в поток событий.',
  },
  {
    key: 'card',
    anchor: 'card',
    text: 'Коснитесь события, чтобы посмотреть подробности.',
  },
];

/**
 * Подсказка у кнопки чата (решение владельца 16.09.2026). Показывается при
 * первом запуске и ещё раз после каждого апгрейда — ключ хранения зависит от
 * тарифа, см. `chatHintKey`.
 *
 * ⚠️ Текстов два, и это не украшение: на free и Веге чат ЗАКРЫТ, на Лире и
 * Орионе открыт. Одна фраза на оба случая либо обещала бы бесплатному то,
 * чего у него нет, либо не сказала бы заплатившему, что у него появилось, —
 * а подсказка после апгрейда именно ради второго и показывается.
 *
 * ⚠️ Слова про доступ берутся из ТАРИФА, а не из текста: названия приходят из
 * TIER_NAMES, и подставлять «Лира» литералом здесь нельзя — при переименовании
 * тарифа подсказка соврёт молча.
 */
export function chatHint(hasAccess, tierName) {
  return hasAccess
    ? {
      key: 'chat',
      anchor: 'chat',
      text: 'Это чат с Аристеей. Спроси про свою карту, транзит или период — '
        + 'она отвечает по твоим данным, а не вообще.',
    }
    : {
      key: 'chat',
      anchor: 'chat',
      text: `Здесь живёт чат с Аристеей — вопросы по своей карте своими словами. `
        + `Открывается на тарифе ${tierName}.`,
    };
}

export const HINT_BUTTONS = {
  next: 'Далее',
  done: 'Понятно',
};

/** Подпись кнопки «?» для скринридера — сама кнопка рисует только знак. */
export const HINT_BUTTON_LABEL = 'Подсказки по экрану';

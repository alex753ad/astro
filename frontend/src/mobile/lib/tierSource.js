/**
 * tierSource.js — живой тариф для мобильного приложения.
 *
 * ⚠️ Заведён 09.09.2026, потому что источников было два и они расходились.
 * «Ещё» читало живой `GET /profile/subscription`, а экран разбора — `user.tier`
 * из `useAuth`, куда тариф попадает только при обновлении токена. Пока токен
 * жив (15 минут), смена тарифа на сервере до экрана разбора не доезжала: на
 * Лире показывалась приписка ветки free — «на тарифе Вега ~800 слов», то есть
 * платящему предлагали то, что у него уже есть. Приёмка 09.09.2026 поймала
 * расхождение в одну минуту между двумя вкладками.
 *
 * Источником выбран сервер, а не `useAuth`: какие тарифы что дают, решает
 * бэкенд, и копии сетки на клиенте в этом проекте расходились не раз.
 *
 * ⚠️ Сама машинерия — общая с вебом (`lib/subscriptionSource.js`): та же
 * проблема нашлась и там. Здесь остаётся только привязка к тому, как ходит
 * приложение — `fetchSubscription` через `authFetchWithTimeout`. Веб ходит
 * иначе (`request()` с токеном), и это единственное, чем две привязки
 * отличаются; всё остальное — дедупликация, кэш, подписка, сброс — общее.
 *
 * ⚠️ Нового запроса на каждое открытие экрана нет. `TabShell` монтирует три
 * экрана разом, и залп уже был причиной гейта по сроку токена (CLAUDE.md,
 * «Холодный старт упирается в ЗАДЕРЖИВАЮЩИЙ лимит»). До этой правки
 * `/profile/subscription` запрашивался ДВАЖДЫ на холодном старте — из
 * `MoreScreen` и из `useChatAccess`, каждый сам по себе. Теперь один на всех.
 *
 * ⚠️ Кэш живёт ТОЛЬКО в памяти и в localStorage не пишется. Записанный на
 * диск, он пережил бы перезапуск и снова стал бы устаревшим — ровно тем, от
 * чего этот файл избавляет.
 */

import { createSubscriptionSource } from '../../lib/subscriptionSource';
import { fetchSubscription } from './moreApi';

const source = createSubscriptionSource(fetchSubscription);

export const {
  getSubscription,
  onSubscription,
  peekSubscription,
  resetSubscription,
} = source;

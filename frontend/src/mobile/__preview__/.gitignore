# Временный стенд приёмки экрана «Лента»: подменяет window.fetch боевым
# ответом, потому что CORS прода не пускает dev-origin. В репозиторий не
# идёт — здесь и полумегабайтная фикстура со служебной картой, и код,
# который в приложении не участвует. Папка игнорирует сама себя, чтобы
# `git add` по каталогу mobile/ не утянул её снова (уже случилось однажды).
#
# README.md — исключение: способ генерации фикстуры не был записан нигде,
# из-за чего 06.09.2026 отдельная сессия потратила время на его поиск.
# Сам README не подменяет fetch и не содержит боевых данных — его надо
# коммитить, иначе третий заход снова будет гадать.
*
!.gitignore
!README.md

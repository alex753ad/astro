# CLAUDE.md — Astrea Timeline

## Правила работы
1. Спрашивай вместо того чтобы угадывать
2. Пиши минимальный код
3. Делай только то о чём прошу
4. Фокусируйся на результате, а не инструкции
5. Работай аккуратно и внимательно
6. Код не пиши пока я не скажу, отвечай кратко

---

## Проект

**Astrea Timeline** — веб-приложение: натальные карты, транзиты, лунный календарь, AI-интерпретации. Домен: astreatime.ru.
**Версия архитектуры:** 4.0 | Август 2026

---

## Стек

| Слой | Технология |
|---|---|
| Frontend | React 18.3, React Router 6, Vite 5, Tailwind 3.4, D3.js |
| Backend | Python 3.12, FastAPI, Uvicorn |
| БД / ORM | PostgreSQL 18, SQLAlchemy 2.0, Alembic |
| Кэш / очереди | Redis 7, Celery |
| Астрология | pyswisseph (Swiss Ephemeris) |
| AI | OpenAI GPT-4o → DeepSeek V3 → шаблоны; прогнозы — Anthropic Claude Sonnet → GPT-4o |
| Аутентификация | JWT, Google OAuth 2.0, bcrypt |
| Платежи | ЮKassa (в разработке) — Robokassa и Stripe удалены 19.08.2026 как мёртвый код |
| Email | Resend API |
| Геокодинг | Nominatim |
| Хостинг | Timeweb VPS: Nginx + Docker Compose (api, bot, postgres, redis, uptime-kuma) |
| CI/CD | GitHub Actions → SSH-деплой на VPS (backend), фронтенд — вручную скриптом |
| PDF | ReportLab |

**UI-инструменты (активные):**
- 21st.dev — готовые React-компоненты
- Framer Motion — анимации
- Ponytail — Tailwind-компоненты (CLI)

---

## Структура frontend

```
frontend/src/
├── App.jsx
├── main.jsx
├── index.css
├── pages/
│   ├── LandingPage.jsx
│   ├── HomePage.jsx
│   ├── ChartPage.jsx
│   ├── ProfilePage.jsx
│   ├── CRMPage.jsx
│   ├── AdminPage.jsx
│   ├── PlannerPage.jsx
│   ├── LunarCalendarPage.jsx
│   ├── SolarReturnPage.jsx    # in progress
│   ├── SynastryPage.jsx       # in progress
│   ├── RelocationPage.jsx     # in progress
│   ├── PortalPage.jsx         # клиентский портал
│   ├── IntakePage.jsx         # анкета клиента
│   ├── OrionPage.jsx          # тарифы
│   ├── ZodiacPage.jsx
│   ├── SharePage.jsx
│   └── GiftPage.jsx
├── components/
│   ├── NatalChart.jsx      # SVG колесо натальной карты (D3)
│   ├── TransitTimeline.jsx # временная шкала транзитов
│   ├── AuthModal.jsx
│   ├── RagChat.jsx         # чат Астреи (RAG)
│   ├── Toast.jsx
│   └── ThemeToggle.jsx
└── hooks/
    └── useAuth.jsx
```

---

## Структура backend

```
backend/
├── main.py
├── models.py
├── schemas.py
├── config.py
├── database.py
├── cache.py
├── celery_app.py
├── tasks.py
├── limiter.py
├── authz.py
├── metrics.py
├── email_service.py
├── natal_pdf.py
├── health.py
├── auth/
├── calendar/
├── crm/
├── ephemeris/
├── interpretation/
├── transit/
├── payments/          # common.py (провайдер-независимая логика: идемпотентность,
│                      # комиссии, активация; TIER_PRICES_RUB — единственный
│                      # источник цен на бэкенде) + payments_router.py (подписка,
│                      # админский set-tier) + yookassa_router.py (checkout, вебхук)
├── admin/
├── push/              # web push (pywebpush + VAPID)
├── notifications/     # telegram
├── pilot/             # пилотная Telegram-программа
├── feedback/
├── exit_survey/
└── profile/
```

---

## Тарифы

Таблица утверждена владельцем 19.08.2026, накопительная (каждый следующий
тариф — «всё из предыдущего, плюс…»; строки ниже — уже развёрнутый полный
вид, не сокращённый).

| | Бесплатный | Вега (Lite) | Лира (Pro) | Орион (Premium) |
|---|---|---|---|---|
| Цена/мес | 0 ₽ | 790 ₽ | 2490 ₽ | 7990 ₽ |
| Карты (= профили на аккаунт) | 2 | 5 | 15 | безлимит |
| AI-интерпретации/мес | 1 навсегда, дальше шаблон | 5 (по числу карт) | 15 (по числу карт) | безлимит |
| Планер | — | рекомендации на месяц | все планеты + долгосрочные (год) | всё из Лиры |
| Транзиты | — | горизонт 6 мес, AI-разбор 3/мес | горизонт 12 мес, AI без лимита | горизонт 24 мес, AI без лимита |
| Лунный календарь | текущий месяц | год | год | безлимит |
| Google Calendar | — | 1 карта | 1 карта (унаследовано) | 1 карта (унаследовано) |
| Чат с Астреей | — | — | ✓ | ✓ |
| PDF-экспорт | — | 5 карт | 15 карт | безлимит |
| Кабинет астролога (CRM) | — | — | — | ✓ |

Модель интерпретации — везде DeepSeek Pro (не зависит от тарифа, решение
владельца 19.08.2026); тариф регулирует глубину через лимит слов
(`interpretation_word_limit`), а не модель. Чат Астреи — отдельно, на Flash.

Источник истины — `TIER_FLAGS` (`backend/auth/rate_limits.py`) и `TIERS`
(`frontend/src/constants.js`); оба обязаны совпадать. «Карты» в таблице —
это `profiles_limit`: слотовая модель (вариант А, решение владельца
19.08.2026), сколько карт одновременно может быть сохранено, не месячная
квота — удаление карты освобождает слот, строить и смотреть карты можно
без ограничений. Enforced в `POST /chart/calculate` (`main.py`), проверяется
раньше остальных лимитов этого эндпоинта.

До 19.08.2026 существовало отдельное поле `charts_per_month` — та же цифра,
вручную продублированная под другим именем как «скорость создания в месяц».
Два независимых числа, обязанных совпадать, — рано или поздно расходятся
(так уже случалось с `pdf_per_month`, см. `TestTierMonotonicity`). 20.08.2026
поле убрано: единственный источник числа — `profiles_limit`. Вместо него
`main.py` использует отдельную константу `CHART_CREATION_ABUSE_LIMIT = 100`
(`auth/rate_limits.py`) — не тарифную фичу, а плоский бэкстоп от ботов, один
на все тарифы, нигде не описанный (ни в оферте, ни в интерфейсе). Для
free/lite/pro он в принципе недостижим — `profiles_limit` блокирует
создание намного раньше; единственный тариф, где он реально что-то
защищает, — Orion, где `profiles_limit = None` (слотов без ограничения) и
раньше не было вообще никакой защиты от скрипта, создающего карты по кругу.

Лимиты применяются через `TierRateLimiter` (`auth/rate_limits.py`), расход
считается в `UsageCounter`. Монотонность сетки (платный тариф не хуже
дешёвого ни по одному полю) проверяется тестом `TestTierMonotonicity` в
`test_rate_limits.py`.

### Письма о тарифах: числа выведены из флагов, одна фраза — нет

Приветственные письма (`email_service.py`) берут числа из `TIER_FLAGS`
через `_tier_limits()`, а не набирают их в тексте. Так сделано после
30.08.2026, когда сверка нашла три расхождения сразу: письмо Веги обещало
транзиты на 12 месяцев при `transits_months = 1`, письмо Лиры — 5 PDF при
`pdf_per_month = 15` и «на GPT-4o» при едином движке DeepSeek. Первое
человек читал сразу после оплаты.

⚠️ **Исключение, которое надо править руками: «Лунный календарь на год
вперёд» в приветственном письме Веги.** Это проза, а не подстановка: слово
«год» из `lunar_months` не выводится. Сегодня совпадает (`lunar_months =
12`), но при смене флага разойдётся молча — тест этого не поймает, потому
что сверять нечего с чем.

Склонение при подстановке чисел делает `_plural()` там же: «1 месяц» /
«3 месяца» / «12 месяцев». Без него первая же смена сетки сделала бы текст
неграмотным.

Формулировки про оплату в письмах обязаны совпадать с окном оплаты
(`PaywallModal.jsx`, поле `price`) и с п. 4.3 оферты: «Доступ на 1 месяц ·
Без автопродления». Автопродления в проекте нет, поэтому обещать отмену
подписки нельзя — человек пойдёт её отменять и придёт в поддержку. Так
уже было: письмо Веги день 14 до 30.08.2026 заканчивалось строкой «Отмена
в любой момент · Без обязательств».

---

## Ключевые API эндпоинты

```
POST /api/v1/chart/calculate
GET  /api/v1/chart/{id}/interpret       # SSE
GET  /api/v1/chart/{id}/transits/interpret  # SSE
GET  /api/v1/chart/{id}/forecast/daily|weekly|monthly
POST /api/v1/auth/register
POST /api/v1/auth/login
GET  /api/v1/auth/google
GET  /api/v1/payments/subscription       # текущая подписка
POST /api/v1/clients                    # CRM (Premium)
```

### `api/client.js` — не единственный путь к бэкенду

Часть компонентов ходит на бэкенд прямым `fetch`/`authFetch`, минуя обёртки
`client.js`: `TransitTimeline.jsx` (сама собирает URL транзитов и разбора
события), `LunarCalendarPage.jsx` и `PlannerPage.jsx` (обе сами дергают
`/calendar/lunar`, каждая своим `fetch`). Обнаружено 30.08.2026 при уборке
мёртвого кода: удаление неиспользуемых функций `getTransits`,
`streamTransitEventInterpretation`, `getLunarCalendar` из `client.js` не
затронуло сами эндпоинты — их продолжают звать теми же путями напрямую.

**Следствие: мёртвая обёртка в `client.js` не означает мёртвый эндпоинт.**
Перед тем как удалять бэкенд-ручку по признаку «не вызывается из
client.js», grep обязателен по всему `frontend/src` на сам URL-путь
(`fetch(`.../transits`` и т.п.), а не только по имени функции-обёртки.

### Ключ квоты `"transit_ai"` общий на разбор транзитов и три прогноза

`tier_limiter.check_transit_ai_limit` / `commit_transit_ai`
(`auth/rate_limits.py`) стоят не только на `/transits/event/interpret`, но
и на `/forecast/daily`, `/forecast/weekly`, `/forecast/monthly` — все четыре
списывают один и тот же счётчик `"transit_ai"`, то есть общую квоту
`transits_ai_per_month` (3 в месяц у Веги).

Сегодня прогнозные ручки недостижимы из интерфейса — их единственный
потребитель, `ForecastScale.jsx`, удалён коммитом `f82b49c`. Поэтому квоту
фактически расходует только AI-разбор транзитного события. Если прогнозы
когда-нибудь оживят на фронте, витрина «AI-разбор аспектов транзитов, 3 в
месяц» (`constants.js`, `/pricing`, `/orion`) перестанет быть правдой —
квота начнёт тратиться и на то, что там не описано. Править оба места
вместе.

---

## AI-контуры — их три, и это не одна цепочка

До 23.08.2026 здесь была строка «GPT-4o → DeepSeek V3 → Template engine». Она
противоречила разделу «Тарифы» выше и не описывала ни один реальный путь
исполнения. Ниже — то, что фактически делает код.

**1. Интерпретация натальной карты** (`interpretation/router.py`).
Порядок движков строит `_engines_for_tier`, и **DeepSeek стоит первым всегда**,
включая pro и premium:

```
DeepSeek v4-pro → GPT-4o → Template
   (всегда)      (только pro/premium,   (всегда доступен)
                  и только если DeepSeek
                  упал или не прошёл
                  валидацию ответа)
```

Модель DeepSeek одна на все тарифы — `TIER_FLAGS[*]["ai_engine"]`
(`auth/rate_limits.py`) во всех четырёх тарифах равен
`settings.deepseek_model_pro`. Тариф регулирует **глубину** через
`interpretation_word_limit` (500/800/2500/5000), а не модель — это решение
владельца 19.08.2026, см. раздел «Тарифы».

⚠️ Докстринг модуля `interpretation/router.py` («GPT-4o → DeepSeek → Template
fallback chain») и его же комментарий в классе `InterpretationRouter` — тот же
устаревший текст. Ориентироваться на `_engines_for_tier`/`select_model`, не на
докстринг. `select_model` возвращает «предпочтительный» движок, но им
определяется только, попадёт ли GPT-4o в список вообще, — первым в очереди он
не становится никогда.

**2. Чат Астреи (RAG)** — `interpretation/rag_router.py`, отдельно и всегда
`settings.deepseek_model_flash`. К цепочке выше отношения не имеет.

**3. Прогнозы (daily/weekly/monthly) и общий астрокалендарь** — `main.py`,
прямые вызовы `httpx` **мимо** `InterpretationRouter`:

```
Anthropic Claude Sonnet (claude-sonnet-4-20250514) → OpenAI GPT-4o
```

Ключи `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` этот контур читает через
`os.getenv` напрямую, а не из `Settings` (`config.py`) — в `Settings` их нет
вовсе. Общий суточный бюджет `BudgetTracker` при этом делится со всеми тремя
контурами (ключ бюджета для прогнозов — `"claude"`); при исчерпании
интерпретации принудительно уходят в Template.

---

## Чеки по 54-ФЗ формируются вручную — автоматизировать через ЮKassa нельзя

**ЮKassa закрыла сервис «Чеки для самозанятых» 29.12.2025** (ответ поддержки,
получен владельцем 22.08.2026). Практическое следствие: чек по НПД не
сформировать **ни через личный кабинет, ни передачей `receipt` в API** —
передавать его больше некуда, приёмника не существует.

Поэтому блока `receipt` в `backend/payments/yookassa_router.py` нет, и это не
недоделка, а осознанное состояние. Не «добавить чек» при следующем заходе в
файл: код станет отправлять поле, которое ЮKassa не обработает.

Доход владелец регистрирует **вручную в приложении «Мой налог»**. Чтобы для
этого не ходить в кабинет ЮKassa, вебхук на `payment.succeeded` шлёт в
служебный Telegram сумму, дату, email плательщика и срок подписки
(`_notify_payment`). Уведомление молча пропускается, если не задан
`TELEGRAM_SUPPORT_CHAT_ID` — тогда о платежах узнать неоткуда, кроме кабинета.

**Открытая задача: автоматизация чеков через API ФНС напрямую**, мимо
ЮKassa. Это единственный оставшийся путь. Отдельная работа: своя регистрация
в качестве партнёра ФНС, свой контур авторизации, свой формат — с платёжной
интеграцией не пересекается и в неё не встраивается.

Проверено практикой до ответа поддержки: после живого платежа 22.08.2026 и
после возврата в «Мой налог» не появилось ни одного чека.

---

## Вебхук ЮKassa держится на определении IP — три вещи, ломающие его молча

Уведомления ЮKassa **не подписывает**. Вся аутентификация отправителя — сверка
его адреса с `YOOKASSA_NETWORKS` (`payments/yookassa_router.py`). Значит любая
поломка в цепочке «кто у нас считается клиентом» отбивает вебхуки целиком, и
отказ выглядит одинаково: деньги списаны, подписка не выдана, в логе ERROR.

**1. Литерал `127.0.0.1` в `proxy_pass` — условие работы, а не небрежность.**
`deploy/opt-astro/nginx/astreatime.conf`, восемь `location`-блоков. Не менять
на имя сервиса (`http://api:8000`): сеть `astro_net` поднята с
`enable_ipv6: true` (`docker-compose.yml`), резолв имени может уйти в AAAA, и
peer, который увидит приложение, станет IPv6-шлюзом `fd00:cafe:1::1`. Его нет
в `TRUSTED_PROXY_IPS` → `client_ip` перестаёт доверять `X-Forwarded-For` и
возвращает сам шлюз → адрес не входит в подсети ЮKassa → **403 на все
вебхуки**. Сегодня безопасно ровно потому, что оба звена пути прибиты к IPv4:
литерал в `proxy_pass` и публикация порта `127.0.0.1:8000:8000`. Комментарий
об этом стоит в конфиге рядом с `proxy_pass` в `location /api/`.

Обсуждено и **сознательно не сделано** 23.08.2026: не расширяем
`TRUSTED_PROXY_IPS` IPv6-шлюзом и не выключаем `enable_ipv6`. Правка сетевых
настроек прода ради гипотетической будущей ошибки дороже самой ошибки —
вместо неё комментарий и уведомление (п. 3).

**2. `TRUSTED_PROXY_IPS = 172.28.0.1` — это шлюз docker-сети `172.28.0.0/16`.**
Пересоздание сети с другой подсетью (docker выдаёт следующую свободную, если
диапазон занят) тихо ломает вебхуки тем же способом: шлюз станет другим,
доверия к `X-Forwarded-For` не будет. Проверять так:

```
docker network inspect astro_astro_net --format '{{range .IPAM.Config}}{{.Subnet}} {{.Gateway}}{{"\n"}}{{end}}'
```

IPv4-шлюз из вывода обязан совпадать со значением `TRUSTED_PROXY_IPS` в
`/opt/astro/.env`. Не совпал — платежи не активируются.

**3. При отказе по IP владельцу уходит уведомление в Telegram**
(`_notify_ip_reject`, добавлено 23.08.2026). Раньше отказ был виден только в
логе контейнера, а логи не переживают деплой — узнать о сломавшемся фильтре
было неоткуда. В сообщении: адрес отправителя, число отказов за окно, дата
сверки списка подсетей (`YOOKASSA_IP_LIST_CHECKED`), время.

Три вещи в нём сделаны намеренно и выглядят как недоделки:
- **тело запроса не пересылается** — там платёжные данные, а чат служебный;
- **троттл ровно один час** (`SET ... EX 3600 NX` в Redis, ключи
  `yookassa:ip_reject_*`). ЮKassa ретраит доставку, а сломавшийся фильтр
  отбивает всё подряд: без окна владелец получит сотни сообщений и перестанет
  их читать. Счётчик сбрасывается в момент отправки, поэтому в сообщении
  число «за период», а не с начала времён;
- **при недоступном Redis уведомление пропускается молча** — без счётчика
  нечем сдержать лавину, и завалить чат хуже, чем не уведомить. Не «чинить»
  на fail-open.

Отправка не влияет на ответ вебхука: как отвечали 403, так и отвечаем, любая
ошибка Telegram проглатывается (как в `_notify_payment`).

---

## CSP работает в режиме наблюдения — боевую ещё не включали

`deploy/opt-astro/nginx/snippets/csp.conf` отдаёт заголовок
`Content-Security-Policy-Report-Only` (строка 26), боевая строка рядом
закомментирована (27). **Браузер сейчас ничего не блокирует** — только пишет
нарушения в консоль. По замыслу так собирают список нарушений 1–2 недели, а
потом меняют строки местами.

**TODO — отдельной задачей: включить боевую CSP.** Обязательный шаг при
включении: проверить, что не отвалилась Яндекс.Метрика. Ей нужны три
источника `mc.yandex.ru` — в `script-src`, `img-src` и `connect-src` (все три
уже прописаны 22.08.2026). В Report-Only ошибка в любом из них незаметна:
счётчик продолжает работать, потому что ничего не блокируется. После
включения боевой недостающий источник тихо срежет часть визитов — падения не
будет, будут неверные цифры.

Второе, что проверить при включении: `script-src` не должен получить
`'unsafe-inline'`. Ради этого из `index.html` вынесена регистрация service
worker (в `main.jsx`) и не используется инлайновый сниппет Метрики из её
кабинета (`frontend/src/analytics.js`). Инлайн в `index.html` не выполнится
и сейчас — там про это стоит комментарий.

---

## `/chart/save-anonymous` создаёт дубль, а не привязывает карту

`POST /api/v1/chart/save-anonymous` (`main.py:799`) **пересчитывает карту
заново и вставляет новую строку** с `user_id=user.id` (`main.py:854`).
Исходную анонимную запись (`user_id IS NULL`) он не привязывает, не помечает
и не удаляет — она остаётся жить сама по себе. Ни FK, ни флага, ни общего
идентификатора между двумя записями нет.

Три следствия, обнаружено 22.08.2026:

1. **Конверсия воронки «аноним построил карту → зарегистрировался» по данным
   не восстановима.** Совсем: связи между записями не существует, а
   сопоставление по дате рождения и месту — догадка, а не метрика. Считать
   можно только начав писать событие в `events` при вызове этой ручки
   (одна строка `log_event`), и только вперёд — задним числом ничего.
2. **База копит парные записи.** Каждая сохранённая после регистрации карта
   существует в двух экземплярах: анонимном и пользовательском. Удаления
   `natal_charts` в бэкенде нет нигде — `expires_at` (`models.py:138`) только
   закрывает доступ в `resolve_chart_access` (`main.py:523`), саму строку не
   трогает. То есть анонимные записи не истекают, а накапливаются навсегда.
3. **Зато метрика «сколько карт построено без регистрации» считается точно и
   по всей истории** — `NatalChart.user_id IS NULL`, отдаётся в
   `/api/v1/admin/stats` (`anon_charts`).

`access_token` в модели помечен комментарием «гасится при claim»
(`models.py:136`), но claim-флоу для карт не существует: единственный `claim`
в кодовой базе — пилотный (`pilot/router.py:91`) и к картам отношения не
имеет. Комментарий описывает несуществующий механизм.

---

## Мониторинг очереди Celery — два контура, и они ловят разное

Добавлено 31.08.2026. До этого мониторинга у очереди не было никакого: ни
Flower, ни Sentry в воркере, ни обработчиков сигналов. `result_expires=3600`
стирает результат через час, логи не переживают перезапуск контейнера —
упавшая задача не оставляла следов нигде.

| Контур | Что ловит | Где живёт |
|---|---|---|
| `task_failure` (`celery_app.py`) | задача **запустилась и упала** | воркер Celery |
| метка + сторож (`beat_watchdog.py`) | задача **не запустилась вовсе** | контейнер `api`, вне очереди |

**Второй не заменяется первым.** Не стартовавший `beat` не падает — он просто
ничего не делает, сигнала о падении нет. Именно этот отказ страшнее: пока
`expire_subscriptions` не идёт, истёкшие платные подписки не понижаются до
free, то есть оплаченный месяц не заканчивается.

### Сторож обязан жить вне очереди, за которой следит

Задача-сторож внутри той же очереди встанет вместе с ней и промолчит ровно
тогда, когда нужна. Поэтому проверка — HTTP-ручка
`POST /api/v1/internal/beat-watchdog` в контейнере `api`, а дёргает её
systemd-таймер на хосте через уже существующий `09-internal-cron.sh`. Ни
воркер, ни `beat`, ни брокер в этом пути не участвуют.

Единственная общая точка отказа — сам Redis, и она обработана явно: его
недоступность трактуется как отказ по существу и тоже шлёт уведомление
(брокер Celery — тот же Redis, значит очередь и правда стоит).

### Что здесь выглядит как недоделка, но сделано намеренно

- **Метка без TTL.** С TTL «метки нет» означало бы два разных состояния —
  «протухла» и «никогда не писалась», — различить их было бы нечем.
- **Ошибка записи метки не роняет задачу.** Полезная работа уже закоммичена;
  ронять её из-за недоступного Redis значит превратить сбой мониторинга в сбой
  того, за чем он следит. Худшее последствие — ложная тревога.
- **Порог 26 часов и время таймера 07:30 UTC подобраны в паре** с расписанием
  задачи (05:00 UTC): здоровый возраст метки ~2.5 ч, при одном пропуске
  ~26.5 ч. Сдвигая одно, сдвигать второе. Закреплено тестом
  `test_threshold_matches_timer_schedule`.
- **Сторож отвечает 200 даже при тревоге.** 200 значит «проверка выполнена», а
  не «всё хорошо»; результат в поле `status`. Не-2xx оставлял бы красный
  systemd-юнит при исправном стороже.
- **Троттл падений — по имени задачи**, а не один на всю очередь (в отличие от
  `_notify_ip_reject`). Разложившаяся задача иначе заглушит первое падение
  соседней.
- **Обработчик падений завёрнут в свой `try/except`, хотя Celery уже ловит
  исключения получателей сигнала** (`Signal.send`, проверено по исходнику).
  Причина не в страховке, а в маскировании: перехват Celery пишет полный
  трейсбек обработчика через `logger.exception` вплотную к настоящему падению
  задачи, и читающий видит два трейсбека, не понимая, какой из них причина.
- **Об успехе не сообщаем.** Ежедневное «всё хорошо» перестают читать через
  неделю, после чего оно не отличается от отсутствия мониторинга.

### ⚠️ Юниты таймера НЕ доставляются деплоем

Код (`beat_watchdog.py`, `celery_app.py`, `tasks.py`) едет сам — он в образе.
А `deploy/opt-astro/systemd/astro-beat-watchdog.{service,timer}` приезжают в
`/opt/astro/app` с `git pull` и **там остаются**: `05-update.sh` синхронизирует
рядом с `app/` только четыре файла, юнитов среди них нет, а в
`/etc/systemd/system` их не копирует никто. `08-setup-automation.sh` копирует,
но сам не синхронизируется и использует относительный путь, из `/opt/astro` не
разрешающийся.

Значит: **ставить руками один раз, и любую последующую правку этих двух файлов
тоже переносить руками.** Блок команд — в `FIXES_13.md`. Так оставлено
сознательно: чинить доставку systemd-юнитов — отдельная задача.

Следят пока за одной задачей (`WATCHED_TASK`) — решение владельца 31.08.2026,
сначала смотрим на одной. Падения остальных контур `task_failure` ловит уже
сейчас, не покрыт только их незапуск.

---

## Swiss Ephemeris — синхронный, не вызывать напрямую из async-хендлеров

`calculate_full_chart`, `calculate_transits`, `compute_exact_facts` и всё,
что в итоге вызывает `pyswisseph` (`backend/ephemeris/calculator.py`,
`backend/transit/engine.py`, `backend/ephemeris/synastry.py`,
`backend/ephemeris/solar_return.py`, `backend/calendar/lunar_engine.py`,
`backend/transit/house_passages.py`) — обычные `def`, не `async def`.
`api` — один процесс (`start.sh`, без `--workers`), один asyncio event loop
на все запросы разом. Вызов такой функции напрямую внутри `async def`
блокирует ЭТОТ event loop на всё время расчёта — не только для вызвавшего
запроса, для вообще всех, кто в этот момент обращается к серверу (чат,
чужие интерпретации, health-check).

Обнаружено 20.08.2026 сразу в трёх независимых местах (`main.py:calculate_chart`,
`crm/router.py:_geocode_and_build_chart`, `interpretation/rag.py:build_transits_block`
из чата) — из кода это не очевидно, ошибка тихая (не падает, просто копится
задержка под нагрузкой), поэтому фиксируется здесь явно.

**Правило**: в любом `async def`-обработчике (FastAPI route, `_scheduler_loop`
в `main.py`) вызов такой функции — только через
`await asyncio.to_thread(calculate_full_chart, ...)` (или аналог), не напрямую.
Внутри Celery-задач (`@celery_app.task`, `backend/tasks.py`, роли `worker`/`beat`)
прямой синхронный вызов — нормально: там нет общего event loop, блокировать
нечего, воркер и так один запрос за раз обрабатывает.

---

## Деплой

Всё на одном сервере: **Timeweb VPS**.

```
push → main
  ├── GitHub Actions: pytest (backend) + vite build (frontend)
  └── deploy job (SSH на VPS) → ./05-update.sh
        git pull → dump БД → docker compose up -d --no-deps api bot worker beat
        → alembic upgrade head → /health check → rollback при ошибке
        → фронтенд пересобирается тем же скриптом, если менялись
          frontend/ или deploy/opt-astro/nginx/
```

### Три слоя доставки

Проверено боем 31.08.2026, деплоем `33369269046` (лог сохранён в
`FIXES_16.md` предыдущего коммита). Ниже — как это работает по факту этого
прогона, не по замыслу.

**1. Три слоя, и у каждого свой способ доехать до сервера.**

| Слой | Кто доставляет | Условие |
|---|---|---|
| Образ бэкенда (`api`, `bot`, `worker`, `beat`) | `05-update.sh` напрямую (`docker compose build` + `up`) | безусловно, каждый запуск |
| Фронтенд (`dist`) и конфиг nginx | `04-frontend-deploy.sh`, вызывается из `05-update.sh` | по флагу — см. п. 2 |
| Сами деплой-скрипты (`05-update.sh`, `04-frontend-deploy.sh`, `docker-compose.yml`, `07-backup-cron.sh`) | `05-update.sh`, копирование `app/deploy/opt-astro/*` поверх рабочих копий | безусловно, каждый запуск (кроме `--frontend-only`) |

Бэкенд пересобирается и перезапускается **всегда**, даже если ничего не
менялось. Фронтенд и nginx — единственный условный слой.

**2. Флаг фронтенда: `frontend/` ИЛИ `deploy/opt-astro/nginx/`, от метки.**
Поднимается, если хоть один из двух путей отличается между
`/opt/astro/.frontend-deployed-rev` и `HEAD`. Метка двигается только после
того, как `04-frontend-deploy.sh` вернул 0 — то есть упавший деплой не теряет
изменения, следующий запуск их доделает. Подробности и разбор случаев —
ниже, «Метка `.frontend-deployed-rev`».

До 31.08.2026 флаг считался по диффу **внутри одного `git pull`** — конфиг
nginx отстал на четыре недели именно из-за этого: коммит, трогающий только
`deploy/opt-astro/nginx/`, флага не поднимал, а повторные запуски делали
пустой `pull` и рапортовали успех, ничего не доставив. Сегодняшний прогон
подтвердил новую логику дословно: `git pull` не принёс изменений
(`уже на актуальном коммите`), но метки ещё не было — скрипт написал
`метки .frontend-deployed-rev нет или она нечитаема — считаю, что фронтенд
изменился` и выполнил полный цикл 04: сборка, публикация `dist`, обновление
конфига, `nginx -t`, `reload`, запись метки.

**3. `04-frontend-deploy.sh` синхронизируется вместе с тремя остальными
файлами.** До 31.08.2026 не синхронизировался вовсе — правки в него
приходилось класть на сервер руками, иначе они не доезжали никаким числом
коммитов и повторных запусков.

Правка самого `05-update.sh` (и добавление в его список `04`) доезжает через
**два деплоя подряд**, не через один — это не дефект, а следствие того, что
скрипт синхронизирует файлы на диске, но продолжает исполнять уже запущенный
процесс:

- деплой №1 (`33368262693`, 31.08.2026 07:24): `git pull` принёс новую версию
  `05-update.sh`, она легла на диск — но исполнялся ещё старый процесс со
  старым списком из трёх файлов. Лог: `синхронизировано: 05-update.sh,
  docker-compose.yml, 07-backup-cron.sh` — без `04`, без строки про метку.
- деплой №2 (`33369269046`, 31.08.2026 07:38): исполнялась уже новая версия.
  Лог: `синхронизировано: 05-update.sh, 04-frontend-deploy.sh,
  docker-compose.yml, 07-backup-cron.sh`, затем строка про отсутствующую
  метку и полный вызов `04`.

**4. НИКОГДА не запускать `04-frontend-deploy.sh` (и `05-update.sh`) целиком
под `sudo`.** Скрипты берут `sudo` точечно сами, где он нужен. Запуск под
root делает `nginx-backup` и `frontend/dist` root-овыми, и следующий обычный
запуск от `deploy` падает на `mkdir`/`rm` этих каталогов — так уже ломали
30.08.2026.

**5. Порядок действий при накопившемся отставании** (когда бэкенд и фронтенд
разошлись сильнее обычного): сначала бэкенд через обычный деплой (Actions),
затем `./04-frontend-deploy.sh --nginx-check` от `deploy`, без `sudo` — он
копирует, проверяет `nginx -t` и откатывает, ничего не доставляя, — и только
при зелёной проверке запускать настоящий деплой фронтенда. Откат конфига при
проблеме — копии в `/opt/astro/nginx-backup/`, ротация хранит последние 10.

**6. `/etc/nginx/nginx.conf` правился руками на сервере 30.08.2026** —
закомментированы `ssl_protocols` и `ssl_prefer_server_ciphers` (строки 33–34
файла на сервере). Дистрибутивные строки Ubuntu конфликтовали с
`conf.d/00-astro-hardening.conf`: hardening из-за них не вставал. **В
репозитории этой правки нет** — `nginx.conf` не наш файл, деплой его не
трогает. При переустановке сервера — повторить эту правку заново, иначе
`nginx -t` при следующем деплое конфига может не пройти тем же способом.

**7. `08-setup-automation.sh` — второй независимый писатель в
`/etc/nginx/snippets/`**, о существовании `04-frontend-deploy.sh` не знает и
наоборот. Оба кладут файлы в один и тот же каталог, не координируясь. Не
трогали — только фиксация факта, подробнее в разделе «nginx: конфиг в
репозитории — не тот, что работает на сервере» ниже.

### Метка `.frontend-deployed-rev` — почему флаг фронтенда считается не по pull

До 31.08.2026 `frontend_changed` выводился из диффа `before_rev..after_rev`
одного `git pull`. У этого было два молчаливых отказа, оба с зелёным джобом:

1. **Коммит, трогающий только `deploy/opt-astro/nginx/`, флага не поднимал.**
   Конфиг nginx кладёт на сервер тот же `04-frontend-deploy.sh`, но в списке
   путей стоял один `frontend/`. Конфиг не уезжал вообще никогда, ни при каком
   числе повторных запусков — так он и отстал на четыре недели.
2. **Изменения терялись навсегда при падении после `pull`.** Упал healthcheck,
   сработал откат — следующий запуск делает пустой `pull`, флаг `false`,
   фронтенд молча остаётся старым. То же самое при `--backend-only`, который
   честно вычислял флаг и выбрасывал его.

Теперь точка отсчёта — **`/opt/astro/.frontend-deployed-rev`**: один коммит-хеш,
последний, для которого `04-frontend-deploy.sh` вернул 0. Флаг = дифф от метки
до `HEAD` по `FRONTEND_PATHS` (`frontend/` + `deploy/opt-astro/nginx/`).

Что здесь намеренно и выглядит как недоделка:

- **Файл лежит в `/opt/astro`, а НЕ в `app/`.** `app/` — рабочая копия git,
  посторонний файл в ней блокирует следующий `git pull` (уже роняло прод).
- **Нет метки → считаем, что фронтенд изменился.** Первый запуск после этой
  правки пересоберёт фронтенд один раз; дальше метка есть, и лишних пересборок
  не будет. Так же обрабатываются пустая метка и коммит, которого нет в
  репозитории (переклонировали `app/`). Лишняя пересборка безопасна, не
  пересобрать нужное — нет.
- **Ошибка записи метки НЕ роняет деплой.** Фронтенд к этому моменту уже
  доставлен, ронять запуск после успеха нельзя. Худшее последствие — лишняя
  пересборка в следующий раз, и о ней есть громкое предупреждение в stderr.
  Реальный сценарий: файл остался root-овым после запуска `05-update.sh` под
  `sudo`.
- **Метка не двигается, когда фронтенд пропущен как неизменившийся.** Она
  означает «последнее, что доставлено», а не «последнее, что видели».

⚠️ **Никогда не запускать `05-update.sh` и `04-frontend-deploy.sh` под `sudo`.**
Оба рассчитаны на пользователя `deploy` и берут `sudo` точечно там, где он
нужен. Запуск целиком под `sudo` делает root-овыми `app/.git`,
`app/frontend/node_modules` и метку — после чего обычный запуск падает или
молча теряет метку. Каталоги, за которые это уже ломалось (`nginx-backup`,
`frontend/dist`), 31.08.2026 переведены на «всегда `sudo`»: root перезапишет
любого владельца, а `deploy` root-овый каталог удалить не может — поэтому
лечение именно такое, а не «убрать `sudo` везде».

✅ **Больше не критично: `05-update.sh`, `04-frontend-deploy.sh`,
`docker-compose.yml` и `07-backup-cron.sh` синхронизируются сами.** Раньше
`git pull` на сервере обновлял только каталог `app/`, а копии этих файлов
рядом с ним не подтягивались автоматически. С 14.08.2026 `05-update.sh` сам
копирует их из `app/deploy/opt-astro/` поверх рабочих копий сразу после
`git pull` — при каждом обычном запуске и при `--backend-only` (не при
`--frontend-only`, там `git pull` для `app/` не выполняется). Ручной шаг
больше не нужен. (nginx-конфиги — отдельный случай, их копирует сам
`04-frontend-deploy.sh` при каждом запуске, это не менялось).

**`04-frontend-deploy.sh` добавлен в список только 31.08.2026** — до этого
его не синхронизировал никто, и на сервере исполнялась копия, положенная
руками неизвестно когда: правки этого файла в репозитории не доезжали до
прода вообще никак. Он же единственный доставщик конфига nginx, сниппетов,
`conf.d` и собранного `dist`, поэтому его собственная несинхронность была
корнем сразу нескольких «правка закоммичена, а на проде её нет».

Пропущенный вручную шаг уже дважды ронял прод из-за этого рассинхрона:
09.08.2026 — новые прод-guard'ы уже действовали в приложении, а
preflight-проверки и логика отката в `05-update.sh` на сервере ещё были
старыми; 14.08.2026 — фикс в `07-backup-cron.sh` был закоммичен, но
ночной бэкап продолжал падать, потому что таймер дёргал старую копию
скрипта рядом с `app/`. Второй случай и стал поводом сделать
синхронизацию автоматической.

⚠️ **У автосинхронизации есть свой риск: `05-update.sh` копирует новую
версию самого себя поверх себя же, пока сам ещё выполняется** (`cp` в
блоке синхронизации, не atomic rename). 19.08.2026 это уронило деплой:
правка `05-update.sh` в этом же коммите изменила размер файла настолько,
что чтение скрипта bash'ем съехало по байтовым офсетам сразу после
самокопирования — `syntax error near unexpected token` на строке, где в
новом файле стоит безобидный `fi`. Раньше это «случайно» не всплывало,
потому что старая и новая версия были близки по размеру/структуре в
районе того места, откуда bash дочитывает буфер.

Практическое следствие: **любая правка `deploy/opt-astro/05-update.sh`
(включая мелкие) — операция с повышенным риском для ближайшего деплоя**,
не только смена preflight-проверок. Правильный фикс — писать во временный
файл и переносить атомарным `mv` вместо `cp` поверх работающего процесса;
это отдельная задача, специально отложенная (см. ниже), не путать с
обычными правками скрипта.

✅ **Сделано (`ce3c349`, 20.08.2026): самокопирование переведено на
temp-файл + atomic `mv`.** Правка `05-update.sh` от 21.08.2026 (preflight
ЮKassa) — первый деплой, который снова меняет размер этого файла, то есть
первая практическая проверка фикса. Если деплой упадёт с `syntax error
near unexpected token` — причина именно здесь, а не в самой правке.

- **Backend**: Docker Compose (`api`, `bot`, `worker`, `beat`, `postgres`, `redis`, `uptime-kuma`), Nginx проксирует `/api/` и `/health` на `127.0.0.1:8000`. Один образ, `SERVICE_ROLE` переключает роль контейнера (`bot` — Telegram-бот, `worker`/`beat` — Celery).
- **Frontend**: деплой автоматический через CI (`04-frontend-deploy.sh`: npm run build → копирование `dist/` → reload nginx), запускается из `05-update.sh` только если менялся `frontend/`. Ручной запуск `./04-frontend-deploy.sh` на сервере остаётся доступен.
- **Бэкапы**: systemd-таймер, `pg_dump` ежедневно в 03:30 (шифруется `age`, выгружается за пределы хоста), ротация 14 дней локально.

---

## nginx: конфиг на сервере совпадает с репозиторием — с 31.08.2026, не навсегда

**С 31.08.2026 `/etc/nginx/sites-available/astreatime.conf` на сервере — это
`deploy/opt-astro/nginx/astreatime.conf` из репозитория**, доставленный
обычным деплоем (`33369269046`). Расхождение существовало с 03.08.2026 по
31.08.2026: причина и починка — в разделе «Три слоя доставки» выше, здесь не
повторяю.

⚠️ **Правило остаётся в силе, расхождение закрыто на сегодня, а не отменено
навсегда: не описывать поведение прода по файлам из `deploy/` без проверки.**
Они описывают желаемое состояние, которое механизм доставки может снова не
довезти — новым разрывом синхронизации, ручной правкой на сервере поверх
задеплоенного, или третьим писателем в тот же каталог (см. `08-setup-automation.sh`
ниже). Утверждение вида «nginx проксирует такой-то путь» требует **свежего**
вывода `nginx -T` с сервера, а не grep по репозиторию и не ссылки на этот
раздел без даты. В FIXES_4 такое утверждение было подано как проверенный факт
и оказалось неверным.

**Чем проверяется совпадение — `nginx -T`, конкретно:**

```bash
sudo nginx -T 2>/dev/null | grep -c 'location'
sudo nginx -T 2>/dev/null | grep -A3 'location /share/'
sudo nginx -T 2>/dev/null | grep -E 'csp\.conf|security-headers\.conf|00-astro-hardening'
```

Смотреть на: число `location`-блоков (репозиторный конфиг — 275 строк, короткий
серверный вариант их не содержал вовсе); блок `location /share/` (добавлен
владельцем вручную 30.08.2026, до 31.08.2026 не проксировался, уходил в
SPA-фолбэк); include’ы `csp.conf`, `security-headers.conf` и
`00-astro-hardening.conf` (их отсутствие в выводе — признак, что снова
разъехалось). Отсутствие любого из них в `nginx -T` — не «ещё не проверяли», а
сигнал регресса, требующий разбора по той же схеме, что и в этот раз.

**Как файлы попадают на сервер.** Workflow (`ci.yml`, job `deploy`) не
копирует ничего: только `ssh` и `cd /opt/astro && ./05-update.sh`.
`/opt/astro` — не репозиторий; репозиторий это `/opt/astro/app`, туда и
делается `git pull`. Дальше:

| Кто | Что копирует | Куда |
|---|---|---|
| `05-update.sh` | `05-update.sh`, `04-frontend-deploy.sh`, `docker-compose.yml`, `07-backup-cron.sh` | `/opt/astro/` |
| `04-frontend-deploy.sh` | `nginx/snippets/*.conf` | `/etc/nginx/snippets/` |
| — то же — | `nginx/conf.d/00-astro-hardening.conf` | `/etc/nginx/conf.d/` |
| — то же — | `nginx/astreatime.conf` | `/etc/nginx/sites-available/` |
| — то же — | собранный `frontend/dist/` | `/opt/astro/frontend/dist` |

Второй, независимый писатель в `/etc/nginx/snippets/` — `08-setup-automation.sh`
(строка 177, ручной одноразовый установщик панели `status.astreatime.ru`). Два
скрипта пишут в один каталог, ни один не знает о другом; трогать это не стали.
`nginx/status.astreatime.conf` не доставляет никто из автоматики — только он же,
причём по относительным путям, которые из `/opt/astro` не разрешаются.

`04-frontend-deploy.sh` запускается из `05-update.sh` **только если менялись
`frontend/` или `deploy/opt-astro/nginx/`** (или при `--frontend-only`).
Как именно считается «менялись» — в разделе «Метка `.frontend-deployed-rev`»
ниже.

Практическое следствие: **файл nginx в репозитории не декоративный.**
Первый же деплой с изменением фронтенда перезапишет живой конфиг целиком,
безусловным `cp`, без резервной копии. Ручные правки на сервере переживут
ровно до этого момента.

Второе следствие, менее очевидное: копирование идёт **до** `nginx -t`, а в
скрипте стоит `set -euo pipefail`. Если проверка не пройдёт, скрипт умрёт
уже после `cp` — в памяти останется старый конфиг, на диске будет лежать
новый и нерабочий. Сайт продолжит работать, а упадёт при следующем
`reload` или перезагрузке, когда связь с деплоем уже забудется.

Разбор того, что именно приедет при следующем деплое фронтенда и чем это
грозит, — в `FIXES_11.md`.

---

## Деплой — обязательная процедура

### Разделение ответственности

Claude Code **не имеет SSH-доступа к серверу**. Всё, что делается на VPS,
выполняет владелец руками. Поэтому Claude Code обязан:

- никогда не предлагать редактировать файлы напрямую на сервере;
- любую правку вносить только в репозиторий и доставлять через `git push`;
- если нужно действие на сервере — выдать **готовый блок команд одним куском**,
  без пояснений между строками, чтобы владелец вставил их одной вставкой;
- в командах не использовать `sed` с текстом, содержащим `|`, `&`, кавычки или
  кириллицу — только `python3 - <<'PYEOF'` с точным поиском строки и проверкой
  `assert count == 1` (обязательные детали — в разделе «Правка файлов скриптом»
  ниже).

### Правка файлов скриптом: две проверки, и одна другую НЕ заменяет

Заметка из практики 23.08.2026, оба случая произошли в один день.

**1. `assert count == 1` не доказывает, что замена применилась.** Он проверяет
только, что искомая подстрока в файле есть. Если `old` собран из двух кусков,
между которыми в файле стоит что-то ещё (перенос строки, продолжение с `\`),
то `t.replace(old, new)` молча ничего не сделает, а скрипт отрапортует успех.
Так тест `test_foreign_currency` остался с прежним `assert 400`, и обнаружилось
это только по упавшему прогону.

**Правило: после каждой замены — `assert t2 != t`.** Обе проверки обязательны:
`count == 1` ловит неоднозначный или отсутствующий якорь, `t2 != t` ловит
несработавшую замену. Ни одна из них не покрывает вторую.

```python
assert t.count(old) == 1, f"якорь: {t.count(old)}"
t2 = t.replace(old, new)
assert t2 != t, "замена не применилась"
p.write_text(t2, encoding="utf-8")
```

**2. В heredoc этого окружения съедаются обратные слеши.** `"\\\n"` внутри
python-строки доезжает как литеральные символы `\` и `n`, а не как
«backslash + перевод строки». Один раз это записало literal `\n` в середину
`deploy/opt-astro/05-update.sh` — файл повышенного риска, ошибка ловится только
`bash -n`.

Отсюда: **не писать обратные слеши в python-коде внутри heredoc.** Нужен
перевод строки или слеш в результате — собирать через `chr(10)` / `chr(92)`
или короткими якорями без продолжений строк. После правки любого shell-скрипта
— `bash -n <файл>` перед коммитом.

### Железное правило: сервер не редактируем

Каталог `/opt/astro/app` — это рабочая копия git. Любая ручная правка в нём
блокирует следующий `git pull` и роняет деплой. Уже случалось дважды.
Если правка нужна срочно — сделать её на сервере, **сразу** продублировать в
репозиторий и при первом же деплое откатить локальную версию через
`git checkout --`.

### Файлы, которые портятся сами

`npm run build` (он же `04-frontend-deploy.sh`) на каждом запуске
перезаписывает файлы, которые лежат в репозитории. Из-за этого `git status` на
сервере никогда не бывает чистым, и `git pull` падает:

| Файл | Кто портит |
|---|---|
| `frontend/dist/index.html` | `vite build` |
| `frontend/dist/_redirects` | `vite build` |
| `frontend/public/sitemap.xml` | `generate-sitemap.js` (запускается из `build`) |

`frontend/dist/` уже в `.gitignore` (строка 51), но два файла были закоммичены
раньше правила и продолжают отслеживаться. Лечится один раз:

```bash
git rm --cached frontend/dist/index.html frontend/dist/_redirects
echo 'frontend/public/sitemap.xml' >> .gitignore
git rm --cached frontend/public/sitemap.xml
git commit -m "chore: не отслеживать генерируемые при сборке файлы"
```

До тех пор перед каждым деплоем на сервере нужно:

```bash
cd /opt/astro/app && git checkout -- . && git status --short
```

### Порядок деплоя

1. **Claude Code**: прогнать `pytest` и `vite build` локально. Если тесты
   падают — не пушить молча. Объяснить, воспроизводятся ли падения на коде
   *до* правок, и дождаться решения владельца.
2. **Claude Code**: `git push` в `main`. Автодеплой запускается сам.
3. **Claude Code**: следить за GitHub Actions до конца, не отчитываться
   «запушено» раньше, чем отработает job `deploy`.
4. **Владелец**: если деплой упал на `git pull` — выполнить блок из пункта
   «Файлы, которые портятся сами», затем сказать Claude Code перезапустить:
   `gh run rerun <ID> --failed`.
5. **Владелец**: проверить результат по чек-листу приёмки.

`05-update.sh`, `04-frontend-deploy.sh`, `docker-compose.yml` и
`07-backup-cron.sh` больше не нужно
копировать руками — `05-update.sh` сам синхронизирует их из
`app/deploy/opt-astro/` поверх рабочих копий в `/opt/astro` сразу после
`git pull`, при каждом запуске (обычном и `--backend-only`). Исключение:
`--frontend-only` не делает `git pull` для `app/`, поэтому и синхронизацию
не запускает. Добавлено 14.08.2026 после падения ночного бэкапа — фикс в
`07-backup-cron.sh` был закоммичен, но не доезжал до сервера этим же путём,
что раньше случалось с `05-update.sh` и `docker-compose.yml`.

### Доступ сервера к GitHub

Пароли для git отключены с 2021 года. Сервер тянет код по SSH через deploy key
`~/.ssh/astro_deploy_key`, публичная часть добавлена в
Settings → Deploy keys репозитория (только чтение). Настроено 13.08.2026 после
падения деплоя с `Authentication failed`.

Если снова появится запрос логина — ключ или конфиг `~/.ssh/config` потерян,
проверять: `cd /opt/astro/app && git remote -v` должен показывать
`git@github.com:...`, а не `https://`.

### Бэкапы: что уже проверено, что нет

Прогон на проде 13.08.2026 прошёл полностью — дамп создаётся, шифруется,
расшифровывается ключом и читается как валидный архив.

- `age` ставится через `apt-get install -y age`, на чистом сервере его нет.
- Приватный ключ `/opt/astro/backups/astro-backup.key` **обязан** храниться
  вне сервера. Без него бэкапы бесполезны.
- `pg_restore -l` не читает дамп формата `-Fc` из потока — нужен файл с
  возможностью перемотки. Проверка внутри контейнера пишет во временный файл.
  Не «чинить» обратно на pipe.
- `BACKUP_S3_TARGET` не задан — копии только на этом сервере. Открытый риск:
  отказ диска уносит базу и бэкапы разом.

### Прод-гварды платежей — вернулись под ЮKassa

19.08.2026 из `backend/main.py` и `deploy/opt-astro/05-update.sh` были сняты
проверки `ROBOKASSA_IS_TEST`/`ROBOKASSA_MERCHANT_LOGIN` — вместе с самим
Robokassa (и Stripe), удалённым как мёртвый код: аккаунта и ключей никогда не
было, ни одного платежа не проходило.

21.08.2026 гварды вернулись под `YOOKASSA_SHOP_ID` / `YOOKASSA_SECRET_KEY`,
в обоих местах — и в приложении, и в деплой-скрипте (именно отсутствие второй
проверки уже дважды роняло прод из-за рассинхрона, см. раздел «Деплой»).

**23.08.2026 переменные стали ОБЯЗАТЕЛЬНЫМИ** (прежний TODO про мягкие гварды
выполнен и снят). Мягкая логика была удобна до запуска, но магазин
зарегистрирован и оплата объявлена пользователям: пустой `.env` означает молча
неработающую кнопку «оплатить», о которой узнаёшь от пользователя.

| Состояние `.env` | Поведение |
|---|---|
| обе переменные пусты или пустые строки | **старт и деплой падают** |
| задана ровно одна из двух | старт и деплой падают |
| обе заданы, ключ `test_`, прод | старт и деплой падают (иначе подписки выдавались бы без реальной оплаты) |
| обе заданы боевыми | норма |
| `DEBUG=true` или `TESTING=true` | проверка не применяется — иначе локальная разработка и CI не запустятся |

⚠️ **Практическое следствие: деплой невозможен, если переменных нет на
сервере.** Обе обязаны быть в `/opt/astro/.env` — именно оттуда их читает и
`_required` в `05-update.sh` (`grep` по файлу в рабочем каталоге), и
приложение через `Settings` (`env_file=".env"` в контейнере). Имена в коде,
скрипте и `.env.example` совпадают до символа. Магазин: `shopId = 1442186`,
модерация пройдена, боевой ключ выпущен.

Проверка «ровно одна из двух» после этого стала недостижимой (пустая половина
падает раньше) и **намеренно оставлена** в обоих местах — страхует, если
обязательность когда-нибудь смягчат, и держит отдельную внятную формулировку.
Не удалять как мёртвый код.

Синхронность двух мест проверяется тестом
`TestDeployScriptStaysInSync` (`test_yookassa_required_guard.py`): он читает
`05-update.sh` и требует обе переменные в списке `_required`. Добавляя новый
прод-гвард в `main.py`, не забыть дописать переменную в `PROD_STARTUP_ENV`
(`backend/tests/prod_env.py`) — иначе все subprocess-тесты прод-гвардов начнут
падать по чужой причине.

---

## Важные соглашения

- Не использовать `sudo` с npm
- Framer Motion — только поверх готовых компонентов
- Новые компоненты брать из 21st.dev или Ponytail, не генерировать с нуля
- Один компонент за раз, не рефакторить всё сразу
- Перед правкой файла — уточнить задачу
- Никогда не редактировать файлы напрямую на сервере — только через репозиторий
- «Готово» означает проверено в работе. Если проверить нельзя (нет браузера,
  Docker, доступа к серверу) — писать «код готов, приёмка не пройдена» и явно
  перечислять, что осталось непроверенным
- Всё, что выясняется по ходу работы, записывать сразу — в этот файл, в
  комментарий или в docstring, туда, где найдёт следующий читающий. Не ждать
  отдельной просьбы и не отдавать владельцу списком «что стоило бы записать».
  Записывать не то, что сделано (это видно из git), а **почему** и **что
  сломается, если сделать иначе**. Особая ценность — у неочевидного:
  намеренные литералы, порядок операций, места, где правильное решение
  выглядит как недоделка. Мысль «выглядит странно, но трогать нельзя,
  потому что…» — это ровно то, что надо записать.

---

## Аудит безопасности 23.08.2026

Полный аудит по коду (прод не трогался). Ниже — итог, чтобы не перепроверять
заново и не искать находки повторно.

### Проверено и держится — перепроверять не нужно

- **Сумма чекаута клиентом не управляется.** `CheckoutRequest` объявлен с
  `extra: "ignore"`, цена берётся только из `TIER_PRICES_RUB[tier]`,
  `return_url` всегда свой, `premium` отбивается явным 400.
- **Тариф и сумма в вебхуке берутся из перечитанного платежа**
  (`GET /v3/payments/{id}`), а не из тела уведомления. Подделанное тело даёт
  либо 403, либо запрос несуществующего платежа → отказ без активации.
- **Идемпотентность — на уникальном индексе `payment_events.inv_id` в БД**
  (не в Redis: там она была fail-open). Запись платежа и активация — одна
  транзакция, при падении активации общий rollback.
- **`client_ip` fail-closed** (`limiter.py`): `X-Forwarded-For` разбирается
  справа налево до первого недоверенного адреса, при пустом
  `TRUSTED_PROXY_IPS` заголовок игнорируется целиком. Подделать левые
  сегменты бесполезно.
- **BOLA по картам закрыт**: `resolve_chart_access` (`main.py`) — везде 404,
  `secrets.compare_digest` для `access_token`, проверка `expires_at`.
  Используется во всех эндпоинтах с `chart_id`. Идентификаторы — UUID.
- **CRM изолирована**: все ручки с `client_id` идут через
  `_get_client_or_404`, где стоит `ClientProfile.astrologer_id ==
  astrologer.id`. Чужого клиента не получить.
- **SSE-тикеты одноразовые**: атомарный `GETDEL`, TTL 60 с, fail-closed при
  недоступном Redis.
- **`token_version` гасит все токены разом**: смена пароля и `logout-all`
  инкрементят счётчик, и это обесценивает access, refresh, ссылку сброса
  пароля и ссылку подтверждения email одновременно. Алгоритм JWT берётся из
  настроек, не из токена (`alg: none` невозможен).

### Открытые находки — не чиним пока, но помним

Нумерация — из отчёта аудита 23.08.2026.

| № | Что | Где |
|---|---|---|
| ~~2.1~~ | ✅ Закрыто 23.08.2026: `SubscriptionOwnerMissing` + коммит записи о платеже + уведомление владельцу. Подробности — ниже, «Платёж, которому некому выдать подписку» | `payments/common.py` |
| 2.2 | `refund.succeeded` — единственное место, где телу вебхука верят: сумма и id берутся из запроса, а не перечитываются из API. Противоречит докстрингу модуля | `payments/yookassa_router.py` (`_handle_refund`) |
| ~~2.3~~ | ✅ Закрыто 23.08.2026: валюта сверяется вместе с суммой в `_handle_succeeded`, не-RUB → 400, подписка не выдаётся | `payments/yookassa_router.py` |
| ~~2.9~~ | ✅ Закрыто 23.08.2026 вместе с непригодной metadata: постоянные причины отвечают 200, платёж записывается, владелец уведомляется. Подробности — ниже, «Код ответа вебхука» | `payments/yookassa_router.py` |
| 2.4 | Гонка при активации пилотного токена: `row.used` читается и пишется без `FOR UPDATE` — один токен может выдать premium нескольким аккаунтам | `pilot/router.py` (`claim_pilot`) |
| ~~2.5~~ | ✅ Закрыто 23.08.2026: `/admin/coupons/apply` переведён на `require_admin`. Подробности — ниже, раздел «Промокоды не подключены» | `admin/promo_router.py` |
| 2.6 | Отключение клиентского портала не отзывает ссылку: токен не ротируется, `expires_at` не сбрасывается — после повторного включения старая ссылка снова работает | `crm/router.py` (`set_portal`) |
| 3.1 | `/payments/checkout` без собственного rate limit (только глобальный 300/мин и nginx-зона `api`), а каждый вызов создаёт платёж на стороне ЮKassa | `payments/yookassa_router.py` |
| 2.10 | **Оферта п. 4.5 утверждает неправду.** «Исполнитель… формирует чек в момент расчёта и направляет его Пользователю на электронную почту (ч. 3 ст. 14 ФЗ-422)» — этого не происходит: ЮKassa закрыла «Чеки для самозанятых» 29.12.2025, чек оформляется вручную в «Мой налог» и пользователю не отправляется. Правится отдельным коммитом: две правки правового документа смешивать нельзя, при откате одной полетит вторая | `oferta_final.md` + `frontend/src/pages/TermsPage.jsx` (пара) |
| 3.6 | `react-router` ≤7.17.0 — open redirect через protocol-relative URL (GHSA-2j2x-hqr9-3h42), 3 moderate в `npm audit` | `frontend/package.json` |

Требует действий владельца (не Claude Code): `key_githab.txt` и
`ключ для бэкап.txt` лежат в корне репозитория untracked и **не покрыты**
`.gitignore` — любой `git add -A` их закоммитит, а gitleaks в CI untracked-файлы
не видит. Ключ от бэкапов по правилам обязан храниться вне сервера.

---

## Правовые документы — пара .md + .jsx, и она обязана совпадать

`oferta_final.md` ↔ `TermsPage.jsx` и `privacy_final.md` ↔ `PrivacyPage.jsx`.
`.md` программно **не читается** ничем — ни сборкой, ни тестами; синхронизация
ручная, по конвенции из шапки `.jsx`. Правишь одно — правишь оба в том же
коммите.

Что тянется за правкой текста оферты:

1. **`CURRENT_TERMS_VERSION`** (`backend/auth/consent.py`) — дата редакции.
   Используется ТОЛЬКО при создании аккаунта: штампует
   `user.consent_terms_version`. Сравнения с ней нигде нет — ни повторного
   согласия, ни баннера, ни разлогина, механизма не существует. Подъём версии
   ничего не ломает и никого не трогает: старые пользователи сохраняют старый
   штамп, новые получают новый. Читается только в выгрузке `/profile/export`.
2. **Строка «Дата публикации» в `.jsx`** — обязана совпадать с константой,
   это проверяет `test_consent_versions.py`. Разойдутся — упадёт тест.
3. **Комментарий в шапке `.jsx`** с датой редакции исходника.

Меняются все три разом, иначе документ утверждает неправду о самом себе.
Приватность и оферта версионируются **независимо** (две константы) — правка
одной не поднимает вторую.

⚠️ **Версия — это календарная дата, поэтому две редакции одного дня получают
одну версию.** 23.08.2026 так и вышло: утром правился п. 4.3 (сгорание
остатка), вечером п. 4.5 (чек) — обе редакции записаны пользователям как
`2026-08-23`, различить их по `consent_terms_version` нельзя. Ставить завтрашнюю
дату, чтобы «развести» версии, нельзя: документ будет утверждать, что
опубликован в будущем. Если понадобится точность внутри дня — менять сам формат
версии (например добавить порядковый номер редакции), а не подкручивать дату.

**П. 10.1 оферты требует уведомить пользователей за 14 дней** до вступления
изменений в силу, публикацией и письмом. Механизма рассылки в коде нет.
23.08.2026 уведомление не запускалось сознательно: правило про сгорание
остатка работало в коде с самого начала, ничьи условия не ухудшались —
менялась полнота информирования, а не сами условия; плюс пользователей на
проде ещё нет. **Для правки, которая реально меняет условия, это решение не
переиспользовать.**

---

## Смена тарифа сжигает остаток — правило описано в оферте, менять только вместе

`activate_subscription` (`payments/common.py`): срок суммируется **только**
при повторной оплате ТОГО ЖЕ тарифа по живой подписке; **любая смена тарифа**
— и повышение, и понижение — обнуляет остаток и отсчитывает 30 дней от даты
оплаты.

Важное, что легко упустить: сжигается остаток **не только при апгрейде**.
Понижение с Лиры на Вегу ведёт себя так же, и чекаут этого не запрещает
(`CHECKOUT_TIERS = ("lite", "pro")`, купить младший тариф поверх старшего
можно). Поэтому в оферте формулировка «при переходе на **другой** тариф», а
не «на более дорогой» — узкая формулировка не покрыла бы понижение и снова
оставила бы пользователя без информации.

Текст живёт в трёх местах: `oferta_final.md` п. 4.3, `TermsPage.jsx` п. 4.3
(продолжение пункта — нумерацию намеренно НЕ сдвигали, на пункты
опубликованного правового документа потом ссылаются в переписке) и
`PricingPage.jsx` (блок `s.legal`).

Связь кода с текстом держит `TestOfferDocumentsTheRule`
(`test_subscription_renewal.py`): проверяет наличие формулировки в обеих
половинах пары и на `/pricing`, а последним тестом — что код действительно
сжигает остаток. Упал последний — значит поведение изменили, и оферта стала
неправдой: править надо оба места, а не assert.

---

## Код ответа вебхука ЮKassa: постоянная причина → 200, временная → 500

Правило, к которому пришли аудитом 23.08.2026 (находки 2.1 и 2.9). ЮKassa
ретраит доставку суток, пока не получит 2xx. Значит код ответа — это не
«хорошо/плохо», а ответ на один вопрос: **изменит ли что-нибудь повтор?**

| Причина отказа | Постоянная? | Ответ |
|---|---|---|
| API ЮKassa недоступен (`_fetch_payment` → None) | нет | **500**, ретрай нужен |
| `PaymentProcessingError` (сбой обработки) | нет | **500** |
| Сумма не совпадает с ценой тарифа | да | **200** + запись + уведомление |
| Валюта не RUB | да | **200** + запись + уведомление |
| Непригодная metadata (нет `user_id`, тариф не продаётся) | да | **200** + запись + уведомление |
| Пользователя из metadata нет в БД | да | **200** + запись + уведомление |
| `status != succeeded` по данным API | да | **200**, записывать нечего — денег не было |
| IP не из подсетей ЮKassa | да | **403** — исключение, см. ниже |
| Нечитаемое тело, нет `object.id` | да | **400** — исключение, см. ниже |

**200 обязан оставлять след.** Иначе это молчаливое проглатывание платежа:
деньги пришли, ЮKassa ушла удовлетворённой, в базе пусто. Поэтому все ветки
с 200 по денежной причине идут через `_record_unusable_payment` — запись в
`payment_events` под тем же `inv_id` (уникальный индекс заодно не даст
обработать платёж повторно) плюс уведомление владельцу. Коммит **до**
уведомления, как в `_handle_refund`: параллельный ретрай уходит по
`IntegrityError` и второго сообщения не будет.

**Два намеренных исключения из правила:**
- **403 по IP** — постоянная причина, но не-2xx здесь работает на нас: если
  список подсетей устареет и стучится настоящая ЮKassa, сутки ретраев дают
  окно, за которое список можно поправить, и платёж активируется сам.
- **400 на нечитаемое тело** — записывать нечего, там нет даже идентификатора
  платежа, а отправитель почти наверняка не ЮKassa.

**Троттла у денежных уведомлений нет** (в отличие от `_notify_ip_reject`, где
гасится лавина одинаковых отказов): каждое такое событие — отдельные деньги
конкретного человека, пропустить нельзя ни одного.

**Сырую metadata в Telegram не кладём** (решение владельца 23.08.2026): уходит
`payment_id`, сумма и тариф, если распознан. Остальное владелец видит по
`payment_id` в кабинете ЮKassa, а состав полей metadata может измениться, и
передача их в служебный чат политикой конфиденциальности не описана.

---

## Платёж, которому некому выдать подписку — 200, а не 500

Если `metadata.user_id` пришедшего платежа нет в `users` (человек удалил
аккаунт после оплаты — `DELETE /api/v1/auth/me` доступен ему самому; либо
строка пропала при восстановлении из бэкапа), `activate_subscription`
поднимает `SubscriptionOwnerMissing`, `process_payment` **коммитит запись о
платеже** и пробрасывает исключение, а вебхук отвечает **200** и шлёт
владельцу `_notify_orphan_payment`.

Три решения, каждое из которых легко «исправить» обратно во вред:

1. **200, а не 500.** Причина постоянная — ретрай ЮKassa сутки подряд ничего
   не изменит, только зашумит. Временный сбой БД сюда не попадает вовсе: он
   поднимает исключение из самого запроса и уходит в `PaymentProcessingError`
   → 500 → ретрай, как и раньше. Разделение точное: постоянная причина → 200,
   временная → 500.
2. **Коммит в ветке `except`, и она обязана стоять ВЫШЕ общего `except`.**
   Общий делает `rollback` и потерял бы запись, ради которой всё затевалось.
   `db.commit()` физически живёт внутри `activate_subscription`, поэтому
   прежний ранний `return` не коммитил ничего — это и был корень дефекта.
   Если сам этот commit упадёт, исключение уходит наверх → 500 → ретрай, и
   это правильно.
3. **У `_notify_orphan_payment` нет троттла**, в отличие от
   `_notify_ip_reject`. Там гасится лавина одинаковых отказов, здесь каждое
   событие — отдельные деньги конкретного человека, пропустить нельзя ни
   одно. Текст намеренно требует ДВУХ действий: провести доход в «Мой налог»
   (обязательно в любом случае — деньги получены) и разобраться с подпиской
   руками. Просто переиспользовать `_notify_payment` было нельзя: он говорит
   «💰 Оплата» и умалчивает, что подписки нет.

⚠️ **Ограничение теста** (`test_missing_user_keeps_payment_record_and_does_not_retry`):
проверять наличие строки в БД тут бесполезно — `conftest` отдаёт роутеру ту же
сессию, что и фикстуре `db`, и не закрывает её, а в проде запись терялась
именно на `db.close()` в `get_db`. Поэтому тест считает число `commit()`:
ровно оно и было нулём. Не «упрощать» обратно на проверку строки.

---

## Промокоды не подключены — целиком, и в чекауте это намеренно

Фича существует кусками, ни один из которых не работает. Проверено по коду и
истории 23.08.2026. Не «чинить UI», не «дописать вызов» — сначала прочитать это.

| Звено | Состояние |
|---|---|
| Поле ввода промокода, `PaywallModal.jsx` (~175-192) | Есть, живое |
| `validatePromoCode` → `GET /api/v1/payments/promo-validate` | **Эндпоинта в бэкенде нет вовсе** → всегда 404 → поле ВСЕГДА показывает «Промокод не найден или истёк» |
| `createCheckoutSession` шлёт `promo_code` | Бэкенд принимает и игнорирует: `CheckoutRequest` объявлен с `extra: "ignore"` |
| `POST /admin/coupons/apply` | Вызывающих нет. С 23.08.2026 — `require_admin` |
| `record_promo_usage` | Вызывающих не было **никогда** (`git log --all -S`, функция родилась как `def` в `26ef160`) |

**Игнорирование `promo_code` в чекауте — решение, а не недоделка**
(`yookassa_router.py`, комментарий в `CheckoutRequest`): промокод — вторая
точка, где сумма платежа могла бы разойтись с тем, что реально списала
ЮKassa. Провайдер уведомления не подписывает, сумму мы и так перечитываем из
API и сверяем с `TIER_PRICES_RUB` — вводить в эту сверку ещё одну переменную
без отдельного решения нельзя.

Поэтому «поле промокода не работает» чинится не в UI: пока нет
`promo-validate` и решения по сумме, любой ремонт интерфейса даст
полурабочее состояние. Полный список того, что нужно сделать разом, — в
докстринге `record_promo_usage`.

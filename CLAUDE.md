# CLAUDE.md — Aristea Timeline

## Правила работы
1. Спрашивай вместо того чтобы угадывать
2. Пиши минимальный код
3. Делай только то о чём прошу
4. Фокусируйся на результате, а не инструкции
5. Работай аккуратно и внимательно
6. Код не пиши пока я не скажу, отвечай кратко

Открытые задачи — [TASKS.md](TASKS.md). Разборы и история — docs/HISTORY-*.md.

---

## Проект

**Aristea Timeline** — веб-приложение: натальные карты, транзиты, лунный календарь, AI-интерпретации. Домен: aristeatime.ru.
**Версия архитектуры:** 4.0 | Август 2026

---

## Стек

| Слой | Технология |
|---|---|
| Frontend | React 18.3, React Router 6, Vite 5, Tailwind 3.4, D3.js |
| Backend | Python 3.12, FastAPI, Uvicorn |
| БД / ORM | PostgreSQL 18, SQLAlchemy 2.0, Alembic |
| Кэш / очереди | Redis 7, Celery |
| Астрология | pyswisseph (Swiss Ephemeris) |
| AI | интерпретации — DeepSeek Pro (→ GPT-4o → шаблон); прогнозы в приложении — DeepSeek Pro; общий астрокалендарь — Anthropic Claude Sonnet. Подробно — раздел «AI-контуры» |
| Аутентификация | JWT, Google OAuth 2.0, bcrypt |
| Платежи | ЮKassa (в разработке) — Robokassa и Stripe удалены 19.08.2026 как мёртвый код |
| Email | Resend API |
| Геокодинг | Nominatim |
| Хостинг | Timeweb VPS: Nginx + Docker Compose (api, bot, postgres, redis, uptime-kuma) |
| CI/CD | GitHub Actions → SSH-деплой на VPS (ручной запуск джоба); фронтенд пересобирается тем же `05-update.sh`, отдельного запуска не требует |
| PDF | ReportLab |

⚠️ **Визуальный язык у веба и приложения РАЗНЫЙ с 17.09.2026** (решение
владельца): веб — прежняя палитра с градиентом, приложение — «Глубокий
аметист». Значит `frontend/src/index.css` и `frontend/src/mobile/mobile.css`
больше НЕ обязаны совпадать по значениям, и правка одного не должна
автоматически повторяться во втором. Таблица различий и то, что осталось
общим (цвета планет), — `DESIGN_SYSTEM.md` §1.

**UI-инструменты (активные):**
- 21st.dev — готовые React-компоненты
- Framer Motion — анимации
- Ponytail — Tailwind-компоненты (CLI)

---

## Структура frontend

```
frontend/src/
├── App.jsx
├── main.jsx
├── index.css
├── pages/
│   ├── LandingPage.jsx
│   ├── HomePage.jsx
│   ├── ChartPage.jsx
│   ├── ProfilePage.jsx
│   ├── CRMPage.jsx
│   ├── AdminPage.jsx
│   ├── PlannerPage.jsx
│   ├── LunarCalendarPage.jsx
│   ├── SolarReturnPage.jsx    # in progress
│   ├── SynastryPage.jsx       # in progress
│   ├── RelocationPage.jsx     # in progress
│   ├── PortalPage.jsx         # клиентский портал
│   ├── IntakePage.jsx         # анкета клиента
│   ├── OrionPage.jsx          # тарифы
│   ├── ZodiacPage.jsx
│   ├── SharePage.jsx
│   └── GiftPage.jsx
├── components/
│   ├── NatalChart.jsx      # SVG колесо натальной карты (D3)
│   ├── TransitTimeline.jsx # временная шкала транзитов
│   ├── AuthModal.jsx
│   ├── RagChat.jsx         # чат Аристеи (RAG)
│   ├── Toast.jsx
│   └── ThemeToggle.jsx
└── hooks/
    └── useAuth.jsx
```

---

## Структура backend

```
backend/
├── main.py
├── models.py
├── schemas.py
├── config.py
├── database.py
├── cache.py
├── celery_app.py
├── tasks.py
├── limiter.py
├── authz.py
├── metrics.py
├── email_service.py
├── natal_pdf.py
├── health.py
├── auth/
├── calendar/
├── crm/
├── ephemeris/
├── interpretation/
├── transit/
├── payments/          # common.py (провайдер-независимая логика: идемпотентность,
│                      # комиссии, активация; TIER_PRICES_RUB — единственный
│                      # источник цен на бэкенде) + payments_router.py (подписка,
│                      # админский set-tier) + yookassa_router.py (checkout, вебхук)
├── admin/
├── push/              # web push (pywebpush + VAPID)
├── notifications/     # telegram
├── pilot/             # пилотная Telegram-программа
├── feedback/
├── exit_survey/
└── profile/
```

---

## Тарифы

Таблица утверждена владельцем 19.08.2026, накопительная (каждый следующий
тариф — «всё из предыдущего, плюс…»; строки ниже — уже развёрнутый полный
вид, не сокращённый).

| | Бесплатный | Вега (Lite) | Лира (Pro) | Орион (Premium) |
|---|---|---|---|---|
| Цена/мес | 0 ₽ | 790 ₽ | 2490 ₽ | 7990 ₽ |
| Карты (= профили на аккаунт) | 2 | 5 | 15 | безлимит |
| Интерпретации/мес | 1 навсегда, дальше шаблон | 5 (по числу карт) | 15 (по числу карт) | безлимит |
| Планер: месяц | — | рекомендации на месяц | все планеты + долгосрочные (год) | всё из Лиры |
| Планер: неделя (Луна по домам) | текущая неделя | всё окно ленты | всё окно ленты | всё окно ленты |
| Транзиты | — | горизонт 6 мес, разбор 3/мес | горизонт 12 мес, разбор без лимита | горизонт 24 мес, разбор без лимита |
| Лунный календарь | текущий месяц | год | год | безлимит |
| Google Calendar | — | 1 карта | 1 карта (унаследовано) | 1 карта (унаследовано) |
| Чат с Аристеей | — | — | ✓ | ✓ |
| PDF-экспорт | — | 5 карт | 15 карт | безлимит |
| Кабинет астролога (CRM) | — | — | — | ✓ |
| Прогноз на сегодня и на фазы Луны (приложение) | ✓ | ✓ | ✓ | ✓ |

Модель интерпретации — везде DeepSeek Pro (не зависит от тарифа, решение
владельца 19.08.2026); тариф регулирует глубину объёмом текста, а не моделью.
Чат Аристеи — отдельно, на Flash.

История: Почему формулировка «глубину регулирует `interpretation_word_limit`» была неверна — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

Источник истины — `TIER_FLAGS` (`backend/auth/rate_limits.py`) и `TIERS`
(`frontend/src/constants.js`); оба обязаны совпадать.

⚠️ **Слова «AI» и «ИИ» в пользовательских текстах не употребляются** (решение
владельца 17.09.2026). Разбор называется разбором, интерпретация —
интерпретацией. Единый словарь замен — в TASKS.md, «Слово „AI“ убрано».
Исключение ровно одно: юридические документы (политика и текст согласия),
где слово называет получателя данных при трансграничной передаче, — там оно
остаётся намеренно.

⚠️ **Продукт обращается к человеку на «ты»** (решение владельца 24.09.2026):
веб, приложение, уведомления, письма, ошибки API и тексты моделей. Держит
`backend/tests/test_informal_address.py` — сканирует литералы Python, строки
JSON и JS/JSX без комментариев на «вы/ваш», «-йте» и глаголы второго лица
(«можете»). На «вы» намеренно: оферта, политика, текст согласия; всё, что
видят КЛИЕНТЫ астролога (портал, анкета, письма под брендом астролога,
страница отписки) — это голос астролога; сообщения владельцу. Файл целиком —
`ALLOWED_FILES` с причиной, строка — маркер `вы-разрешено` в комментарии.
Промпты берут одну строку `ADDRESS_RULE` (`interpretation/address.py`), тест
требует её в каждом промпте, текст которого читает человек.

⚠️ **«Ты» не должно угадывать пол.** Пола человека мы не знаем: «ты рано
узнал», «ты уверен», «будь осторожен» переписываются без рода («береги себя»,
«тебя не было 5 дней»). Та же оговорка — в `ADDRESS_RULE` и в правилах
прогнозов. Проверки на это нет, только вычитка.

⚠️ **Строка «Планер: неделя» описывает только БУДУЩЕЕ.** Завершившиеся проходы
Луны по домам открыты на всех тарифах, включая бесплатный, и числом в таблице
не регулируются (решение владельца 16.09.2026, разбор — «Завершившийся проход
открыт ВСЕМ тарифам» ниже). Витрина обязана называть обе половины: текст,
называющий у free одну текущую неделю, недодаёт человеку то, что у него уже
есть, — и делает это незаметно. «Карты» в таблице —
это `profiles_limit`: слотовая модель (вариант А, решение владельца
19.08.2026), сколько карт одновременно может быть сохранено, не месячная
квота — удаление карты освобождает слот, строить и смотреть карты можно
без ограничений. Enforced в `POST /chart/calculate` (`main.py`), проверяется
раньше остальных лимитов этого эндпоинта.

История: История поля `charts_per_month` и почему вместо него `CHART_CREATION_ABUSE_LIMIT = 100` — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

Лимиты применяются через `TierRateLimiter` (`auth/rate_limits.py`), расход
считается в `UsageCounter`. Монотонность сетки (платный тариф не хуже
дешёвого ни по одному полю) проверяется тестом `TestTierMonotonicity` в
`test_rate_limits.py`.

История: Письма о тарифах (числа из `TIER_FLAGS`, `_plural()`, исключение про «год» в письме Веги) — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

---

История: Долг бэкенда за мобильный заход (горизонт транзитов free, `FREE_TRANSITS_TEASER_MONTHS`, снятый обход в `ProfilePage.jsx`) — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

## Ключевые API эндпоинты

```
POST /api/v1/chart/calculate
GET  /api/v1/chart/{id}/interpret       # SSE
GET  /api/v1/chart/{id}/transits/interpret  # SSE
GET  /api/v1/chart/{id}/forecast/day|lunation   # приложение, DeepSeek, без квоты
POST /api/v1/auth/register
POST /api/v1/auth/login
GET  /api/v1/auth/google
GET  /api/v1/payments/subscription       # текущая подписка
POST /api/v1/clients                    # CRM (Premium)
GET  /api/v1/push/upcoming              # будущие уведомления для планирования на устройстве
```

### Публичные ручки обязаны принимать HEAD — FastAPI не добавляет его сам

**Starlette добавляет HEAD к любому GET-маршруту, FastAPI — нет.**
`starlette/routing.py:229-234` делает `if "GET" in self.methods:
self.methods.add("HEAD")`; `fastapi/routing.py:892` — та же строка **без
второй половины**. Свойство фреймворка, не наша ошибка в коде, и оно
неочевидно: 01.09.2026 все 63 GET-ручки проекта отвечали на HEAD **405**
с `allow: GET` и `content-type: application/json` (405 формирует Starlette,
JSON-тело — обработчик исключений FastAPI; nginx ни при чём, воспроизводится
локально через TestClient).

⚠️ **Что сломается иначе: превью в мессенджере не строится при полностью
исправных OG-тегах и рабочей картинке.** Краулеры превью проверяют ресурс
HEAD-запросом до GET; получив 405, до GET они не доходят. Отладка при этом
уходит в теги, кэш Telegram и отправителя — всё это выглядит правдоподобно и
всё мимо. Потратили на это несколько заходов 01.09.2026, пока не сделали
`HEAD` вручную и не увидели 405.

**Правило: любая новая публичная ручка, ссылку на которую можно прислать в
мессенджер, объявляется через `@router.api_route(..., methods=["GET",
"HEAD"])`.** Не `@router.get` (он `methods` не принимает) и не отдельный
`@router.head` — вторая функция пришлось бы держать в синхроне с первой по
заголовкам, а это ровно тот класс дефекта, который в этом проекте уже
расходился не раз.

Сегодня так объявлены три: `/share/{token}`, `/share/{token}/card.png`
(`share_router.py`) и `/portal-report/{token}` (`crm/portal_router.py`).
`/health` намеренно не трогали — его опрашивают GET'ом.
`robots.txt`/`sitemap.xml`/`og-preview.png` — статика, HEAD для них
обслуживает nginx.

История: Почему у `share_card_png` и `portal_report` ранний выход по HEAD, а у страницы нет — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

### `POST /chart/calculate` координаты из тела не принимает

`BirthDataInput` (`schemas.py`) объявляет только `name`, `birth_date`,
`birth_time`, `birth_place`, `house_system`. Полей `latitude`/`longitude` в
ней нет, `model_config` не задан, поэтому `extra` — pydantic-овский по
умолчанию `ignore`: присланные координаты **молча отбрасываются**, ошибки не
будет. Проверено исполнением, а не чтением:
`BirthDataInput(..., latitude=999.0)` конструируется, и `latitude` не
попадает в `model_dump()`.

Источник координат ровно один — геокодинг по `birth_place`
(`main.py`, `geo = await geocode_place(data.birth_place)`). Стоящий сразу за
ним `validate_coordinates(geo.latitude, geo.longitude)` проверяет **ответ
Nominatim**, а не ввод клиента, и при выходе за диапазон отдаёт 400. То есть
**кода 422 за координаты у этой ручки нет и никогда не было**, а 400
достижим только при неисправном геокодере. Сама функция вызывается ровно в
одном месте и покрыта юнит-тестами (`TestCoordinateValidation` в
`test_validation.py`), интеграционного дубля ей не нужно.

⚠️ **Что сломается, если про это не знать: тест, кладущий координаты в
payload, будет зелёным, ничего не проверяя.** Так и произошло с
`test_invalid_coordinates_returns_422` — он слал `latitude: 999.0`, получал
200 (поле проигнорировано, карта построена по геокодингу «Moscow») и
проходил по `assert resp.status_code in (200, 422, 400)`. Он держал место
проверки валидации, не будучи ею, и заодно был единственным тестом,
незаметно ходившим в живой Nominatim: фикстуры `mock_geo` он не запрашивал.
Удалён 01.09.2026 вместе с координатами из всех payload'ов
`test_integration.py` — чтобы следующий читающий не решил, что ручка их
принимает. Тогда же удалён класс `CoordinatesInput` из `schemas.py`: он был
написан в `c086efa` (27.05.2026) и не вызывался ни разу за всю историю.

### `api/client.js` — не единственный путь к бэкенду

Часть компонентов ходит на бэкенд прямым `fetch`/`authFetch`, минуя обёртки
`client.js`: `TransitTimeline.jsx` (сама собирает URL транзитов и разбора
события), `LunarCalendarPage.jsx` и `PlannerPage.jsx` (обе сами дергают
`/calendar/lunar`, каждая своим `fetch`). Обнаружено 30.08.2026 при уборке
мёртвого кода: удаление неиспользуемых функций `getTransits`,
`streamTransitEventInterpretation`, `getLunarCalendar` из `client.js` не
затронуло сами эндпоинты — их продолжают звать теми же путями напрямую.

**Следствие: мёртвая обёртка в `client.js` не означает мёртвый эндпоинт.**
Перед тем как удалять бэкенд-ручку по признаку «не вызывается из
client.js», grep обязателен по всему `frontend/src` на сам URL-путь
(`fetch(`.../transits`` и т.п.), а не только по имени функции-обёртки.

### Ключ квоты `"transit_ai"` — только разбор транзитного события

`tier_limiter.check_transit_ai_limit` / `commit_transit_ai`
(`auth/rate_limits.py`) стоят на одной ручке — `/transits/event/interpret`.
Квота `transits_ai_per_month` (3 в месяц у Веги) тратится только на неё, и
витрина «разбор транзитов, 3 в месяц» (`constants.js`, `/pricing`, `/orion`)
это и описывает.

До 23.09.2026 тот же счётчик списывали ещё `/forecast/daily|weekly|monthly`
(без потребителя с `f82b49c`). Они удалены вместе с промптами, а прогнозы в
приложении (`backend/forecast/`, контур 4 ниже) квоту **не трогают
намеренно** — это для всех тарифов, и списывай они `transit_ai`, free не
получил бы ничего, а Вега сожгла бы три разбора за три дня. Держит тест
`test_forecast.py` (фикстура `no_transit_ai` роняет любое обращение к квоте).
⚠️ Новой ручке, которой нужна квота, — свой ключ, а не этот: общий ключ и
есть то, из-за чего витрина однажды перестала бы быть правдой.

История: Лента `GET /chart/{id}/feed`: `planner_longterm` вне окна ответа, устойчивость ключей, методология сверки окон — [docs/HISTORY-feed.md](docs/HISTORY-feed.md).

### Отложено: лента не отдаёт границ транзита, окно в тизере — свойство планеты

Найдено 09.09.2026 при сборке тизера транзита (`mobile/lib/transitTeaser.js`).

**Лента не сообщает, когда транзит начался и когда закончится.** У события
`kind == "transit"` поля `ends_at` и `duration_days` равны `null`, а
`start_date`/`end_date` сервер не присылает вовсе — они обрезаны окном запроса,
и показывать их как «начало» и «конец» нельзя (тот же мотив, что у
`started_before` в разделе про ленту выше).

Поэтому строка «окно около недели» в тизере — **не расчёт по датам события, а
свойство транзитной планеты**, её скорость: такая же статика, как название и
тема. Слово «около» в тексте не украшение, оно и означает порядок величины, а
не срок конкретного события.

⚠️ **Что сломается, если про это не знать:** попытка «уточнить» окно по данным
события упрётся в `null` и, скорее всего, будет заменена арифметикой от `at` —
то есть выдуманным сроком, который выглядит точным. Настоящее окно должна
прислать лента, и это работа на бэкенде: границы у транзита в движке есть
(`transit/engine.py`), в ответ они не попадают.

Пока этого нет — не выводить в интерфейсе ни одной ДАТЫ границы транзита,
только порядок величины.

---

### Неделя в ленте: горизонт БЫЛ в ответе и фактически отсутствовал

Найдено 16.09.2026 разведкой по заданию «три вкладки планера в ленте».
⚠️ **Записано ради приёма, а не ради самого дефекта: kind в выдаче был,
события приходили, тесты были зелёными — и горизонта не существовало.**

`_planner_events` (`feed/builder.py`) звал `compute_planner_periods` с окном
ленты в 148 дней и брал оттуда `moon_week`. Но `moon_week` считается **не по
окну**: это неделя ВЕБ-ПЛАНЕРА, отсчитанная от первого понедельника
отображаемого месяца и листаемая параметром `week_offset`
(`house_passages.py`). На любом окне лента получала одну неделю — **4 события
на пять месяцев** (`SPEC_FEED_SCREEN.md` §2, боевой замер 04.09.2026), при том
что проход Луны по дому длится ~2.3 суток и на том же окне их ~64.

Четыре события выглядели правдоподобно и потому никого не насторожили: они
попадали под свёртку лунного фона (`FeedLunarFold.jsx`), где ни времени, ни
срока не видно вовсе. **Проверка «kind присутствует в ответе» была зелёной и
не значила ничего.** Отсюда правило для любой будущей сверки горизонтов ленты:
считать ПЛОТНОСТЬ (событий на сутки окна), а не факт наличия вида — тест
`test_window_is_covered_not_one_week` сформулирован именно так и специально не
через «больше четырёх», потому что «больше четырёх» прошло бы и на пяти.

Лечение: отдельная функция `compute_moon_house_passages` для окна ленты;
ветка `week_offset` не тронута (на ней листание недель в вебе). Расчёт у обеих
общий — `_moon_passages_between`.

⚠️ **Окно проходов Луны СВОЁ и не выводится из `feed_horizon()`**: от начала
окна ленты до трёх месяцев вперёд от понедельника текущей недели, одинаково на
всех тарифах (`MOON_WINDOW_MONTHS_AHEAD`, решение владельца 16.09.2026).
Тарифом регулируется РАСШИФРОВКА, а не наличие события. Верхняя граница — плата
за вес: на горизонте Ориона (24 месяца) одних только проходов Луны было бы
~320.

### Граница недели проходит ПО ПРОХОДАМ, а не по полуночи

Проход Луны по дому длится ~2.3 суток и регулярно пересекает границу недель:
начался в субботу — кончился во вторник. Отсюда два следствия, каждое из
которых при чтении кода выглядит недоделкой.

**1. Проход, пересекающий окно, отдаётся ЦЕЛИКОМ, с настоящими временами.**
Обрезка по краю окна дала бы границу «дата 00:00» — момент, которого не было,
и выглядящий при этом совершенно нормально. В вебе это вылечено ещё 03.08.2026
(`00b00e7`): сканирование шире окна на `MOON_SCAN_PAD_DAYS` плюс
`refine_edges=True` у `calculate_house_passages`. Лента переиспользует то же
решение, своего не заводит. Закреплено `test_no_midnight_boundaries_from_clipping`.

⚠️ **Побочное следствие, которое иначе примут за сбой сортировки:** проход,
идущий на момент начала окна, приходит с `at` РАНЬШЕ `from_date` ответа, то
есть с `started_before: true`. Таких событий ровно одно на ленту, и оно даёт
один лишний день в самом начале списка. До 16.09.2026 в потоке не было ни
одного события с этим флагом (комментарий в шапке `FeedScreen.jsx` это
утверждал) — теперь есть.

**2. Тарифный гейт считает по НАЧАЛУ прохода, а не по тому, «в какую неделю он
попадает».** Считай мы по календарным суткам, один и тот же проход был бы
одновременно открыт и закрыт. Правило целиком — в докстринге
`is_moon_week_locked` (`transit/planner_engine.py`).

### Завершившийся проход открыт ВСЕМ тарифам — это витрина, а не щедрость

Решение владельца 16.09.2026. Человек видит объём того, что прошло мимо, на
своих настоящих данных, а не на примере. Та же логика, по которой горизонт
ленты назад одинаков у всех (`feed/horizon.py`).

Вперёд — по тарифу: `planner_weeks_ahead` недель СЧИТАЯ текущую. У free это
1, у платных `None` — «всё окно», по общему правилу безлимитов в `TIER_FLAGS`.
Зеркало на фронте — `PLANNER_WEEKS_AHEAD` (`constants.js`, у платных `null`),
синхронность держит `api/plannerWeeks.test.js` по образцу
`transitsHorizon.test.js`.

⚠️ **У платных стояла четвёрка, и это отменено 16.09.2026.** Число было
одинаковым у Веги, Лиры и Ориона — то есть закрытую неделю нельзя было открыть
НИЧЕМ: ни временем (она не приближалась быстрее), ни деньгами (следующий тариф
давал столько же). Состояние без выхода человеку сообщать нечем, и текст под
него («Рекомендации появятся ближе к сроку») удалён вместе с ним. Возвращать
конечное число у платных — значит возвращать и эту развилку.

⚠️ **`locked` у события планера не означает «купи тариф» сам по себе** — это
зависит от ПАРЫ (тариф, вид события). Проход Луны и месячный период закрыты
только на free, долгосрочный — на free и Веге. Поэтому:

* плашка апселла в веб-планере (`PlannerPage.jsx`) показывается только на free;
* в приложении решение принимает пара (тариф, вид события), а не флаг —
  `mobile/lib/plannerAccess.js`, перебор всех сочетаний в его тесте. Там же
  лежит вторая копия трёх серверных правил (`planner_moon_house` и
  `planner_period` — гейт только на free, `planner_longterm` — на free и Веге);
  разъедется — человек увидит кнопку, которая ему ничего не даст.

Предлагать Веге перейти на Вегу значило бы продавать то, что у человека уже
есть, — тот же класс ошибки, что приписка, зовущая через тариф.

⚠️ **Клиентский запрет листать недели на free снят и возвращать его нельзя.**
`changeWeek` открывал пейволл при `isFree` — а по новому правилу прошлое
открыто всем, и без листания free до него не добраться: сервер отдавал бы
то, чего с экрана не достать. Тариф решает, что ВНУТРИ недели закрыто, и
решает это сервер.

### Приёмка на боевом API проверяет пару «новый APK + СТАРЫЙ сервер»

Записано после 16.09.2026, когда два дефекта приёмки оказались одним и тем же.
Время прохода Луны расходилось с вебом ровно на пояс, а прошедший проход
приходил без расшифровки — оба уже были исправлены в коде, но ветка не
деплоилась, и приложение говорило с прод-бэкендом прошлой недели.

⚠️ **Это не видно ниоткуда.** APK свежий, фронтенд новый, ошибок нет; сервер
отвечает исправно — просто старым кодом. Разбор при этом уходит в тот код,
который у тебя перед глазами и который уже правильный.

**Правило: перед разбором любого расхождения, замеченного НА УСТРОЙСТВЕ,
сначала проверить, доехал ли бэкенд.** Дешевле всего — по публичному следу
свежей правки (строка на `/pricing`, поле в открытой ручке). И отдельно:
мобильная приёмка на боевом API имеет смысл только после деплоя ветки, иначе
проверяется пара, которой не будет существовать.

⚠️ Истину о расчёте при этом всё равно нельзя брать ни с одного из двух
экранов — только прямым счётом мимо обоих (разбор — «Проход Луны по дому
отдавался со сдвигом времени на пояс» ниже).

### Полоса планет: три горизонта одним рядом, одна семантика — дом

Решение владельца 16.09.2026. Ряд значков в ленте один на оба вида полосы
(развёрнутая `FeedNowStrip`, липкая `FeedNowCompact`) и собран одним
компонентом `FeedPlanetStrip.jsx`. Порядок — `PLANNER_PLANET_ORDER`
(`mobile/lib/feedGlyphs.js`), отбор — `plannerTimeline` (`mobile/lib/feedNow.js`).

⚠️ **До этого рядов было ДВА, и в одном из них числа означали разное:** в
липкой полосе «☉ 12» — это были ДНИ остатка периода, а «♃ 11» — ДОМ. Два
смысла в одном ряду, различимые только знанием, что у Юпитера не бывает
двенадцати дней. Правили один ряд — второй оставался; ровно поэтому порядок и
выборка вынесены из компонентов.

⚠️ **Текущая запись выбирается по МОМЕНТУ, а не по календарной дате.** Луна
меняет дом раз в ~2.3 суток, то есть почти всегда посреди дня: 16.09 до 03:00
она в 12 доме, с 07:00 — в 3-м. По датам оба прохода «покрывают сегодня», и в
полосу попадал первый — вчерашний дом с сегодняшней датой. Сравнение идёт
через `Date.parse` (у событий ISO со смещением карты, у устройства своё —
лексикографически такие строки сравнивать нельзя).

⚠️ **Закрытый тарифом горизонт значка не лишается.** Человек должен видеть,
что период идёт; закрыт только разбор, и об этом говорит панель
(`plannerAccess.js`). Спрятав значок, мы спрятали бы сам факт, а не платное.

### Липких полос в ленте по-прежнему ОДНА

⚠️ **Полоса планет «всегда видна» не значит «всегда прилеплена».** Она стоит в
двух местах: в развёрнутом блоке в начале списка и в липкой полоске, которая
появляется, когда развёрнутый блок уходит за верх. Вместе они и дают «видна
всегда», не заводя второго липкого слоя и не отъедая 44 px у каждого экрана
прокрутки. Правило «выбрать одно, не оба» (§5 DESIGN_SYSTEM.md) в силе.

### Полоска дней тянула ленту в начало окна — `scrollIntoView` двигает ВСЕ предки

Найдено приёмкой 16.09.2026: полоска дней подсвечивала сегодняшнее число, а
лента стояла на месяце назад.

`FeedDayStrip` центрировал сегодняшнюю колонку через
`scrollIntoView({inline: 'center', block: 'nearest'})`. `inline` делал своё
дело, а `block: 'nearest'` поднимал **вертикальный** скроллер ленты (он один на
все вкладки, живёт в `TabShell.jsx`) так, чтобы полоска попала в кадр. Полоска
стоит в самом верху — значит страница уезжала в начало окна, поверх прокрутки
к сегодняшнему дню, которую `FeedScreen` только что сделал.

⚠️ **Это гонка, и потому она то воспроизводилась, то нет.** Эффекты детей
выполняются раньше родительских, и подъём успевал встать между первым прыжком
к якорю и повторными (`requestAnimationFrame`, `document.fonts.ready`).

Лечится своим `scrollLeft` — он трогает ровно ту полоску, которая имелась в
виду. Закреплено `feedAnchor.test.js`: проверка читает исходник и требует
`scrollLeft` без `scrollIntoView` в этом эффекте. Само правило выбора дня
вынесено в `mobile/lib/feedAnchor.js` — в рендере его нельзя было проверить
отдельно от прокрутки, и разбор начинался не с того конца.

### Проход Луны по дому отдавался со сдвигом времени на пояс — дважды

⚠️ **Движок домов возвращает ДВА разных вида наивных дат, и по значению их не
отличить.** Периоды планет (`fast_planets`, `slow_planets`) — наивный UTC.
Проходы Луны — уже МЕСТНЫЕ: `house_passages.py` прибавляет смещение внутри,
чтобы метки для человека («21.05 Чт 03:22») совпадали с границами.

`add()` в `feed/builder.py` считала наивной датой UTC всё подряд, поэтому
местное значение объявлялось UTC ещё раз: в Москве проход, начавшийся в 15:56,
приезжал в ленту как 18:56. Дефект дожил с первого коммита ленты, потому что
этих событий было четыре и все они лежали под свёрткой, где время не
показывается вовсе. Лечится флагом `local=True` у `add()` —
признак передаётся ЯВНО именно потому, что из самого значения он не выводится.

**Какое поле какого вида — таблица, а не догадка:**

| Источник | `start_dt` / `end_dt` | Почему так |
|---|---|---|
| `fast_planets[].periods[]` (Солнце…Марс) | наивный **UTC** | `calculate_house_passages` считает в UTC и отдаёт как есть |
| `slow_planets[]` (Юпитер…Плутон) | наивный **UTC** | то же |
| `moon_week[]` / `compute_moon_house_passages` | наивное **МЕСТНОЕ** | `_moon_passages_between` прибавляет смещение внутри, чтобы метка для человека («21.05 Чт 03:22») совпадала с границей |

⚠️ **Различить их по значению НЕЛЬЗЯ** — это одинаковые `datetime` без
`tzinfo`, и ошибка на три часа выглядит как правдоподобное время. Поэтому
признак и передаётся параметром, а не угадывается.

**Как проверить, не сломалось ли снова, не имея под рукой ни прода, ни
устройства.** Не сравнивать два экрана между собой: 16.09.2026 приложение
показывало «22:33», веб «19:33», и по ним нельзя было сказать, который врёт.
Истина берётся мимо обоих — прямым расчётом: долгота Луны (`swe.calc_ut`)
против куспидов карты, бисекция до секунды, результат в UTC и перевод в пояс
карты. На стенде `test_feed` вход в 1 дом — `2026-09-12 13:03:35` по
Europe/Moscow, и ровно это обязан отдавать `compute_moon_house_passages`.
Взаимную сверку двух экранов держит тест
`test_feed_and_planner_agree_to_the_minute` (обратный прогон на коде с
дефектом: падает).

---

## Мобильное приложение — две вещи, которые ломаются молча и НЕ видны на десктопе

Обе найдены 05.09.2026 при сборке экрана «Лента» (`frontend/src/mobile/`).
Общее у них то, что на машине разработчика всё выглядит правильно, а
ломается на телефоне — то есть обычная проверка «посмотрел, работает» их
не ловит.

### Палитра — источник истины не здесь

Значения токенов — `frontend/src/mobile/mobile.css` (`:root`/`.dark`). Какая
тема по умолчанию и почему — `DESIGN_SYSTEM.md` §1 и запись v1.3 в его
changelog (умолчание в мобильном приложении обратное вебу: светлая
основная). Третьего описания здесь нарочно нет — за цветом в прототип
(`aristea-mobile.html`) тоже не ходить, он не источник истины.

### Астрологические значки требуют своего шрифта, системного не хватает

Значки планет и точек (♃ ♄ ♅ ♆ ♇ ☉ ☽ ☿ ♀ ♂ ☊ ☋, блок Unicode Misc
Symbols) нельзя рисовать системным шрифтом. На Android и iOS покрытие
этого блока либо ОТСУТСТВУЕТ — и вместо значка встаёт пустой прямоугольник
(тофу), — либо подменяется цветным emoji-шрифтом, который рисует картинку и
**игнорирует `color`**: значок перестаёт слушаться темы и на тёмном фоне
остаётся тёмным.

**На десктопе этого не видно вообще.** В Windows и macOS системные шрифты
эти кодпоинты покрывают, поэтому и в браузере разработчика, и на
скриншотах всё правильно; расходится только на устройстве.

Лечение уже лежит в репозитории и его не нужно изобретать заново: сабсеты
`src/assets/fonts/NotoSansSymbols-subset.woff2` (всё, кроме Солнца) и
`NotoSansSymbols2-subset.woff2` (только ☉ U+2609 — в первом файле его нет,
это свойство шрифтов Noto). Веб подключает их внутри `NatalChart.jsx`,
мобильное приложение — через `@font-face` семейства `AstroSymbols` в
`mobile/mobile.css`; потребители берут его из `mobile/lib/feedGlyphs.js`.
Файлы общие, третьей копии заводить не нужно.

История: Прокрутка к якорю сбивается подменой шрифта (лента открывается не на сегодняшнем дне) — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

История: Холодный старт и задерживающий лимит nginx на `/api/v1/auth/` (гейт по сроку токена, `REFRESH_COOLDOWN_MS`) — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

История: Обновление сессии: одноразовая цепочка таймера, `delay <= 0`, разбор по размеру ответа в логах nginx — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

### Объект плагина Capacitor нельзя возвращать из async-функции и нельзя await-ить

**`Preferences` — не объект, а `Proxy`** (`registerPlugin`,
`@capacitor/core/dist/index.js:157-172`). Его `get`-ловушка отдаёт вызов
моста для ЛЮБОГО имени свойства; исключений ровно четыре: `$$typeof`,
`toJSON`, `addListener`, `removeListener`. **Имени `then` среди них нет** —
значит по правилам языка этот Proxy является thenable.

Дальше работает механика промисов, а не Capacitor: `return Preferences` из
`async`-функции (равно как и `await Preferences`) пропускает значение через
разворачивание thenable, то есть **вызывает `Preferences.then(resolve,
reject)`**. Мост принимает `then` за имя метода плагина и отвечает
`"Preferences.then()" is not implemented on android`, а `resolve` и `reject`
считает аргументами вызова и **не вызывает никогда**.

⚠️ **Поэтому это не ошибка, а зависание, и оно проходит мимо всех
предохранителей сразу.** Промис не разрешается и не отклоняется:
- `catch` вокруг `await` не срабатывает — отклонения нет;
- `finally` не срабатывает — промис не завершается, а `finally` выполняется
  только на завершении. Значит и `refreshInFlight` (`api/client.js`) остаётся
  заполненным НАВСЕГДА: каждый следующий `refreshSession()` получает по
  ранней ветке тот же неразрешающийся промис, и все четыре пути обновления
  (таймер, возврат из фона, монтирование, 401) молчат разом, по одной
  причине;
- отказ уходит в промис, который никто не слушает, — единственный внешний
  признак это `Uncaught (in promise)` в консоли `chrome://inspect`.

**Правило: объект плагина Capacitor никогда не возвращать и не `await`-ить
напрямую.** Только обёртку (`return { plugin: mod.Preferences }`) либо
результат конкретного метода (`await Preferences.get({...})` — здесь под
`await` уже промис метода, а не Proxy). Это касается любого плагина, не
только Preferences: свойство общее для всего `registerPlugin`.

История: Разбор этого дефекта целиком (цена, ошибочно приписанное следствие, порядок веток `register`/`session-diag`, методологический вывод про «подтверждено измерениями») — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

Инвариант закреплён тестом `frontend/src/api/authTransport.test.js`:
свойство `then` у объекта плагина не читается никогда. Мост подделан тем же
`Proxy`, что и настоящий, зависание ловится гонкой с таймером и читается как
`ПОВИСЛО`, а не как таймаут прогона; DOM-окружение не нужно — дефект чисто в
механике промисов. Проверено обратным прогоном на коде ДО правки: падают все
четыре кейса. Существующий `session.test.js` поймать это не мог по
построению — он подменяет весь модуль транспорта заглушкой, у которой нет
`then`.

История: Тариф не доезжал до приложения: `onTokensRefreshed` без подписчиков, `lib/sessionUser.js` — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

### Приписка зовёт в соседний тариф, а не через один

Решение владельца 09.09.2026: free → Вега, lite → Лира. До этого free звали
сразу в Лиру — человеку с 0 ₽ предлагали 2490 ₽ мимо тарифа за 790 ₽, то есть
самый дорогой шаг из возможных и единственный названный.

Тексты и правило «кому показывать» — общий файл `lib/interpretationUpsell.js`
(веб и приложение), числа объёма — `TIER_WORDS`, связанные тестом с
`TIER_FLAGS[tier]["interpretation_word_limit"]`. Литералов в тексте нет
намеренно: правка флага на бэкенде обязана менять текст, а не расходиться с
ним молча — так уже расходились письма (5 PDF при `pdf_per_month = 15`).

⚠️ Отдельно закреплено тестом, что названное число принадлежит тому тарифу, в
который зовут. Иначе free увидит объём Лиры под именем Веги — обещание чужого
тарифа, заметное только по жалобе.

### Склонение русских слов под подстановку — в двух рантаймах, местом стало три

`_plural()` (`backend/email_service.py`) — числа в письмах: «1 месяц»,
«3 месяца», «12 месяцев». Бэкенд, Python — остаётся отдельно, как и было:
другой рантайм, другая категория слов (числительные), сводить не с чем.

На фронтенде мест стало два, и с 06.09.2026 у них общий файл —
`frontend/src/mobile/lib/ruDeclension.js`:
- `tierInRu()` (была `inTier()` в `FeedHorizonCard.jsx`) — названия тарифов
  в предложном падеже: «на Веге», «на Лире», «на Орионе» (подстановка без
  склонения давала «на Вегае»);
- `signInRu()` (была в `feedTime.js`) — знаки зодиака в предложном падеже:
  «Луна в Деве», «до новолуния в Рыбах» (§6 SPEC_FEED_VISUAL.md).

Общее место — не общий алгоритм: оба словаря остались перечислением
исключений, не суффиксным правилом (см. докстринг файла, почему). Общий
файл нужен, чтобы третий такой случай на фронтенде начинали искать здесь, а
не копированием готовой функции в новый компонент — так уже было один раз
до этой правки.

### Из мобильного приложения можно вести на веб-оплату — RuStore, не Google Play

Поправка от 06.09.2026, после проверки правил RuStore: предыдущая
формулировка («нельзя вести на веб-оплату и на страницу цен») была взята
из политики Google Play и здесь не применима — публикуемся в RuStore, а
он прямо разрешает монетизацию вне собственных платёжных SDK и не берёт
комиссию за внешние платежи.

Второй (нативный) платёжный контур не строится по двум причинам:

1. **Монетизация RuStore физически недоступна.** Она открыта только ИП и
   юрлицам; самозанятым отключена с 12.12.2025. Регистрировать ИП ради
   этого не будем.
2. **Оплата в приложении идёт на сайт, через уже существующую ЮKassa** —
   второй платёжный контур (нативная подписка через RuStore Billing и
   т.п.) не строим: это отдельная интеграция ради канала, которым нельзя
   воспользоваться.

Вести из мобильного приложения на страницу тарифов и на оплату —
**разрешено**. Это снимает ограничение, которое стояло на кнопке чата с
Аристеей (FAB-задача, free/Вега): вести её на `/pricing`/чекаут во внешнем
браузере можно.

⚠️ **Непроверено живой модерацией.** Правила RuStore это разрешают на
бумаге, но ни одной публикации с переходом на внешнюю оплату ещё не было
— при первой такой публикации нужно явно убедиться, что модерация её
пропустила, и только тогда считать этот пункт подтверждённым практикой,
а не только текстом правил.

### Отменено 10.09.2026: правило «один плагин на приложение»

Решение владельца. Правило существовало без причины: его нигде не выводили
из ограничения — ни размера APK, ни модерации, ни поддержки, — а
пользовались им как твёрдым решением. **Оно и фактически не выполнялось:
нативных плагинов в проекте с самого начала два** — `@capacitor/preferences`
и `@capacitor/browser` (`@capacitor/core` и `@capacitor/android` плагинами
не являются, это ядро и платформа). То есть при каждой ссылке на правило
проект уже был в состоянии, которое правило запрещает.

⚠️ **Записано ради того, чем это обошлось: правило дважды влияло на
планирование, и каждый раз задача откладывалась со ссылкой на него, а не по
существу.** «Поделиться» (`@capacitor/share`), скачивание PDF
(`@capacitor/filesystem` + открытие файла), клавиатура в чате
(`@capacitor/keyboard`) — во всех трёх разборах счёт плагинов стоял в
графе «почему нет», рядом с настоящими причинами и неотличимо от них.
Настоящая причина у «Поделиться» была другая и достаточная: задача
закрывалась уже имеющимися средствами.

**Взамен — не запрет, а требование спросить: нативный плагин добавляется
отдельным решением владельца.** В решении назвать две вещи: что плагин даёт
(какую задачу закрывает и почему она не закрывается тем, что уже есть) и
чего это стоит (нативная зависимость, пересборка, разрешения, размер).
Формулировка «это был бы N-й плагин» аргументом больше не является — сама
по себе она ничего не измеряет.

**Первое такое решение принято 13.09.2026: `@capacitor/local-notifications`,
третий нативный плагин.** По той же форме, что требует правило выше:

* **Что даёт.** Уведомления на устройстве, которые приходят, когда приложение
  закрыто, а телефон офлайн. Уже имеющимися средствами это не закрывается:
  веб-пуши в Capacitor WebView не работают (`PushManager` там может
  отсутствовать вовсе), а `setTimeout` в свёрнутом приложении не выполняется —
  оба пути дают «уведомление приходит, только пока приложение открыто», то есть
  ничего.
* **Чего стоит.** Нативная зависимость и пересборка APK; два рантайм-разрешения
  в манифесте (`POST_NOTIFICATIONS`, `RECEIVE_BOOT_COMPLETED`); системный диалог
  разрешения, отказ в котором на Android необратим. Размер: плагин приезжает
  вместе со своими тремя `BroadcastReceiver`, отдельных ресурсов не тянет.

⚠️ **Что при этом НЕ отменено.** Требование «переводить оба места сразу»
там, где плагин заменяет собой самодельное решение (сегодня это два способа
поделиться ссылкой — `MoreReferralView.jsx` и `ChartShareSheet.jsx`),
остаётся в силе. Оно про целостность интерфейса: два разных способа
поделиться в одном приложении — дефект независимо от того, сколько там
плагинов.

### Мобильные пуши через FCM — ✅ приняты на устройстве 13.09.2026

Живая сводка. Разбор — [docs/HISTORY-push.md](docs/HISTORY-push.md).

**Приёмка пройдена 13.09.2026:** уведомление приходит на ЗАБЛОКИРОВАННЫЙ экран,
то есть устройство будится — ровно то, чего не умеет локальный канал. Значок в
шторке признан приемлемым. Прод: мерж `5cc1523`, миграция `053` применена,
`FCM_PROJECT_ID`/`FCM_SERVICE_ACCOUNT_B64` в `/opt/astro/.env`, проект Firebase
`aristea-f81ac`.

⚠️ **smallIcon: блокер снят, но вопрос вернётся, и вот почему.** Монохромный
логотип оказался не нужен — то, что рисуется сейчас, приемлемо. Но у двух
каналов значки берутся из РАЗНЫХ мест, и после слияния с локальными это станет
видно: `@capacitor/push-notifications` иконку не трогает вовсе (проверено грепом
по его исходникам — там только `MessagingService`), значит для пуша её выбирает
сам SDK Firebase; а локальные уведомления берут дефолт из
`LocalNotificationManager.getDefaultSmallIcon` — системный
`android.R.drawable.ic_dialog_info`, то есть кружок с буквой i. Одно и то же
приложение покажет два разных значка в зависимости от того, каким каналом
пришло уведомление. Лечится одним монохромным drawable, прописанным обоим
(meta-data `com.google.firebase.messaging.default_notification_icon` и поле
`smallIcon` у локальных). До слияния каналов это незаметно и потому не срочно.

⚠️ **Локальные уведомления не будят телефон, и это не чинится настройками.**
Точный будильник мы не запрашиваем (решение владельца), поэтому Android ставит
НЕточный, а тот вправе ждать, пока устройство проснётся само. Проверено на
устройстве: уведомление приходит ровно в момент включения экрана, при любом
способе сворачивания приложения. Для утреннего прогноза это отказ.

- **Доставка отделена от отбора одной функцией** — `send_to_user`
  (`push/sender.py`). Её зовут все четыре потребителя (тик планировщика,
  «Отправить тест», две рассылки пилотной программы), поэтому мобильный канал
  подключён там и достался им всем сразу. Отбор, тексты, склейка,
  `push_sent_log` и окно тишины не менялись ни на строку.
- **Возвращается СУММА доставок по каналам.** По этому числу `_process_user`
  решает, отмечать ли событие в `push_sent_log`: вернуть только веб-доставки
  значило бы не отметить доставленное в приложение и отправлять его заново
  каждые 15 минут.
- **Токены — отдельная таблица `device_tokens`** (миграция `053`), не поле в
  `push_subscriptions`: у той все три поля транспорта NOT NULL, и FCM-токен
  пришлось бы класть в `endpoint` с фиктивными ключами, после чего
  `send_web_push` попытался бы отправить его через pywebpush. Уникален сам
  токен: один телефон — одна запись, при смене аккаунта она ПЕРЕЕЗЖАЕТ.
- **Мёртвый токен удаляется тем же контрактом `PushGone`**, что и мёртвая
  веб-подписка: снаружи транспорты обязаны вести себя одинаково.
- **Ненастроенный FCM — нормальное состояние, а не ошибка.** Прод-гварда на
  `FCM_*` нет намеренно: иначе включение мобильного канала стало бы условием
  запуска сервера. Инвариант держит тест, а не проверка на старте.
- **`keys` в payload** — те же `kind:ref`, по которым дедуплицирует сервер.
  Нужны устройству, чтобы гасить своё локальное уведомление о том же событии.
- **Дедуп между каналами живёт НА УСТРОЙСТВЕ** (решение владельца 13.09.2026):
  получил токен FCM — локальных уведомлений не планирует. Серверный вариант
  («отдавать пустую выдачу в `/push/upcoming`») рассмотрен и отклонён: он
  лишает устройство запасного канала при молчаливом отказе доставки.
  ⚠️ Молчаливые (data-only) сообщения не используем — они доставляются хуже, а
  пришли мы сюда именно за надёжностью доставки. Следствие: гашение по `keys`
  работает, только когда приложение открыто, то есть это СЕТКА, а не механизм.
- **Локальные уведомления остаются запасным каналом** для устройств без
  сервисов Google: там регистрация FCM не удастся никогда.

⚠️ **`google-services.json` секретом не является** — он лежит внутри каждого
APK. Секретом в CI он сделан только потому, что gitleaks принимает его
`current_key` за утёкший ключ API, а заводить исключение в сканере секретов
ради несекретного файла — плохой размен. Настоящий секрет один: ключ
сервис-аккаунта, он в `/opt/astro/.env` (`FCM_SERVICE_ACCOUNT_B64`).

⚠️ **Подключение плагина google-services в шаблоне Capacitor обёрнуто в
try/catch, и в catch стоит `logger.info`.** Нет файла — сборка зелёная, APK
уезжает, пуши молча не работают. Поэтому в `mobile-build.yml` стоят ДВЕ
проверки: файл непустой, валидный и с нашим `package_name` — и, после сборки,
что плагин реально сгенерировал ресурсы с `google_app_id`. Вторая проверяет
факт, а не успех шага, по образцу проверки иконки.

### Дедуп двух каналов: ✅ принят на устройстве 13.09.2026

Приёмка пройдена: тумблер работает, **дубля нет** — событие приходит один раз,
отладочная панель снята, FCM продолжает приходить на заблокированный экран, то
есть выбор канала ничего не сломал.

### Дедуп двух каналов: активен ровно один, и решает это устройство

Правило — `mobile/lib/channelChoice.js`, применяет его единственный тумблер
`MoreDeviceChannel.jsx`:

```
есть токен FCM            → канал server: локальные не планируются, план снят
нет токена, разрешение да → канал device: локальные запасным каналом
нет ни того, ни другого   → не включается ничего
```

⚠️ **Порядок проверок не взаимозаменяем.** Серверный канал будит устройство,
локальный нет — выбрать локальный там, где работает серверный, значит
сознательно отдать человеку худший канал. Поэтому токен проверяется первым.

⚠️ **Оба конца опираются на ОДИН факт — «токен есть».** Сервер шлёт FCM,
потому что у пользователя есть запись в `device_tokens`; устройство не
планирует локальных, потому что токен у него на руках. Разъехаться негде,
поэтому один и тот же `key` не может прийти дважды. Второго протокола между
ними нет и не нужно.

⚠️ **Правило вынесено в отдельный файл не ради красоты.** Внутри обработчика
тумблера инвариант «активен ровно один канал» нечем закрепить, кроме рендера, а
цена нарушения — дубли в шторке у каждого, кто включил уведомления. Закреплено
перебором всех сочетаний входов в `channelChoice.test.js`, причём рядом стоят
assert'ы, что в переборе встретились ОБА вида включённого канала: без них
проверка «не включены оба» зелёная на выборке, где не включается ничего.

Запасной канал честно подписан в интерфейсе: на устройстве без сервисов Google
под тумблером стоит строка о том, что уведомления появятся, когда человек
возьмёт телефон в руки, а не в тот же момент. Обещать там своевременность
значило бы обещать то, чего локальный канал не умеет.

### Локальные уведомления: состояние

Живая сводка. Разборы (как ловили, почему решили так) — в
[docs/HISTORY-push.md](docs/HISTORY-push.md).

- **Склейка событий одного дня — задача КЛИЕНТА**, ручку
  `GET /api/v1/push/upcoming` под это не менять. День с тремя событиями
  отдаётся тремя записями. Клиент склеивает СПИСОК, но не сочиняет ТЕКСТ:
  формулировки остаются серверными.
- **Дедуп — по полю `key` = `kind:ref`**, ровно тому, по которому
  дедуплицирует сервер (уникальный индекс `user_id, kind, ref_key`). Ключ
  собран из события, а не из даты запроса, и переживает правку формулировок.
  `push_sent_log` в выдачу не переносится: он про уже отправленное сервером.
- **Окно отправки — пара границ**: нижняя — время ежедневного прогноза,
  верхняя — `push_quiet_from` (миграция `052`, дефолт `22:00`). Считает одна
  функция `in_send_window`, её зовут и планировщик, и ручка.
  `PATCH /push/settings` отбивает `quiet_from <= daily_time` кодом 422,
  проверяя пару целиком и после присваивания обеих границ. Бессмысленная пара
  трактуется как «верхней границы нет», а не как пустое окно.
- **Разрешение спрашивается только при выраженном намерении** — тумблер в
  «Профиль → Уведомления». Автоматически звать `enablePush` запрещено: на
  Android отказ в системном диалоге окончателен, обычными средствами человек
  его не вернёт, то есть один вопрос в неудачный момент закрывает канал
  навсегда.
- Гейта по тарифу нет и не заводится. Дат в тексте уведомления быть не должно
  — только порядок величины; закреплено `TestNoDatesInNotificationText`.
- **Клиентская сторона появилась 13.09.2026** (ветка `local-notifications`,
  плагин `@capacitor/local-notifications`). Три файла с разными ролями, и
  граница между ними не стилистическая: `mobile/lib/localNotificationPlan.js`
  считает ЧТО показать (ни одного обращения к Capacitor — потому и покрыт
  тестами целиком), `localNotifications.js` умеет поставить это на устройстве,
  `localNotificationsSync.js` держит условия и момент. План пересобирается
  ЦЕЛИКОМ при каждом заходе в приложение и возврате из фона (`TabShell.jsx`):
  уведомление живёт в системе Android, а не в приложении, и расходится с
  реальностью само собой.
- **Точный будильник не запрашивается — решение владельца 13.09.2026.** Ни
  `SCHEDULE_EXACT_ALARM`, ни `USE_EXACT_ALARM` в манифесте нет и не будет:
  впереди публикация в Google Play, где такое разрешение пришлось бы
  обосновывать, а утренней сводке точность до минуты не нужна — сдвиг на 20–40
  минут признан приемлемым. Плагин без этих разрешений сам уходит в
  `setAndAllowWhileIdle` (`LocalNotificationManager.setExactIfPossible`).
  ⚠️ **Практическое следствие: нигде нельзя обещать показ «через N минут» —
  только «не раньше».** Первая приёмка 13.09.2026 упала ровно на этом: кнопка
  обещала две минуты, уведомление не пришло, и разбор пошёл по работающему коду.
- **Запас у ВЕРХНЕЙ границы окна — 45 минут** (`QUIET_MARGIN_MINUTES`,
  `mobile/lib/localNotificationPlan.js`). Раз срабатывание неточное, ставить
  ближе 45 минут к `push_quiet_from` значит целиться в тихие часы: поставленное
  на 21:50 приедет в 22:30. Величина взята от допуска (40 минут) плюс резерв.
  У нижней границы запаса нет и не нужно — сдвиг работает только вперёд.
  ⚠️ Событие, попавшее в запас, **переносится на следующий слот из той же
  выдачи**, а не отбрасывается и не пересчитывается: клиент не собирает времён
  сам, он только выбирает из присланных сервером строк `at`. Своя арифметика
  окна стала бы вторым источником истины ровно так же, как свои формулировки.
  Если следующего слота нет — событие остаётся на месте (тихая потеря дороже
  позднего показа). Если всё окно короче запаса — запас не применяется вовсе,
  по образцу серверного решения про бессмысленную пару границ.
- **Пересборка плана трогает ровно своё множество id.** Список поставленного
  хранится в `localStorage` (`aristea_local_notification_ids`), отменяются
  только свои id, которых нет в новом плане. ⚠️ До 13.09.2026 снималось всё,
  что вернул `getPending`, и проверка «плана нет — выходим» стояла ПОСЛЕ
  отмены: временно опустевшая выдача оставляла человека без уведомлений, а
  чужие записи (проверочное из отладочной панели) исчезали при первом возврате
  в приложение.
- **Серверные тумблеры уведомлений больше не спрятаны за проверкой Web Push.**
  До 13.09.2026 весь экран «Уведомления» закрывался условием
  `serviceWorker && PushManager && Notification`, хотя виды событий, время и
  тихие часы от Web Push не зависят вовсе — это настройки на сервере, и они
  управляют в том числе тем, что попадёт в выдачу `/push/upcoming`. В Android
  WebView, где `PushManager` может отсутствовать, человек увидел бы
  «недоступно» на экране, который работает.

История: Вакуумный тест (проверка «ни один не нарушает» на пустой выборке всегда зелёная) — [docs/HISTORY-testing.md](docs/HISTORY-testing.md).

### Статус на 13.09.2026: оба канала уведомлений приняты на устройстве

| Ветка | Что в ней | Состояние |
|---|---|---|
| `icon-launcher` | цветная иконка лаунчера через `@capacitor/assets`, шаг и проверка в `mobile-build.yml`; отмена правила «один плагин на приложение» | ✅ принята на устройстве 10.09.2026, влита в `main`, ветка удалена |
| `push-upcoming` | `GET /api/v1/push/upcoming`, `push_quiet_from` + миграция `052`, замок на даты | ✅ влита в `main` (`7edee40`, `f0a1689`) |
| `local-notifications` | плагин `@capacitor/local-notifications`, разрешения в `patch-android.mjs` + проверка в CI, запас у тихих часов, адресная отмена в пересборке, выбор канала | ✅ **принята на устройстве 13.09.2026**, влита в `main` |
| `fcm-push` | `device_tokens` + миграция `053`, FCM HTTP v1, диспетчер в `send_to_user`, регистрация токена в приложении, две проверки Firebase в CI | ✅ **принята на устройстве 13.09.2026**, влита в `main` (`5cc1523`) |

**Что проверено на устройстве 13.09.2026:** уведомление приходит на
ЗАБЛОКИРОВАННЫЙ экран (устройство будится — то, чего не умеет локальный канал);
дубля между каналами нет; отладочная панель снята; значок в шторке признан
приемлемым.

**Принято на устройстве 14.09.2026 (APK `34822635435`, коммит `71a312e`):**

* **Выбор времени уведомлений в приложении** (`MoreNotificationsView.jsx`).
  Поле `push_daily_time` существовало на бэкенде и редактировалось на вебе
  (`ProfilePage.jsx`), а `SPEC_MORE_SCREEN.md` §6 требовал его и здесь — то
  есть это был пропуск, а не решение. ⚠️ В отличие от веба поле показывается
  ВСЕГДА, а не только при включённом «Прогнозе дня»: `daily_time` — нижняя
  граница окна для ВСЕХ видов уведомлений (`in_send_window`), спрятав его
  вместе с прогнозом, спрятали бы настройку, управляющую остальными тремя.
  ⚠️ Смена времени обязана звать `syncLocalNotifications()` так же, как смена
  состава: план на устройстве собирается по `daily_time`, иначе молча остался
  бы на старом окне.
* **Тексты приведены к механике.** Было «в выбранное вами время» — обещание,
  которого рассылка не выполняет; стало «начиная с выбранного вами времени», и
  подпись про окно сведена с ним же. Почему `daily_time` — это «не раньше» (и
  почему правильное поведение выглядит как баг), записано в докстринге
  `backend/push/cron.py`; починка самого планировщика — отдельный пункт
  TASKS.md, до него менять формулировки на точные нельзя.
* **Показ пароля** на входе и регистрации (`components/PasswordInput.jsx`,
  общий для веба и приложения). Экраны видны только в разлогиненном
  приложении — при приёмке это первое, обо что спотыкаешься.

⚠️ **`largeIcon` отложен, а `smallIcon` перестал быть блокером — но вернётся.**
Монохромный логотип не понадобился. Однако у двух каналов значок берётся из
РАЗНЫХ мест: `@capacitor/push-notifications` иконку не трогает вовсе (проверено
грепом по его исходникам — там только `MessagingService`), значит для пуша её
выбирает сам SDK Firebase; а локальные уведомления берут системный
`android.R.drawable.ic_dialog_info` (`getDefaultSmallIcon`). Одно приложение
показывает два разных значка в зависимости от канала. Лечится одним монохромным
drawable, прописанным обоим местам (meta-data
`com.google.firebase.messaging.default_notification_icon` и поле `smallIcon` у
локальных).

### ⚠️ CI не собирает мобильный бандл — поломку импорта в `src/mobile/` он не видит

Найдено 13.09.2026 на своей же правке: удалил функцию из
`localNotifications.js`, забыл импорт в `MoreNotificationsView.jsx`, и **все 424
теста остались зелёными**, веб-сборка тоже — потому что `MoreNotificationsView`
в веб-граф не входит вовсе, а vitest грузит только те модули, что тянут тесты.

Job `test-frontend` в `ci.yml` делает `npm test` и `npm run build` — **веб**.
Мобильный вход (`npm run build:mobile`) собирается только в workflow
`Mobile APK`, а он ходит по требованию и на пуш в ветку `mobile`.

Практически: поломка такого рода доезжает до первой сборки APK, то есть до
приёмки — на прод она уехать не может, потому что мобильного кода на проде нет.
Но «CI зелёный» про мобильную часть не означает «собирается». Перед тем как
говорить «готово» по мобильной задаче — гонять `npm run build:mobile` локально
или дожидаться APK, а не CI.

### Артефакт CI `astrea-debug-apk` не переименован вслед за брендом

Переименование Астрея → Аристея (документация подтянута 06.09.2026, см.
раздел «Склонение…» выше) не тронуло имя артефакта в
`.github/workflows/mobile-build.yml` (`upload-artifact: name:
astrea-debug-apk`) и его упоминания в `MOBILE_APK_2026-09-04.md` и
`frontend/CAPACITOR.md`. Это осознанно: имя артефакта — технический
идентификатор, на который ссылаются командой `gh run download -n
astrea-debug-apk`; переименование ничего не даёт продукту (пользователь
имени артефакта не видит вовсе) и один раз сломает скачивание для всех, у
кого команда уже записана. Если понадобится переименовать — переименовывать
везде разом (workflow + оба упоминания в доках) одним коммитом, не по
частям.

История: Построение карты в приложении: форма, маска даты, `chartsVersion`, почему новая карта не становится основной — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

### Поделиться картой из приложения: системного листа нет и не будет дёшево

⚠️ **`navigator.share` в Android WebView отсутствует.** Не «не проверяли», а
проверено по caniuse-lite, лежащему в репозитории (`frontend/node_modules`,
фича `web-share`): строка `android` — `n`, при `and_chr` — `y`. Дело НЕ в
`androidScheme`/origin `https://localhost` — secure context выполнен; Web
Share требует, чтобы его реализовало хост-приложение, а Capacitor этого не
делает. То есть «дешёвый путь через `navigator.share`» — не более дешёвый
путь, а несуществующий: план, построенный на нём, был бы работой над
несуществующей задачей.

История: Почему лист «Поделиться» свой и почему не взяли `@capacitor/share` — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

⚠️ **По публичной ссылке уходят дата и место рождения, а не только знаки.**
`GET /api/v1/share/{token}/data` (`share_router.py:231-249`) отдаёт
`birth_date`, `birth_place` и полное колесо; то же напечатано на самой
картинке (`share_router.py:621-623`). Реальное имя (`chart.name`) не уходит
— карточка подписана `share_name`, а его сегодня не передаёт ни один
вызывающий. Срок — 90 дней (`SHARE_TTL_SECONDS`).

⚠️ **Отзыва ссылки на бэкенде НЕТ.** Эндпоинта, гасящего `public_token`, не
существует: единственный способ закрыть доступ — удалить карту
(`DELETE /profile/charts/{id}`). Поэтому в приложении предупреждение стоит
ДО действия, а не после, и это инвариант, а не текст: множество шагов, где
разрешено действие, обязано быть подмножеством шагов, где предупреждение
видно (`mobile/lib/shareRules.js`, проверяет `shareRules.test.js`). Веб
такого предупреждения не показывает вовсе — расхождение известное, не
чинилось в этом заходе.

⚠️ **PDF из приложения `openInBrowser` не откроет, и это не вопрос
настройки.** `POST /api/v1/chart/{id}/pdf` (`main.py:2310`) — POST с
заголовком Authorization, отдаёт файл вложением; `Browser.open` умеет
только GET без заголовков. GET-альтернативы нет: единственный GET, отдающий
PDF файлом, — `/portal-report/{token}` (`crm/portal_router.py:88`), а это
клиентский портал астролога. Полноценная кнопка = fetch с токеном + запись
через `@capacitor/filesystem` + нативное открытие (`a.download`/blob в
Capacitor WebView не работает — `DownloadListener` не зарегистрирован).
Отложено; в «Ещё» пока ссылка на веб-страницу той же карты.

⚠️ **Тарифный гейт PDF в клиент не копируется.** `check_pdf_limit`
(`auth/rate_limits.py:627`) и его текст отказа — единственный источник;
free сегодня имеет один PDF в месяц (`pdf_export: True`,
`pdf_per_month: 1`). Докстринг самой функции при этом всё ещё утверждает
«Free: запрещено (pdf_export=False)» — устарел относительно флагов рядом,
не ориентироваться на него.

### Отложенное по мобильному приложению — принято на устройстве 06.09.2026, список не терять

Приёмка прошла, три экрана и кнопка чата приняты. Ниже — то, что заметили
по пути и сознательно не стали чинить в этом заходе, чтобы следующая
сессия не открывала это заново как новую находку.

1. **Липкая полоска дней (`FeedDayStrip.jsx`).** Сегодня не липнет —
   прокручивается вместе с лентой. Сделать липкой можно, но ⚠️ **не вместе
   с липким заголовком дня** (`FeedDayHeader.jsx`, уже `position: sticky`,
   §6 SPEC_FEED_VISUAL.md): на телефоне высота экрана и так в дефиците, а
   два одновременно прилипших элемента (полоска + заголовок) съедят
   заметную долю — на компактных экранах доходит до трети видимой высоты
   ленты, прежде чем начнётся первая карточка. Выбрать одно, не оба.
2. **Планшетная раскладка не сделана.** На ширине от ~768px лента (и,
   вероятно, Карта/Ещё) растягивается на всю ширину экрана — макеты
   верстались под телефон, брейкпоинтов нет ни одного. Не проверялось на
   реальном планшете, только логический вывод из вёрстки.
3. ✅ Закрыто 07.09.2026: подпись «Планеты сейчас в домах» над блоком
   чипов домов (`FeedNowStrip.jsx`), тем же стилем, что у остальных
   подзаголовков шапки ленты — заменила собой общее «Сейчас».
4. ✅ Закрыто 07.09.2026: пробел перед «·» в подсказке о зуме
   (`mobile/screens/ChartScreen.jsx`) на проверке уже стоял на месте —
   опечатка из находки 06.09.2026 не подтвердилась повторно, править было
   нечего.
5. **`minSdkVersion 23` — умолчание шаблона Capacitor, не решение
   владельца.** Стоит в `frontend/android/variables.gradle` с самого
   каркаса Android-сборки (`a420537`, 04.09.2026) — никто осознанно не
   выбирал именно 23 (Android 6.0, 2015 год) как нижнюю границу
   поддержки. При следующей правке native-кода (например, если всё же
   понадобится `addJavascriptInterface`-мост из разобранного варианта
   §6.2 `SPEC_MORE_SCREEN.md`) прежде поднять вопрос отдельно: сузить до
   API 26+/29+ убирает необходимость возиться с рантайм-разрешениями
   ради исчезающей на 2026 год доли устройств, но это выбор владельца, не
   то, что можно молча поменять по ходу другой задачи.

### Портретная ориентация — осознанный патч, не умолчание шаблона

В отличие от `minSdkVersion 23` выше, `android:screenOrientation="portrait"`
на `MainActivity` — **не** то, что оставил без изменений шаблон Capacitor.
Это явный патч `frontend/scripts/patch-android.mjs` (шаг
`screenOrientation=portrait`), применяемый на каждой сборке и проверяемый
в CI (`mobile-build.yml`, `grep -q 'android:screenOrientation="portrait"'`
после генерации проекта). Причина в комментарии рядом с патчем: экраны
(форма входа, таб-бар, онбординг) свёрстаны только под портрет, без
альбомной альтернативы — без запрета поворот дал бы не адаптивный
лейаут, а обрезанные и наползающие друг на друга элементы.

Найдено 07.09.2026 при приёмке онбординга: пункт чек-листа «повернуть
экран во время подсказки» оказался непроверяемым — устройство физически
не поворачивается, ориентация зафиксирована на уровне нативной
активности, до всякого JS. Не дефект подсказок, снят из
`SPEC_ONBOARDING.md` §14.

### Иконка лаунчера генерируется в CI, в `res/` её положить нельзя

Положить готовые PNG в `android/app/src/main/res/` невозможно: каталог
`android/` в `.gitignore`, CI создаёт его заново каждым прогоном. Поэтому
иконка не хранится, а СОБИРАЕТСЯ: исходник `frontend/assets/logo.png`
(1024×1024, непрозрачный) + шаг `@capacitor/assets` в `mobile-build.yml`.
Генератор — в `devDependencies`: он работает на машине сборки и в APK не
попадает вовсе, то есть решением о нативном плагине не является.

История: Режим «один логотип», `--iconBackgroundColor`, геометрия `inset 16.7%` и ошибка с «35% ширины» — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

**Порядок шагов в `mobile-build.yml` и почему он такой:** генератор стоит
после `cap add`/`cap sync` (раньше каталога просто нет) и ДО
`patch-android.mjs`. Причина не в `cap sync` — проверено исполнением
10.09.2026, что `sync` трогает только `assets/public`,
`capacitor.config.json` и список плагинов, а `res/` не переписывает. Причина
в том, что **генератор переписывает `AndroidManifest.xml`** (проставляет
`android:icon`/`roundIcon` и переформатирует файл целиком: схлопывает
`meta-data`, убирает пустые строки). Атрибуты патчей это переживают
(проверено), но гарантией должна быть последняя операция, а не первая.

**Проверка в CI сравнивает с шаблоном, а не с прибитым хешем.** Шаг
«Проверить, что иконка не шаблонная» распаковывает tarball шаблона из
`node_modules` и делает `cmp` — при апгрейде Capacitor эталон обновится сам.
Прибитый хеш начал бы сравнивать с состоянием, которого больше нет, то есть
проходил бы всегда. Ловится тут ровно тот отказ, который ничего не роняет:
генератор не нашёл `assets/`, отработал вхолодную с кодом 0, APK уехал с
шаблонной иконкой.

⚠️ **Проверяются ОБА пути, и это не перестраховка.** Только legacy было бы
недостаточно: Android 8+ берёт `mipmap-anydpi-v26/ic_launcher.xml` и на
legacy-PNG не смотрит вовсе. Ровно поэтому одного `icon-only.png` (он пишет
только legacy) для задачи «поменять иконку» не хватает — на всех реальных
устройствах осталась бы дефолтная. Оба отрицательных случая прогнаны руками
10.09.2026: подмена любого из двух файлов шаблонным роняет шаг.

История: Цена `@capacitor/assets` по зависимостям (вложенный `@capacitor/cli ^5`, рост `npm audit`) — [docs/HISTORY-mobile.md](docs/HISTORY-mobile.md).

---

## Приложение без сети — кэш на устройстве (24.09.2026, приёмка НЕ пройдена)

Задача владельца: без сети показывать последнее загруженное с пометкой
«Без сети · данные от 08:14», а не экран ошибки. Код — `mobile/lib/offlineCache.js`,
`netError.js`, `useReconnect.js`, `authFetchTimeout.js` (повторы),
`forecastPrefetch.js`, `components/OfflineNote.jsx`.

* **Хранилище — Capacitor Preferences**, не localStorage (его вправе вычистить
  система) и не файлы (`@capacitor/filesystem` — новый плагин). ⚠️ На Android
  это SharedPreferences, файл целиком в памяти и переписывается на каждой
  записи, поэтому лента кладётся ОБРЕЗАННОЙ (`trimFeed`: 7 дней назад, 30
  вперёд, плюс идущие сейчас периоды), а не окном в 365 дней.
* **Версия формата — в ключе** (`aristea_offline:v1:`). Старая версия, битый
  JSON, чужой владелец (`sub` токена) — запись молча выбрасывается. При потере
  сессии стирается всё (`MobileApp.jsx`, рядом с `resetSubscription`).
* ⚠️ **Первый прогон теста повис на `return s.plugin` из async-функции** — тот
  самый дефект из раздела «Объект плагина Capacitor…». Тест
  `offlineCache.test.js` подделывает плагин thenable-Proxy'ем именно поэтому.
* **Тариф с диска — только для показа в «Ещё».** В `tierSource` он не
  читается: для доступа без сети тариф неизвестен, `known: false`. ⚠️ И
  «неизвестен» не включает пейволла: `useChatAccess` с 24.09.2026 возвращает
  `null` (не `false`), и кнопка чата при `null` не рисуется вовсе — `false`
  рисовал замок и вёл к блоку тарифа, то есть платящему без сети показывался
  пейволл. Закреплено `offlineNoPaywall.test.js` по всем местам, где решает
  тариф. Размытие решает серверный `event.locked` из сохранённой ленты — у
  платного его нет. Из сохранённой ленты выброшен `horizon.next_tier`.
* **Прогноз дня — ключ по дате карточки**, поэтому вчерашний под «сегодня» не
  встанет. Сохранённый текст модели показывается без запроса; запасной
  (`source: 'fallback'`) сервер не кэширует — его перезапрашиваем.
* **Завтрашний прогноз отдельно не подгружается, и это не пропуск:** с 19:00
  карточка «завтра» стоит в ленте и грузит себя сама (лента рисует все дни),
  до 19:00 сервер отвечает 404. Раннюю выдачу владелец отклонил. Заранее
  подгружается только ближайшая фаза — её прогноз открывается из панели.
* **Разбор хранится только основной карты**; смена основной стирает его
  (`rememberCharts`). Сохраняется только полный (`shouldCacheInterpretation`).
* ⚠️ **Признак полного текста — только `[DONE]`.** До 24.09.2026 `_connectSSE`
  (`api/client.js`, общий с вебом) на обрыв после первых данных звал `onDone`:
  обрубок выглядел готовым разбором и в вебе, и в приложении. Теперь это
  `onError('Connection lost')`, и после данных транспорт НЕ переподключается —
  докачки у ручек нет (`last_event_id` сервер не читает), поток начался бы
  сначала и текст задвоился бы. Держит `api/sseTransport.test.js` (обратный
  прогон на старом коде: три кейса падают). Сервер обрубка не хранит: в БД и
  Redis пишет только после полного цикла, а при отключении клиента генератор
  закрывается раньше — `TestClientDisconnect` (`test_interpretation_persisted.py`,
  отключение по ASGI посреди потока) и `TestStreamClosedByClient`
  (`test_ai_cascade.py`).
* **Повторы — только GET** (`getWithRetry`): таймаут — один, 5xx — до двух,
  паузы 1 и 3 с. Нет сети, 429 и прочие 4xx не повторяются. POST/PATCH/DELETE
  и оплата идут через `authFetchWithTimeout` без повторов.
* **Перезапрос сам — `online`, возврат из фона и раз в 30 с**, только пока
  экран показывает сохранённое или «нет сети» и приложение видно. ⚠️ Интервал
  не перестраховка: надёжность `online` в Android WebView при выходе из режима
  полёта не проверена.
* **Без сети «Войти заново» на экране ошибки не показывается**: выход стёр бы
  сессию и кэш, а войти обратно без сети нельзя.
* **Тексты сетевых ошибок — два набора** (`NET_TEXT` для загрузки,
  `NET_WRITE_TEXT` для действий): «покажем, когда появится» при сохранении было
  бы неправдой. Вход и регистрация ходят через `useAuth` мимо
  `authFetchWithTimeout` — там перевод `humanizeErrorText`/`registerRules`.
* **Sentry:** нет сети и таймаут не отправляются (`isNetworkNoise` в
  `main.mobile.jsx`). ⚠️ Признак — TypeError С ТЕКСТОМ «Failed to fetch», а не
  любой TypeError: «Cannot read properties of undefined» — настоящая поломка.
* Локальные уведомления без сети работают: текст кладётся в систему в момент
  планирования, а при отказе `/push/upcoming` план не снимается.

---

## Токены оформления и Framer Motion — две ловушки, выглядящие как недоделка

✅ **Зачистка принята на вебе и на устройстве 14.09.2026** (коммит `0d67b0e`,
APK — прогон `34856961464`). Веб: лендинг, тарифы, натальная карта, лунный
календарь, планер. Устройство: лента, натальная карта, «Уведомления».
Проверялось адресно то, что могло сломаться молча: колесо зодиака
отрисовано (перенос шести значений из атрибутов SVG в `style` сработал),
hover кнопок и подсветка активного тарифа на месте, анимация появления
карточки превью жива, сдвиг радиусов на 2–4px глазом не ловится.

Что сведено к токенам: `--accent-rgb` (106 литералов), `--radius-*`
(227 замен), `--shadow-*` (38), `--font-display`/`--font-body` на вебе (36).
Цвет при этом НЕ менялся — палитра, шрифтовая пара и градиент фона остаются
задачей редизайна (TASKS.md, «Визуальный язык выглядит сгенерированным ИИ»).

Обе найдены 14.09.2026 при уборке перед редизажном (`--accent-rgb`, `--radius-*`,
`--shadow-*`). Общее у них то, что правильный код выглядит избыточным, а
«очевидное упрощение» ломает картинку молча — без ошибки в консоли и без
падения сборки.

### `var()` внутри значения `box-shadow` анимация не резолвит — только целым значением

**Framer Motion подставляет CSS-переменную, только если ВСЁ значение —
это `var(--x)`.** В `readKeyframes` (`framer-motion/dist/framer-motion.dev.js`,
проверено на 12.42.2) ключевой кадр проходит через `isCSSVariableToken`, а тот
требует, чтобы строка начиналась с `var(--` и целиком ему соответствовала
(`singleCssVariableRegex` с якорем `$`). Рядом лежит `containsCSSVariable`,
который ловит `var()` в середине строки, — но он применяется ТОЛЬКО к
позиционным свойствам (ширина, высота, смещения), к `boxShadow` не применяется
вовсе.

Практически это значит: `boxShadow: '0 8px 24px rgba(var(--accent-rgb), 0.32)'`
как цель анимации (`hover`, `whileHover`, `animate`) — **нечитаемая для Framer
строка**: он разбирает её числовым парсером, `var(...)` числом не является,
плавного перехода не выйдет. А `boxShadow: 'var(--shadow-accent)'` — читаемая:
переменная снимается с элемента через `getComputedStyle` и дальше
интерполируется как обычная тень.

⚠️ **Поэтому тень в анимации подставляется ОДНИМ токеном, а не собирается из
кусков, и это не стилистика.** Разложить `--shadow-accent` обратно на
«геометрия литералом + цвет из `--accent-rgb`» выглядит как более гибкий код и
тихо убивает hover-отклик кнопок (§5 B2 `DESIGN_SYSTEM.md`): тень появится
рывком или не появится. Ошибки при этом нет нигде — ни в консоли, ни в тестах,
ни в сборке.

Затронуто сегодня три места: `MotionButton.jsx` (`hover`),
`LyraPaywallModal.jsx` и `PlanComparisonModal.jsx` (`whileHover`). Статические
`style={{}}` этого ограничения не имеют — там `var()` внутри строки разбирает
браузер, и вложенная форма законна; ограничение касается только значений,
которые анимирует Framer.

### `0 0px 0px rgba(0,0,0,0)` в анимации — не мусор, а нулевое состояние

`LandingPage.jsx` анимирует появление тени у карточки превью: `initial` —
нулевая тень, `animate` — `var(--shadow-raised)`. Строка `0 0px 0px
rgba(0,0,0,0)` выглядит написанной по ошибке и первым делом просится в
удаление как мёртвый код — в этой самой уборке она и была так помечена, пока
не выяснилось, для чего стоит.

⚠️ **Заменить её на `none` нельзя: Framer интерполирует числа, а `none` — не
набор чисел.** Переход выродится в скачок либо пропадёт совсем. Нулевая тень
той же формы — штатный способ записать «тени ещё нет» для анимации, и по этой
же причине её нельзя свести к токену: у токена ненулевые значения.

**Правило для обоих случаев: перед тем как упрощать значение `box-shadow`,
посмотреть, не анимируется ли оно.** Если это цель Framer — значение обязано
быть либо целым токеном, либо полным набором чисел; всё промежуточное
ломается беззвучно.

---

## Шрифтовая пара: четыре места переведены ВСЛЕПУЮ, приёмки по ним нет

Пара Literata (антиква) + Golos Text принята владельцем 14.09.2026. Правило
границы, отклонённые кандидаты и замеры — `DESIGN_SYSTEM.md` §3, здесь только
то, что иначе потеряется.

⚠️ **Перевод на антикву в четырёх местах сделан вслепую и НЕ ПРИНЯТ:**

* `AdvancedInterpretation.jsx` — разбор соляра, синастрии и релокации;
* `CRMPage.jsx` — бриф к встрече, резюме клиента, натальный разбор клиента.

**Причина: страницы `/solar-return`, `/synastry`, `/relocation` и кабинет
астролога на 14.09.2026 неактивны — открыть и посмотреть нельзя ни на проде,
ни локально.** Код написан по правилу §3 и собирается, но глазами его не видел
никто. Всё остальное по шрифтам принято: веб — на живом сайте, приложение — на
устройстве.

⚠️ **Что нужно сделать при запуске любой из этих страниц: включить в её приёмку
отдельную строку про шрифт.** Иначе к тому моменту никто не вспомнит, что там
что-то меняли, и антиква либо окажется неуместной и останется, либо не
применится вовсе и это спишут на «так и было». Оба исхода тихие.

⚠️ Рядом в CRM намеренно ОСТАВЛЕН гротеск — заметки астролога, задание
клиенту, вопрос из анкеты, «Прогноз по группе». Это тексты людей и списки, а
не проза модели; если при запуске они окажутся антиквой — это дефект, а не
чужое решение.

Отдельно: кнопки во всём проекте набраны АНТИКВОЙ (берут `--font-display`),
и это не итог, а незакрытое решение — образец `frontend/font-buttons-specimen.html`.

---

## Снятие локальных пространств токенов: два места НЕ ПРИНЯТЫ

Правка 15.09.2026 (`5d03c63`, выкачена на прод): пять локальных пространств
токенов — `tt`, `lc`, `crm`, `rc`, `prof` — сведены с 55 переменных до 15.
Затронуто 152 места в пяти файлах. Разбор — `DESIGN_SYSTEM.md` §2 и TASKS.md.

**Приёмка в тёмной теме пройдена** на профиле, шкале транзитов, лунном
календаре и чате Аристеи — регрессов нет.

⚠️ **Два места приёмки НЕ проходили, и оба увидит только тот, кто запустит
CRM или поймает загрузку:**

1. **Кабинет астролога `/dashboard/clients`** — под тарифным гейтом `premium`,
   открыть не удалось. Это **единственное место во всей правке, где цвет
   действительно изменился**: `--crm-card` был `rgba(26,18,48,0.55)` в тёмной
   теме, стал общим `--bg-card-veil` = `rgba(26,18,48,0.60)`. Решение владельца
   по замеру: разница 0.28–0.31 L\* над тёмной частью небулы при пороге
   различимости около 1.0, на стыке двух половин шва не видно
   (`veil-055-vs-060.png`). Ожидается, что глаз её не поймает, но **проверено
   это расчётом и снимком, а не живой страницей**.
2. **Скелетон загрузки шкалы транзитов** (`TransitTimeline.jsx`) — у заглушки
   снята левая цветная полоса: у загруженной карточки события её нет, и
   заглушка обещала элемент, который потом исчезает. ⚠️ Видно доли секунды при
   загрузке, ловится только жёстким обновлением страницы карты
   (Ctrl+Shift+R). Пропустить при обычной проверке невозможно не по
   невнимательности — этот кадр просто не успевает попасться.

⚠️ **Что нужно сделать при запуске CRM: включить в её приёмку отдельную строку
про фон карточек.** Иначе к тому моменту никто не вспомнит, что там менялась
прозрачность, и разница либо окажется заметной и останется, либо её спишут на
«так и было». Оба исхода тихие — ровно как с антиквой на неактивных страницах
в разделе выше.

---

## Приёмка начинается со СВЕТЛОЙ темы: тёмная прячет целый класс ошибок

Правило, а не наблюдение. Выведено 15.09.2026 из пяти дефектов, найденных
разом при уборке CSS-блоков (`4492bd5`) — все пять жили в проде и все пять
невидимы в тёмной теме.

**Механика.** Токен вида `--accent-muted` — это ФОН с альфой (`rgba(accent,
0.08)` в светлой, `0.15` в тёмной), а не цвет. Поставленный в `color:`, он
даёт текст, состоящий из 8 % акцента поверх подложки:

| | светлая | тёмная |
|---|---|---|
| `color: var(--accent-muted)` на карточке | **1.10:1** | 1.18:1 |
| `#fff` на `background: var(--bg-deeper)` | **1.03:1** | **16.22:1** |

⚠️ **Вторая строка и есть суть правила.** В тёмной теме `--bg-deeper` — это
`#231C38`, тёмный, и белый текст на нём читается отлично. В светлой тот же
токен `#FDFBFF`, почти белый, и элемент исчезает совсем. **Один и тот же код,
одна тема безупречна, другая пустая.** Проверяющий тёмную не видит ничего
подозрительного и подписывает.

Четыре дефекта из пяти были первой строкой (`--accent-muted` как цвет текста),
пятый — второй. Среди них поле ввода опроса: человек печатал и не видел
собственный текст.

**Правило: любая правка цвета, токенов или тем принимается СНАЧАЛА в светлой
теме, и только потом в тёмной.** Светлая — основная (умолчание `useDarkMode`
в `App.jsx` возвращает `false`), то есть ошибка в ней достаётся большинству.
Обратный порядок — тот, которым эти пять дефектов доехали до прода.

⚠️ **Отдельно: `--accent-muted`, `--accent-fill`, `--bg-deeper`, `--bg-card`,
`--bg-*-veil` — ФОНЫ. В `color:` они не ставятся никогда.** Для акцентного
текста есть `--accent-fg`, он для того и заведён. Правило простое: если имя
токена начинается на `--bg-` или заканчивается на `-muted`/`-fill`/`-veil` —
в свойстве `color` ему не место.

⚠️ **Тот же приём остаётся ещё в ТРЁХ местах, все на незапущенных страницах:**
`PortalPage.jsx:106` (заголовок клиентского портала), `PortalPage.jsx:113`
(тело задания) и `IntakePage.jsx:147` (заголовок анкеты клиента). Не чинилось
вместе с остальными, потому что страницы открыть нельзя и приёмку по ним не
пройти. ⚠️ Оба экрана — клиентские, то есть их первым увидит не владелец, а
клиент астролога. Включить в приёмку при запуске CRM; пункт висит в TASKS.md.

---

### ⚠️ `npm run build` в CI поднимает headless-хром и потому флакает

Найдено 14.09.2026: job `test-frontend` упал на `npm run build` с
`Timed out after 30000 ms while waiting for the WS endpoint URL`. Тесты при
этом прошли целиком (458/458 в обоих поясах) — упал шаг сборки, а внутри неё
пререндер (`scripts/prerender.mjs`, раздел «Индексация» ниже).

**Это флак, а не поломка, и доказывается сравнением прогонов:** на ТОМ ЖЕ
коммите push-прогон отработал успешно, а одновременно шедший
`workflow_dispatch` — нет. Два прогона делили раннер, и браузер не уложился в
тридцатисекундный таймаут запуска.

⚠️ **Практическое следствие: упавший `test-frontend` НЕ означает сломанный
код.** Сначала посмотреть, на каком шаге упало. Если на `Build` со словами
`WS endpoint` — это запуск хрома, лечится `gh run rerun <ID> --failed`, а не
разбором своей правки. Отдельно помнить, что `deploy` при этом пропускается
(`needs`), то есть прод не обновится, а в Actions это выглядит как «деплой не
запускался».

## AI-контуры — их четыре, и это не одна цепочка

До 23.08.2026 здесь была строка «GPT-4o → DeepSeek V3 → Template engine». Она
противоречила разделу «Тарифы» выше и не описывала ни один реальный путь
исполнения. Ниже — то, что фактически делает код.

**1. Интерпретация натальной карты** (`interpretation/router.py`).
Порядок движков строит `_engines_for_tier`, и **DeepSeek стоит первым всегда**,
включая pro и premium:

```
DeepSeek v4-pro → GPT-4o → Template
   (всегда)      (только pro/premium,   (всегда доступен)
                  и только если DeepSeek
                  упал или не прошёл
                  валидацию ответа)
```

Модель DeepSeek одна на все тарифы — `TIER_FLAGS[*]["ai_engine"]`
(`auth/rate_limits.py`) во всех четырёх тарифах равен
`settings.deepseek_model_pro`. Тариф регулирует **глубину** через
`interpretation_word_limit` (500/800/2500/5000), а не модель — это решение
владельца 19.08.2026, см. раздел «Тарифы».

⚠️ Докстринг модуля `interpretation/router.py` («GPT-4o → DeepSeek → Template
fallback chain») и его же комментарий в классе `InterpretationRouter` — тот же
устаревший текст. Ориентироваться на `_engines_for_tier`/`select_model`, не на
докстринг. `select_model` возвращает «предпочтительный» движок, но им
определяется только, попадёт ли GPT-4o в список вообще, — первым в очереди он
не становится никогда.

**2. Чат Аристеи (RAG)** — `interpretation/rag_router.py`, отдельно и всегда
`settings.deepseek_model_flash`. К цепочке выше отношения не имеет.

**3. Общий астрокалендарь** (`GET /calendar/monthly`) — `main.py`, прямой
вызов `httpx` **мимо** `InterpretationRouter`, только Anthropic Claude Sonnet
(`claude-sonnet-4-20250514`), запасного провайдера нет: при сбое календарь
отдаётся без обзора (`overview: null`), и такой ответ не кэшируется.

Ключ `ANTHROPIC_API_KEY` читается через `os.getenv` напрямую, а не из
`Settings` (`config.py`) — в `Settings` его нет. Расход пишет
`track_claude_spend` (из `usage` ответа), ключ проверки бюджета — `"claude"`.
До 23.09.2026 контур обслуживал ещё прогнозы daily/weekly/monthly с
фолбэком на OpenAI GPT-4o; они удалены, а с ними `track_openai_spend` и
`OPENAI_API_KEY` в этом контуре. Проверка записи расхода — на календаре
(`test_claude_budget_spend.py`).

**4. Прогнозы в приложении** — `backend/forecast/` (с 23.09.2026): на день
(`GET /chart/{id}/forecast/day?date=&tz=`) и на новолуние/полнолуние
(`GET /chart/{id}/forecast/lunation?phase=&date=&tz=`). DeepSeek Pro без
потока (`settings.deepseek_model_pro`), для всех тарифов, **без квоты и без
гейта** (раздел про `transit_ai` выше). Расход — `track_engine_spend(
"deepseek", …)` с контурами `forecast/today` и `forecast/lunation`.

* **Модель видит смыслы, а не астрологию** (`meanings.py`): в дневном промпте
  нет ни планет, ни аспектов, ни домов — повторить термин ей не из чего. В
  лунном знак и планеты разрешены, но только с пояснением «а значит…».
* **Каждый ответ проходит `validate.py`**: не прошёл — один повтор, потом
  запасной текст из тех же смыслов (`fallback.py`), а не 503. ⚠️ Проверка — по
  целым словам и оборотам, не по подстрокам: «побудь дома», «рабочая среда»,
  «выбор» обязаны проходить (набор нормальных фраз закреплён тестом). Даты в
  лунном тексте — только посчитанные кодом и переданные в промпт.
* **Кэш — только ответ модели**, ключ с версией промпта
  (`DAILY_PROMPT_VERSION`/`LUNATION_PROMPT_VERSION` в `prompts.py`): подними
  версию — старые тексты перестанут отдаваться сами.
* **Граница дня — пояс телефона** (`tz` в запросе), без него — пояс карты.
  Уведомления на пояс телефона НЕ переведены — долг в TASKS.md.
* **Открыты три дня: вчера, сегодня и — с 19:00 по местному времени —
  завтра** (решение владельца 24.09.2026); остальное — 404. 19:00 для ВСЕХ, а
  не только для тех, кому вечернее уведомление уходит в 19:00: уведомление не
  должно вести к закрытой карточке. ⚠️ Граница записана в двух местах —
  `TOMORROW_OPEN_HOUR` в `forecast/router.py` и в `mobile/lib/feedAnchor.js`;
  разойдутся — карточка «завтра» откроется в 404 (или не появится вовсе).
* ⚠️ **Текст дня — без «сегодня/завтра/вчера»**, и это не стилистика. Кэш по
  дате, и один и тот же текст показывается вечером накануне как «завтра»,
  днём как «сегодня» и наутро как «вчера»; роль называет только подпись
  карточки. Проверка (`validate.py`) такие слова отбраковывает. Образец
  владельца с «Сегодня…» остался образцом тона — модели сказано это слово не
  брать.
* **Вечернее уведомление «прогноз на завтра»** (`push/cron.py`, вид
  `tomorrow`, `target: "feed_tomorrow"`): обычно 20:00; если тихие часы
  начинаются в 21:00 или раньше — 19:00; если в 19:00 или раньше — не уходит.
  Тумблер общий с утренним «Прогнозом дня». ⚠️ Отдельным пушем, мимо
  `_collect_candidates`: склейка с утренним и потолок мягких (1 за 48 ч)
  съели бы его через день. В `/push/upcoming` — своим временем, а не утренним
  слотом.
* **Карта без времени рождения — урезанный режим**: без домов и без касаний к
  натальной Луне (при ±6° и орбе 2° это шум). Фазы — полностью, кроме дома
  точки фазы.
* Образцы стиля — `docs/samples/` (два лунных, чтобы модель брала форму, а не
  содержание) и `DAILY_SAMPLE` в `prompts.py`. Все на «ты» в выдаче, хотя
  лунные образцы написаны на «вы».

Общий суточный бюджет `BudgetTracker` делится между всеми четырьмя
контурами; при исчерпании интерпретации уходят в Template, а прогнозы — в
запасной текст.

### Самопроверка прогнозов: запасной текст прячет поломку

С 24.09.2026. Код — `backend/selfcheck.py`, счётчики — `backend/forecast/stats.py`,
тесты — `test_selfcheck.py`. Повод: у конкурента прогноз на день был сломан
больше года, и этого никто не заметил. У нас запасной текст спасает от пустой
карточки, но тем же самым делает поломку невидимой.

* **Утром в 07:30 МСК** (Beat `tasks.selfcheck_daily`) проверяются четыре шага на
  служебной карте: прогноз на СЕГОДНЯ и из модели, ближайшая фаза из модели,
  лента на сегодня с лунными событиями, планер на текущий месяц.
* **Каждый час в :50** (`tasks.selfcheck_hourly`) проверяются ключ, бюджет
  (< 20 % — сигнал), пробный запрос к модели на 1 токен и доля запасных
  текстов за скользящие 24 ч (> 20 % при ≥ 10 прогнозах). Заодно повторяются
  утренние шаги, которые сейчас красные.
* Сигналы идут в чат поддержки (`send_support_message`), по одному на
  инцидент, с сообщением «починилось» при восстановлении. Инцидент — ключ
  `astro:incident:<имя>` в Redis без TTL.

⚠️ **Пробный запрос делается к генерации, а не к `/v1/models`:** когда на счёте
DeepSeek кончаются деньги, `/models` остаётся зелёным (так устроен и `/health/ai`),
а генерация отвечает 402.

⚠️ **«Нет ключа» и «бюджет кончился» проверяются по состоянию, а не по счётчику
запасных,** и минимальной выборки у них нет (решение владельца): при двух
пользователях порог в 10 прогнозов в сутки не наберётся никогда.

⚠️ **Кэш-хит прогноза считается показом «из модели».** Запасной текст не
кэшируется и отдаётся заново на каждом открытии, так что без этого одна
неудача, открытая пять раз, перевешивала бы пять удачных показов.

⚠️ **Служебная карта существует только в памяти** — без пользователя и без
строки в БД, поэтому в метрики и рассылки ей попадать неоткуда. Счётчики её не
учитывают (`SELFCHECK_CHART_ID`). Авторизацию и маршруты HTTP самопроверка не
проверяет.

⚠️ Beat-сторож по-прежнему следит только за `expire_subscriptions`. Если beat
умер, об этом скажет он (с задержкой до суток), а не самопроверка: она
остановится вместе с beat.

### Sentry: три проекта, регион EU (`ingest.de.sentry.io`)

Где лежат DSN (с 24.09.2026): бэкенд — `SENTRY_DSN` в `/opt/astro/.env`
(api, worker и beat получают её через `env_file`); веб — `VITE_SENTRY_DSN` в
`/opt/astro/frontend.env` (его читает `04-frontend-deploy.sh`, в git этого
файла нет); APK — `VITE_SENTRY_DSN` в `frontend/.env.mobile`, в git, потому что
CI собирает APK только из репозитория. DSN не секрет — в APK и в бандле веба он
лежит открытым текстом.

⚠️ **IP в браузерных событиях SDK не контролирует.** Для платформы
`javascript` сервер Sentry сам подставляет IP отправителя, если в событии его
нет, так что `delete event.user` от этого не спасает. Защищает только
настройка проекта: Settings → Security & Privacy → Prevent Storing of IP
Addresses. Её нужно включить в проектах веба и приложения.


Бэкенд подключается через `backend/sentry_setup.py` (api, worker, beat), веб —
`main.jsx`, приложение — `main.mobile.jsx` (только JS, без `@sentry/capacitor`).
Пустой DSN означает, что ничего не инициализируется и не отправляется, а во
фронтенде SDK вырезается из бандла (проверено сборкой APK с DSN и без).
Фильтр персональных данных — `scrub` на бэкенде и `lib/sentryScrub.js` на
фронтенде. ⚠️ Тело запроса и query string удаляются целиком, а не
маскируются по списку ключей: в них лежат дата и место рождения и вопросы к
Аристее, а список ключей рано или поздно отстанет от новой ручки.
Версия бэкенда — `GIT_SHA` из образа (зашивается при сборке, чтобы откат не
рапортовал номер нового коммита), приложения — `aristea-mobile@<версия>+<sha>`.

⚠️ **Правка `05-update.sh` действует только со СЛЕДУЮЩЕГО деплоя.** Скрипт
обновляет сам себя через temp-файл и atomic `mv`, а работающий bash дочитывает
старый inode. Поэтому деплой `1bd327b`, который принёс `export GIT_SHA`, собрал
образ ещё без него: события бэкенда идут без `release`, пока не пройдёт
следующий деплой. Это не поломка, и чинить её не нужно.

---

История: Объём разбора: `_volume_plan`, замеренные константы (70 слов в абзаце, +20 на коротких секциях), зависимость объёма от карты, потолок токенов, удалённый пейволл усечения — [docs/HISTORY-interpretation.md](docs/HISTORY-interpretation.md).

---

## Чат Аристеи: четыре ограничения, которые НЕ дефекты

Чат в приложении принят на устройстве 10.09.2026 (APK `34397967028`, ветки
`rag-chat-mobile` и `chat-fixes` в `main`). Ниже — то, что при следующем
заходе выглядит как недоделка и чинить это не надо, не подняв вопрос отдельно.
Всё проверено исполнением 09–10.09.2026, а не выведено из чтения.

### 1. История диалога — 10 реплик и 6 часов, и обрезка идёт при ЗАПИСИ

`MAX_HISTORY = 10`, `HISTORY_TTL = 6 * 3600` (`interpretation/rag_router.py`).
Это пять обменов, не десять.

⚠️ **Обрезают ДВА места, и последствия у них разные.** `_load_history` режет
при чтении, а `_save_history` — при записи (`history[-MAX_HISTORY:]`). Значит
старые реплики не фильтруются на выдаче, а **физически стираются из Redis**:
снять ограничение в ручке чтения бесполезно, вернутся всё те же десять.
Замерено на 12 вопросах подряд: в Redis 10 реплик, ручка отдала 10, первого
вопроса нет нигде.

Поднимать нельзя молча: история подмешивается в промпт при КАЖДОМ вопросе, то
есть длиннее история — длиннее промпт на каждом запросе, и платится это
токенами на всех репликах сразу, а не один раз. Симптом, с которого начнётся
разбор, звучит как «история не отматывается к началу разговора» — это оно.

### 2. Память Аристеи не сворачивает отбой по чужой теме

Ветка `topic != "astrology"` отдаёт `StreamingResponse` **без** `background=`
— намеренно. Свернуть отказ значило бы записать в долговременную память, что
человек интересовался курсом доллара, и потом подмешивать этот факт в каждый
запрос про его карту.

В историю диалога (Redis) отказ при этом ПИШЕТСЯ, и это не противоречие:
там нужен связный разговор, чтобы модель не повторяла один и тот же отказ.
Разные хранилища — разные задачи. Закреплено комментарием в коде и тестом
`TestOffTopicDoesNotTouchMemory` (`test_rag_memory.py`).

⚠️ Разведка 09.09.2026 нашла эту ветку пятой в списке «где свёртка
пропускается молча» и ответить, задумано так или забыто, не смогла — ради
этого запись и сделана.

### 3. Классификатор темы видит ТОЛЬКО последний вопрос

`_classify_topic(question)` получает одну строку. Ни истории, ни предыдущих
реплик, ни того, что человек уже полчаса говорит про свою карту. Поэтому
«дом», написанное в продолжение разговора, для него неотличимо от «дом»
первым сообщением.

Отсюда лечение 09.09.2026 — не контекст, а прямые правила в промпте: «одно
слово без пояснения = просьба рассказать по карте», «есть астрологическое
прочтение → astrology», «сомневаешься → astrology», плюс «дом», «знак»,
«отношения», «деньги» названы примерами. Незнакомая метка тоже трактуется как
`astrology` (`_label_from_reply`): цена ошибки несимметрична — лишний вопрос к
модели стоит копейки, отбитый настоящий вопрос про карту стоит ушедшего
человека.

⚠️ **Передать классификатору историю — не бесплатная правка, а смена
контура:** это второй промпт, который начнёт расти с разговором, и второй
источник расхода на каждой реплике. Отдельное решение, не «улучшение по
дороге».

### 4. Недоступность модели приходит обычным ответом Аристеи

Ветка `httpx.HTTPStatusError` в `_sse_generator` шлёт «Извините, AI-сервис
временно недоступен…» кадром `{"text": …}` — структурно тем же, чем идёт
нормальный ответ, без поля `error`. Отличить на клиенте нечем: единственный
способ — сравнить строку, а привязываться к формулировкам сервера в этом
проекте запрещено отдельно (`transitInterpretRules.js`), их правят.

Практическое следствие: при отказе DeepSeek человек видит эту фразу пузырьком
**от Аристеи**, как будто она так ответила. Единственный наблюдаемый след —
фраза НЕ попадает в серверную историю (`_persist_turn` в той ветке не
зовётся), то есть после переоткрытия шторки её не будет. В момент показа —
неотличимо.

Чинится только на сервере: отдать этот случай кадром с `error`, как три
остальных (`timeout`, `empty_response`, `stream_failed`). Закреплено тестом в
`chatRules.test.js` как ЗАКРЕПЛЕНИЕ ограничения, а не проверка правильности.

---

История: Кэш-хит обязан отдаваться порциями: три пути отдачи, `replay_as_stream`, переписанный `sectionStream.js` вместо `flushBuffer` — [docs/HISTORY-interpretation.md](docs/HISTORY-interpretation.md).

---

История: `cache_key` в коде — не тот ключ, что в Redis (префикс инстанса, чистка по `interp:*`) — [docs/HISTORY-interpretation.md](docs/HISTORY-interpretation.md).

---

История: Чеки по 54-ФЗ формируются вручную, через ЮKassa автоматизировать нельзя — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

---

История: Вебхук ЮKassa держится на определении IP: литерал `127.0.0.1` в `proxy_pass`, `TRUSTED_PROXY_IPS`, `_notify_ip_reject` — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

---

История: CSP в режиме наблюдения (Report-Only) и что проверить при включении боевой — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

---

История: `/chart/save-anonymous` создаёт дубль, а не привязывает карту (и что из этого следует для метрик) — [docs/HISTORY-backend.md](docs/HISTORY-backend.md).

---

История: Мониторинг очереди Celery: два контура, сторож вне очереди, недоставляемые systemd-юниты — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

---

## Пароли: обрезка до 72 байт в hash И в verify — удалять нельзя

passlib убран 22.09.2026, `backend/auth/passwords.py` зовёт `bcrypt.hashpw` /
`bcrypt.checkpw` напрямую (`bcrypt 5.x`, rounds 12 — ровно те, что ставил
passlib по умолчанию; формат хешей прежний `$2b$12$…`, перешифровки не было).

⚠️ **Оба вызова получают `plain.encode("utf-8")[:72]`, и срез выглядит лишним
ровно до первой жалобы на вход.** passlib обрезал секрет молча, bcrypt 5.x на
секрете длиннее 72 байт кидает `ValueError`. Из этого два следствия, и второе
важнее:

1. **Хеши длинных паролей уже лежат в базе** — запрет появился только
   18.07.2026 (`6d1b4b8`, `validate_password`), а посчитаны они ОТ ОБРЕЗАННОГО
   секрета. Без среза в `verify` такой человек получит **500 вместо входа**:
   `/auth/login` пароль не валидирует вовсе (`LoginRequest` в `schemas.py` —
   без валидаторов, в отличие от регистрации и сброса), длина долетает до
   хешера как есть. Кириллицей это начинается с 36 символов.
2. Без среза в `hash` новый хеш разошёлся бы с тем, что проверяет `verify`.

`verify_password` на пустом/`NULL` хеше (аккаунты Google заводятся с
`hashed_password=None`) и на битой строке возвращает `False`, а не исключение —
иначе такой аккаунт отдавал бы 500 вместо «неверный пароль».

Закреплено `test_password_bcrypt_migration.py`: в нём строками записаны хеши,
созданные НАСТОЯЩИМ passlib 1.7.4 (после удаления зависимости получить их
больше нечем), в том числе от пароля в 100 байт. Обратный прогон со снятым
срезом — три кейса падают.

---

## Письма онбординга и после покупки — один путь, журнал в БД, без countdown

23.09.2026 письмо «Ваши тарифы на Aristea Timeline» (retention day14) пришло
владельцу ~10 раз в одну минуту. Ниже — механизм и то, что его заменило;
код — `backend/lifecycle_emails.py`, тесты — `test_lifecycle_emails.py`.

### Почему пачка: countdown дольше visibility timeout Redis

Письмо ставилось `apply_async(countdown=14 суток)`. Задача с ETA держится
воркером неподтверждённой до срока, а не подтверждённая за visibility timeout
Redis-брокера (умолчание **1 час**) выдаётся заново — каждый час, «again and
again in a loop» (docs.celeryq.dev → Redis → Visibility timeout). У всех копий
один ETA, поэтому они срабатывают разом; тёплый рестарт воркера (деплой)
копии сбрасывает, то есть число писем ≈ часам с последнего деплоя.

⚠️ **Поэтому в бэкенде нет и не должно быть `countdown`/`eta` длиннее часа** —
держит `test_no_long_countdown_in_backend` (рядом проверка, что сканер ловит
ровно ту строку, что была 23.09, — иначе тест был бы зелёным впустую).
Visibility timeout возвращён к умолчанию: поднять его до 31 суток (временная
правка `72d73da`) значило бы, что задача, чей воркер убит жёстко, повторится
через 31 сутки, — это лечение симптома ценой другого дефекта.

### Один путь

До 23.09.2026 day2/day7 слали ДВА пути: цепочка Celery от первой карты и ручка
`POST /internal/onboarding-emails` (systemd-таймер) от регистрации — в разные
дни. Теперь:

* day2/day7/day14, `lite_day14`, `pro_day30` — только Beat-задача
  `tasks.send_lifecycle_emails`, раз в час 06:15–18:15 UTC (09:15–21:15 МСК,
  решение владельца). Ежечасно, а не раз в сутки: деплой в 06:15 не стоит
  суток задержки.
* приветствие после оплаты — `tasks.send_purchase_welcome_task` сразу после
  активации, по id платежа (ждать до утра письма «оплата прошла» нельзя).
* что эти письма зовёт только `lifecycle_emails.py`, а ручки
  `onboarding-emails` в приложении нет, держит тест. Ручку и таймер не
  возвращать.

### Точка отсчёта онбординга — регистрация, а не первая карта

Решение владельца 23.09.2026. `users.created_at` не меняется; дата первой карты
нигде не хранится как факт, а минимум `natal_charts.created_at` сдвигается,
когда человек удаляет первую карту (слотовая модель это поощряет) — день
письма поехал бы вслед. Условие «есть карта» для day2/day7 проверяется в
момент отправки: без карты письма нет, но и строки в журнале нет — построит
карту внутри окна, письмо придёт.

Письма после покупки считаются от оплаты с `payment_events.starts_chain`
(оплата начала тариф, а не продлила). Отдельная колонка нужна потому, что в
той же таблице лежат продления, возвраты (`refund:…`, amount < 0) и непригодные
платежи (period NULL) — по остальным полям «покупку» однозначно не выбрать.

### Выборка «пора, не опоздали, нет в журнале», а не «ровно сегодня»

| Письмо | Пора | Последний день | Условие |
|---|---|---|---|
| day2 | +2 д | +5 д | есть карта |
| day7 | +7 д | +11 д | free, есть карта |
| day14 | +14 д | +21 д | free |
| lite_day14 | +14 д от оплаты | +21 д | тариф всё ещё lite |
| pro_day30 | +30 д от оплаты | +37 д | тариф всё ещё pro |

Окна не пересекаются намеренно: после долгого простоя человек не получит два
письма онбординга в один день. Пропущенный прогон догоняется следующим;
зарегистрированный полгода назад day2 не получит.

### Журнал `email_sent_log` (миграция 054)

Уникальный `(user_id, kind, ref)`: `ref = ""` у онбординга (раз в жизни),
`ref = id payment_events` у писем после покупки (раз на покупку).
**Порядок в `send_once` несущий:** INSERT (flush) → отправка → commit, при
неудаче rollback. Второй параллельный прогон на Postgres ждёт на уникальном
индексе и получает IntegrityError; отдельный SELECT «не отправляли ли»
пропустил бы оба. Остаточный риск один — письмо ушло, commit упал, будет
повтор; обратный порядок менял бы его на тихую потерю.

⚠️ **Отправка идёт внутри открытой транзакции, поэтому у запроса к Resend
общий потолок** (`RESEND_TOTAL_TIMEOUT_SEC`, `asyncio.wait_for` поверх
`httpx.Timeout`): `httpx.Timeout` ограничивает каждую фазу отдельно, а не
сумму, и медленный ответ держал бы блокировку сколь угодно долго.

⚠️ **`sent_at = NULL` — строка восстановлена миграцией**, а не отправка.
Данных о фактически ушедших письмах до 23.09 нет нигде (Celery писал только
ошибки, docker-логи ротируются), поэтому 054 записала «отправленным» всё, чей
срок уже наступил, — иначе после выката ушла бы волна старых писем.
`starts_chain` старым платежам — первый УСПЕШНЫЙ платёж пользователя на тарифе.

### Заглушки снятых задач — до 24.10.2026

12 старых имён (`tasks.send_retention_day*`, `tasks.schedule_*_emails`,
`tasks.send_*_welcome_task`, `tasks.send_lite_day14_task`,
`tasks.send_pro_day30_task`) зарегистрированы и не делают ничего
(`_RETIRED_TASK_NAMES` в `tasks.py`): в Redis лежат их сообщения с ETA до 30
суток. Удали имена — воркер упадёт на незарегистрированной задаче; оставь
рабочими — письмо уйдёт мимо журнала. Снять после 24.10.2026 (TASKS.md).

---

## Время и пояса (проверка 24.09.2026)

* **Пояс рождения** — `timezonefinder` по координатам → `pytz` с историей
  tzdata (`geo.resolve_utc_datetime`). Сверено с Википедией на 9 случаях
  (`test_birth_timezone.py`): декретное время, 2011–2014, Самара 2010, Калининград.
* **Смещение видно и правится.** Ответ карты отдаёт `utc_offset_minutes` и
  `utc_offset_source` (`place`/`manual`); запрос расчёта принимает
  `utc_offset_minutes`, и оно главнее пояса места. Число не хранится —
  выводится из `utc_datetime` (`applied_utc_offset_minutes`), хранится только
  флаг `utc_offset_manual` (миграция 055). Правки у существующей карты нет:
  исправление — новая карта с ручным смещением.
* ⚠️ **Час перевода стрелок назад до 24.09.2026 был тупиком:** кнопки выбора
  слали подпись «02:30 MSD» в поле времени (422), веб читал несуществующее
  `err.data` и показывал «[object Object]», `save-anonymous` отдавал 500.
  Теперь 400 несёт `offsets`, выбор уходит в `utc_offset_minutes`.
* ⚠️ **«Сейчас» на клиенте — `serverNow()` (`lib/serverClock.js`)**, сдвиг
  берётся из `iat` свежего токена. Без этого телефон, спешащий больше ~13
  минут, крутил обновление сессии по кругу (access живёт 15 минут), а
  «сегодня» в ленте было датой телефона. Пояс — телефона.
* ⚠️ **Сервер живёт в UTC: `date.today()` в ручке, где рядом есть «сейчас»
  по поясу карты, — ошибка.** В Москве 00:00–03:00 это вчера. Лента
  исправлена (`feed/router.py`); лимиты «в месяц» считаются по UTC
  намеренно (`_current_period_ym`), новый месяц по Москве — с 03:00 1-го.
* Уведомление о фазе сравнивает МЕСТНУЮ дату фазы (`_phases_on_local_date`,
  `push/cron.py`); раньше UTC-дату — фаза в ночь на 1-е терялась.
* Смена года и 29.02 — `test_year_boundary.py` и `mobile/lib/yearBoundary.test.js`.

## Swiss Ephemeris — синхронный, не вызывать напрямую из async-хендлеров

`calculate_full_chart`, `calculate_transits`, `compute_exact_facts` и всё,
что в итоге вызывает `pyswisseph` (`backend/ephemeris/calculator.py`,
`backend/transit/engine.py`, `backend/ephemeris/synastry.py`,
`backend/ephemeris/solar_return.py`, `backend/calendar/lunar_engine.py`,
`backend/transit/house_passages.py`) — обычные `def`, не `async def`.
`api` — один процесс (`start.sh`, без `--workers`), один asyncio event loop
на все запросы разом. Вызов такой функции напрямую внутри `async def`
блокирует ЭТОТ event loop на всё время расчёта — не только для вызвавшего
запроса, для вообще всех, кто в этот момент обращается к серверу (чат,
чужие интерпретации, health-check).

Обнаружено 20.08.2026 сразу в трёх независимых местах (`main.py:calculate_chart`,
`crm/router.py:_geocode_and_build_chart`, `interpretation/rag.py:build_transits_block`
из чата) — из кода это не очевидно, ошибка тихая (не падает, просто копится
задержка под нагрузкой), поэтому фиксируется здесь явно.

**Правило**: в любом `async def`-обработчике (FastAPI route, `_scheduler_loop`
в `main.py`) вызов такой функции — только через
`await asyncio.to_thread(calculate_full_chart, ...)` (или аналог), не напрямую.
Внутри Celery-задач (`@celery_app.task`, `backend/tasks.py`, роли `worker`/`beat`)
прямой синхронный вызов — нормально: там нет общего event loop, блокировать
нечего, воркер и так один запрос за раз обрабатывает.

---

## Деплой

Всё на одном сервере: **Timeweb VPS**.

```
push в ЛЮБУЮ ветку (кроме dependabot/** и claude/**)
  └── GitHub Actions: pytest (backend) + vite build (frontend) — только тесты

Actions → CI/CD → Run workflow (ветка main)   ← РУЧНОЙ шаг, деплой этим и запускается
  └── deploy job (SSH на VPS) → ./05-update.sh
        git pull → dump БД → docker compose up -d --no-deps api bot worker beat
        → alembic upgrade head → /health check → rollback при ошибке
        → фронтенд пересобирается тем же скриптом, если менялись
          frontend/ или deploy/opt-astro/nginx/
```

⚠️ **`04-frontend-deploy.sh` руками звать не нужно** — таблица «Стек» до
14.09.2026 утверждала обратное («фронтенд — вручную скриптом»), и это было
неправдой. Проверено обоими исходами на живых прогонах: `34824043800` —
`с последнего успешного деплоя фронтенда (c40bb2b) затронут frontend/` →
`==> Собираю фронтенд`; `34827975111` (только бэкенд) —
`Фронтенд и конфиг nginx не менялись — пересборку пропускаю`. Решение
принимает сам `05-update.sh`, второй запуск ничего не добавит.

История: Почему список веток в `ci.yml` перевёрнут на `branches-ignore` — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

⚠️ **Отказ этого класса не виден ниоткуда: ошибки не возникает, просто нет
прогонов.** Заметить можно только глядя на список триггеров, а не на красноту
в Actions. `mobile-build.yml` намеренно оставлен на списке `[mobile]` (решение
владельца 09.09.2026): там 30-минутная сборка Gradle, гонять её на каждый пуш
в любую ветку незачем, штатный путь — `gh workflow run "Mobile APK" --ref
<ветка>`.

История: `github.event_name` в `concurrency.group` — почему ручной деплой вставал в очередь за тестами — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

⚠️ **`deploy` не запускается пушем в main — только вручную.** Условие джоба —
`github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'`
(`.github/workflows/ci.yml`). Раньше было `github.event_name == 'push'`, а
единственной защитой от немедленного выезда на прод считалось окружение
`production` с Required reviewers. Такой защиты нет и включить её нельзя:
раздел Deployment protection rules недоступен на текущем тарифном плане
GitHub — ограничение плана, а не настройки окружения (API отдаёт
`"protection_rules": []`). То есть любой push в main всё это время уезжал на
прод сразу и без подтверждения; после перевода на `workflow_dispatch` push
только прогоняет тесты, а на прод ничего не увозит, пока кто-то не запустит
джоб руками (Actions → CI/CD → Run workflow, ветка `main`) — вручную из
интерфейса или `gh workflow run "CI/CD" --ref main`.
Окружение `production` оставлено ради журнала деплоев и области для
секретов, не ради защиты.

История: Три слоя доставки (что доезжает всегда, что по флагу, самосинхронизация скриптов) — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

История: Метка `.frontend-deployed-rev` и почему флаг фронтенда считается не по `git pull` — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

---

## Индексация: пререндер, канонический хост, честный 404

Сделано 14.09.2026 после разведки по живому домену. Три вещи, каждая из
которых при следующем заходе выглядит как странность и каждую нельзя трогать,
не прочитав почему.

### Список маршрутов один — `frontend/src/routes.js`

Из него кормятся ЧЕТЫРЕ потребителя: sitemap (`generate-sitemap.js`), пререндер
(`scripts/prerender.mjs`), `map` для nginx (`scripts/generate-nginx-routes.mjs`)
и метаданные страниц в `App.jsx` (`useOGMeta` → `seoForPath`). Пятым остаётся
сам список `<Route>` в `App.jsx` — он пока свой, и его держит тест-сторож.

⚠️ **Метаданные переехали сюда 14.09.2026, и до переезда была тихая поломка.**
`useOGMeta` держала собственные литералы на три группы маршрутов и
ПЕРЕЗАПИСЫВАЛА ими то, что записал пререндер, — как только выполнялся JS.
Измерено сравнением документа без JS и после: на «/» и «/zodiac/*» расходились
`title` и `og:*`; на `/pricing` и `/terms` не расходилось ничего, потому что
веток для них там просто не было. **Содержимое страниц при этом совпадало
везде — расходились только метаданные, поэтому проверкой глазами это не
ловилось вовсе.** Google исполняет JS, значит в индекс шёл вариант из
`App.jsx`, а не объявленный в `routes.js`.

Замер после правки: на всех 18 страницах `title`, `description`, `og:*` и
`canonical` переживают выполнение JS без изменений; переходы внутри
приложения тоже обновляют `<head>` из этого же источника. Возврат литералов
запрещён тестом в `routes.test.js` (обратный прогон: падает).

Поле `kind` решает всё: `public` — пререндерится, идёт в sitemap, отдаётся
готовым файлом; `app` — отдаётся шеллом. **Чего в файле нет, того для nginx не
существует: такой адрес получает 404.**

⚠️ **Поэтому маршрут, добавленный в `App.jsx` и забытый в `routes.js`, — это
не «страница без индексации», а страница, которая не откроется.** Цена
расхождения выросла, и держит её `src/routes.test.js`: он парсит `<Route
path=` из `App.jsx` и требует совпадения множеств. Тест временный — снимать
его можно только вместе с переводом `App.jsx` на импорт из `routes.js`
(правка роутера на 14.09.2026 не согласована с владельцем), не раньше.

⚠️ Рядом в том же тесте стоит проверка, что из `App.jsx` вообще разобрано
больше двадцати путей. Без неё регулярка, переставшая что-либо находить,
сделала бы основную проверку зелёной на двух пустых множествах.

### Пререндер живёт на сервере, и перенести его в CI нельзя отдельно

`npm run build` последним шагом поднимает headless-хром и снимает готовый HTML
с 18 публичных маршрутов. На VPS стоит snap-сборка chromium
(`/snap/bin/chromium`, 152.0.7977.64 на 14.09.2026), путь к ней находит
`04-frontend-deploy.sh` и передаёт через `PUPPETEER_EXECUTABLE_PATH`.

⚠️ **Шум в stderr при запуске chromium — норма, а не отказ.** Snap-сборка
сыплет жалобами AppArmor и DBus (`ListActivatableNames`, `UPower`) и строками
`mojo … rejected by interface`. HTML при этом идёт в stdout и корректен
(проверено на сервере исполнением). Признак отказа — код возврата и пустой
результат. Ни `prerender.mjs`, ни `04-frontend-deploy.sh` stderr не
перехватывают; puppeteer вычитывает оттуда только строку `DevTools listening
on ws://…`. **Проверка «stderr пустой», добавленная сюда из лучших
побуждений, уронит деплой на полностью здоровой сборке.**

⚠️ **`--no-sandbox` убирать нельзя.** Snap-сборка без него в headless не
стартует вовсе — её песочница конфликтует с confinement'ом snap. Флаг
безопасен по контексту: браузер открывает ровно один адрес, `http://127.0.0.1`
с нашей же сборкой, без пользовательского ввода и без выхода в сеть.

⚠️ **Почему не в CI, где браузер уже есть.** В снятый HTML запекается
`<script src="/assets/index-<хеш>.js">`, а хеш рождается в конкретной сборке.
Снимок с чужой сборки сошлётся на несуществующий файл: страница отдастся,
бандл не загрузится, приложение не смонтируется вовсе. Сборка фронтенда идёт
на VPS (`04-frontend-deploy.sh` делает там `npm ci && npm run build`), значит и
браузер нужен там. Перенести пререндер в CI можно ТОЛЬКО вместе со сборкой —
это переделка схемы доставки, а не перестановка шага.

⚠️ **`dist/app.html` — не мусор и не дубль `index.html`.** После пререндера
`dist/index.html` это ЛЕНДИНГ, со своим `<link rel="canonical">` на «/».
Ответив им на `/profile`, сервер сообщил бы поисковику, что `/profile` — это
на самом деле главная. `app.html` — чистый шелл, снятый ДО пререндера, он про
себя ничего не утверждает; его и отдаёт `location @spa`.

⚠️ **Скрипт обязан ронять сборку, и проверено, что роняет.** Прогон без
браузера даёт код 1 у `npm run build`, подоболочка в `04-frontend-deploy.sh`
умирает по `set -e`, до шага публикации `dist` управление не доходит вовсе —
на сервере остаётся прежняя сборка, а `05-update.sh` не пишет метку
`.frontend-deployed-rev`, то есть следующий деплой повторит фронтенд. Отказ на
ОДНОЙ странице из восемнадцати ведёт себя так же: цикл не обёрнут в try, первое
же исключение выносит скрипт целиком. Семнадцать готовых и одна пустая уехать
не могут.

#### Сверка с эталоном 404 — НЕ снимать при уборке

Проверок в скрипте четыре, и все про факт, а не про код возврата: текста в
`#root` больше 80 символов; ни один элемент с текстом не остался прозрачным;
список адресов не пуст; и — та, ради которой написан этот раздел — **текст
страницы не совпадает с текстом экрана «страница не найдена»**.

Эталон снимается ПЕРВЫМ, до цикла по маршрутам, и потом сравнивается с каждой
страницей.

⚠️ **Со стороны это выглядит избыточно — рядом уже стоит порог по длине
текста, и кажется, что он то же самое и ловит. Не ловит, и это измерено.**
Обратный прогон 14.09.2026: в `routes.js` вставлен маршрут, которого нет в
`App.jsx`. Роутер отдал на него catch-all `NotFoundPage` — а это **378
символов текста**, порог в 80 пройден с четырёхкратным запасом. Прогон
завершился кодом 0 и отчитался «✅ пререндер: 19 страниц». Страница
записалась в `dist` как нормальная, со своим `title` и собственным
`canonical`, попала бы в sitemap и уехала бы на прод.

**Что сломается, если снять: в выдачу уйдёт экран «страница не найдена» под
видом тарифов или оферты.** Это хуже пустой страницы — пустую видно сразу, а
эта выглядит рабочей и со стороны Google, и при беглой проверке глазами.
Заметить можно только по тому, что в выдаче у страницы не тот заголовок.

⚠️ Сверяется ТЕКСТ, отрендеренный браузером, а не формулировка из
`NotFoundPage.jsx`. Привязка к фразе сломалась бы при первой же правке
текста — причём сломалась бы в сторону «проверка молча перестала ловить», а
не в сторону падения.

Порог в 80 символов при этом тоже нужен и не заменяется сверкой: он ловит
другой отказ — страница не отрисовалась вовсе. Две проверки, два разных
отказа, ни одна не покрывает вторую.

Проверка прозрачности — тоже не перестраховка: секции
лендинга появляются по `whileInView` с вариантом `hidden: { opacity: 0 }`, и
первый же прогон снял главную, где видна только первая ширма. Лечится
прокруткой страницы перед снимком (`revealByScrolling`), а НЕ эмуляцией
`prefers-reduced-motion`: сокращённый вариант убирает сдвиг по `y`, а нулевую
непрозрачность оставляет.

### `/api/` исключён из редиректа `www` → apex НАВСЕГДА

Канонический хост — `https://aristeatime.ru`. Три остальных имени
(`www.aristeatime.ru`, `astreatime.ru`, `www.astreatime.ru`) отдают 301 на него
отдельным `server`-блоком в `astreatime.conf`.

⚠️ **Но `/api/`, `/share/` и `/health` на этих хостах проксируются как раньше,
без редиректа.** Мобильное приложение прибито к абсолютному адресу
`https://www.aristeatime.ru/api/v1` — он попадал в бандл APK на этапе сборки
(`frontend/.env.mobile`). Коды 301 и 302 по историческому поведению клиентов
превращают POST в GET и теряют тело: пусти мы `/api/` через редирект, у
КАЖДОГО установленного приложения разом перестали бы работать вход,
регистрация, построение карты и оплата — без единой ошибки в логах.
Обновить уже установленные APK задним числом нельзя.

`.env.mobile` с 14.09.2026 указывает на apex, но это только про БУДУЩИЕ сборки.

⚠️ **Эта правка сломала сборку APK и пролежала незамеченной.** Проверка
«в бандле абсолютный URL API» в `mobile-build.yml` держала адрес ЛИТЕРАЛОМ и
осталась на `www`, поэтому первый же прогон после `9f29776` упал — а отказ
назывался «`.env.mobile` не подхватился», то есть указывал не туда.
Заметить было неоткуда: `mobile-build.yml` ходит только по требованию и на пуш
в ветку `mobile`, так что между правкой и падением прошло несколько коммитов.
С 14.09.2026 проверка читает `VITE_API_URL` из самого `.env.mobile` — литерала
там больше нет.
Снять исключение можно, когда ни одного APK со старым адресом не останется в
природе, то есть практически никогда. **Не убирать как непоследовательность.**

Проксирующие блоки вынесены в `snippets/backend-locations.conf` и
подключаются `include`-ом в оба `server`-блока: в nginx `location` между
`server` не наследуются, и копия из 219 строк разошлась бы при первой же
правке — причём дефект был бы виден только на старых хостах, то есть только у
пользователей мобильного приложения.

### 404 отдаётся по сгенерированному списку маршрутов

`location @spa` смотрит на `$is_app_route` — переменную из `map`, который
лежит в `/etc/nginx/conf.d/10-astro-routes.conf`. **Файл генерируется сборкой**
из `routes.js`, в репозитории его нет и быть не должно: каталог `app/` на
сервере — рабочая копия git, и файл, который сборка переписывает внутри неё,
ломает следующий `git pull` (раздел «Файлы, которые портятся сами» ниже).
Доставляет его `04-frontend-deploy.sh` отдельным шагом.

⚠️ В `conf.d`, а не в `snippets`, потому что `map` объявляется только на уровне
http, а сниппеты подключаются внутрь `server{}`.

⚠️ `if` внутри `location` — известная ловушка nginx, но `return` входит в те
два случая, которые официально безопасны. Обойтись без него нельзя:
последним аргументом `try_files` код `=404` из переменной не разворачивается.

⚠️ Знаки зодиака перечислены в `map` поимённо, а не как `[^/]+`. Иначе
`/zodiac/абракадабра` вернул бы 200 со страницей «Знак не найден» — тот же
софт-404, ради устранения которого всё делалось.

### `lastmod` в sitemap — дата коммита, а не времени сборки

`generate-sitemap.js` берёт `git log -1 --format=%cs` по исходнику страницы.
Прежняя версия ставила всем адресам дату сборки, то есть сообщала Google об
изменении всех страниц разом при каждой пересборке фронтенда.

⚠️ mtime файла для этого не годится: на сервере сборка идёт из рабочей копии
git, и mtime там — время, когда `git pull` записал файл. У не менявшегося в
этом pull файла останется дата первого клона. mtime описывает историю ДЕПЛОЯ,
а не правок страницы.

⚠️ Если git недоступен или по файлу нет истории, `lastmod` не выводится вовсе.
Решение владельца: неверная дата хуже отсутствующей.

---

## nginx: конфиг на сервере совпадает с репозиторием — с 31.08.2026, не навсегда

История: Как конфиг nginx разошёлся с репозиторием и чем это закрыли 31.08.2026 — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

⚠️ **Правило остаётся в силе, расхождение закрыто на сегодня, а не отменено
навсегда: не описывать поведение прода по файлам из `deploy/` без проверки.**
Они описывают желаемое состояние, которое механизм доставки может снова не
довезти — новым разрывом синхронизации, ручной правкой на сервере поверх
задеплоенного, или третьим писателем в тот же каталог (см. `08-setup-automation.sh`
ниже). Утверждение вида «nginx проксирует такой-то путь» требует **свежего**
вывода `nginx -T` с сервера, а не grep по репозиторию и не ссылки на этот
раздел без даты. В FIXES_4 такое утверждение было подано как проверенный факт
и оказалось неверным.

**Чем проверяется совпадение — `nginx -T`, конкретно:**

```bash
sudo nginx -T 2>/dev/null | grep -c 'location'
sudo nginx -T 2>/dev/null | grep -A3 'location /share/'
sudo nginx -T 2>/dev/null | grep -E 'csp\.conf|security-headers\.conf|00-astro-hardening'
sudo nginx -T 2>/dev/null | grep -c 'is_app_route'
```

Смотреть на: число `location`-блоков (с 14.09.2026 конфиг сайта — 225 строк
плюс 239 в `snippets/backend-locations.conf`, который подключён в ДВА
server-блока, так что в выводе `nginx -T` проксирующие location встретятся
дважды — это норма, а не дубль); переменную `is_app_route` (её задаёт
сгенерированный `conf.d/10-astro-routes.conf`; ноль совпадений значит, что файл
не доехал, и тогда `nginx -t` уже не прошёл бы); блок `location /share/` (добавлен
владельцем вручную 30.08.2026, до 31.08.2026 не проксировался, уходил в
SPA-фолбэк); include’ы `csp.conf`, `security-headers.conf` и
`00-astro-hardening.conf` (их отсутствие в выводе — признак, что снова
разъехалось). Отсутствие любого из них в `nginx -T` — не «ещё не проверяли», а
сигнал регресса, требующий разбора по той же схеме, что и в этот раз.

**Как файлы попадают на сервер.** Workflow (`ci.yml`, job `deploy`) не
копирует ничего: только `ssh` и `cd /opt/astro && ./05-update.sh`.
`/opt/astro` — не репозиторий; репозиторий это `/opt/astro/app`, туда и
делается `git pull`. Дальше:

| Кто | Что копирует | Куда |
|---|---|---|
| `05-update.sh` | `05-update.sh`, `04-frontend-deploy.sh`, `docker-compose.yml`, `07-backup-cron.sh` | `/opt/astro/` |
| `04-frontend-deploy.sh` | `nginx/snippets/*.conf` | `/etc/nginx/snippets/` |
| — то же — | `nginx/conf.d/00-astro-hardening.conf` | `/etc/nginx/conf.d/` |
| — то же — | `frontend/nginx-routes.conf` (**генерируется сборкой**) | `/etc/nginx/conf.d/10-astro-routes.conf` |
| — то же — | `nginx/astreatime.conf` | `/etc/nginx/sites-available/` |
| — то же — | собранный `frontend/dist/` | `/opt/astro/frontend/dist` |

⚠️ Строка про `nginx-routes.conf` — единственная в таблице, где источник не
лежит в репозитории: файл рождается на сборке из `frontend/src/routes.js`
(раздел «Индексация» выше). В режиме `--nginx-check` сборки не было и файла
может не быть — тогда скрипт оставляет на сервере прежний. Если же его нет и
там, `nginx -t` упадёт на неразрешённой `$is_app_route`, и откат отработает
штатно: это правильный исход, конфиг без списка маршрутов отдавал бы 404 на
каждый адрес приложения.

Второй, независимый писатель в `/etc/nginx/snippets/` — `08-setup-automation.sh`
(строка 177, ручной одноразовый установщик панели `status.astreatime.ru`). Два
скрипта пишут в один каталог, ни один не знает о другом; трогать это не стали.
`nginx/status.astreatime.conf` не доставляет никто из автоматики — только он же,
причём по относительным путям, которые из `/opt/astro` не разрешаются.

`04-frontend-deploy.sh` запускается из `05-update.sh` **только если менялись
`frontend/` или `deploy/opt-astro/nginx/`** (или при `--frontend-only`).
Как именно считается «менялись» — в разделе «Метка `.frontend-deployed-rev`»
([docs/HISTORY-deploy.md](docs/HISTORY-deploy.md)).

Практическое следствие: **файл nginx в репозитории не декоративный.**
Первый же деплой с изменением фронтенда перезапишет живой конфиг целиком,
безусловным `cp`, без резервной копии. Ручные правки на сервере переживут
ровно до этого момента.

История: Второе следствие: копирование идёт до `nginx -t`, и упавшая проверка оставляет на диске нерабочий конфиг — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

---

## Деплой — обязательная процедура

### Разделение ответственности

Claude Code **не имеет SSH-доступа к серверу**. Всё, что делается на VPS,
выполняет владелец руками. Поэтому Claude Code обязан:

- никогда не предлагать редактировать файлы напрямую на сервере;
- любую правку вносить только в репозиторий и доставлять через `git push`;
- если нужно действие на сервере — выдать **готовый блок команд одним куском**,
  без пояснений между строками, чтобы владелец вставил их одной вставкой;
- в командах не использовать `sed` с текстом, содержащим `|`, `&`, кавычки или
  кириллицу — только `python3 - <<'PYEOF'` с точным поиском строки и проверкой
  `assert count == 1` (обязательные детали — в разделе «Правка файлов скриптом»
  ниже).

### Правка файлов скриптом: две проверки, и одна другую НЕ заменяет

Заметка из практики 23.08.2026, оба случая произошли в один день.

**1. `assert count == 1` не доказывает, что замена применилась.** Он проверяет
только, что искомая подстрока в файле есть. Если `old` собран из двух кусков,
между которыми в файле стоит что-то ещё (перенос строки, продолжение с `\`),
то `t.replace(old, new)` молча ничего не сделает, а скрипт отрапортует успех.
Так тест `test_foreign_currency` остался с прежним `assert 400`, и обнаружилось
это только по упавшему прогону.

**Правило: после каждой замены — `assert t2 != t`.** Обе проверки обязательны:
`count == 1` ловит неоднозначный или отсутствующий якорь, `t2 != t` ловит
несработавшую замену. Ни одна из них не покрывает вторую.

```python
assert t.count(old) == 1, f"якорь: {t.count(old)}"
t2 = t.replace(old, new)
assert t2 != t, "замена не применилась"
p.write_text(t2, encoding="utf-8")
```

**2. В heredoc этого окружения съедаются обратные слеши.** `"\\\n"` внутри
python-строки доезжает как литеральные символы `\` и `n`, а не как
«backslash + перевод строки». Один раз это записало literal `\n` в середину
`deploy/opt-astro/05-update.sh` — файл повышенного риска, ошибка ловится только
`bash -n`.

Отсюда: **не писать обратные слеши в python-коде внутри heredoc.** Нужен
перевод строки или слеш в результате — собирать через `chr(10)` / `chr(92)`
или короткими якорями без продолжений строк. После правки любого shell-скрипта
— `bash -n <файл>` перед коммитом.

### Железное правило: сервер не редактируем

Каталог `/opt/astro/app` — это рабочая копия git. Любая ручная правка в нём
блокирует следующий `git pull` и роняет деплой. Уже случалось дважды.
Если правка нужна срочно — сделать её на сервере, **сразу** продублировать в
репозиторий и при первом же деплое откатить локальную версию через
`git checkout --`.

### Файлы, которые портятся сами

`npm run build` (он же `04-frontend-deploy.sh`) на каждом запуске
перезаписывает файлы, которые лежат в репозитории. Из-за этого `git status` на
сервере никогда не бывает чистым, и `git pull` падает:

| Файл | Кто портит |
|---|---|
| `frontend/dist/index.html` | `vite build` |
| `frontend/dist/_redirects` | `vite build` |
| `frontend/public/sitemap.xml` | `generate-sitemap.js` (запускается из `build`) |

История: Как это закрыли 13.08.2026 (`git rm --cached`, `.gitignore`) — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

⚠️ **Сама таблица выше остаётся верной и нужной:** файлы по-прежнему
перезаписываются каждой сборкой, просто теперь молча и вне git. Если
`git status` на сервере снова окажется грязным по одному из них — значит
файл вернули в индекс, и лечение то же самое (`git rm --cached`), а не
`git checkout -- .` перед каждым деплоем.

### Порядок деплоя

1. **Claude Code**: `vite build` и точечные `pytest` (задетые файлы/новые
   тесты) — локально, обязательно. Полный `pytest backend/tests` —
   **не гонять**: машина работает на 4 ГБ RAM, и уже на середине прогона
   свободная память падает ниже 1 ГБ независимо от того, что делает pytest
   (нашлось 06.09.2026 — три попытки подряд убило `killed: low memory`,
   ушло больше двух часов без единого достоверного результата). Решающий
   прогон целиком — CI, см. предупреждение ниже; если тесты там падают —
   не пушить молча, объяснить, воспроизводятся ли падения на коде *до*
   правок, и дождаться решения владельца.

   ⚠️ **Зелёный локальный прогон не заменяет CI: часть кода на Windows не
   исполняется вовсе.** Два случая за два дня: `share_card_png` падает на
   `strftime("%-d %B %Y")` (флаг `%-d` есть только в glibc), то есть GET
   картинки локально не проверяется никогда; предохранитель сети ловил
   синхронный httpx, но не асинхронный, потому что asyncio на Windows идёт
   через ProactorEventLoop мимо `socket.connect`. Плюс локально нет Redis и
   Postgres. Решающий прогон — в CI (Linux, оба сервиса), и он же занимает
   ~4 минуты против ~80 локально.

   ⚠️ **6 кейсов в `test_beat_watchdog.py` падают локально на пустом месте —
   известный дефект окружения, не кода.** `TestFailureHandler::test_notifies_on_task_exception`,
   `TestMarker::test_written_after_successful_run`,
   `TestMarker::test_not_written_when_task_fails`,
   `TestWatchdog::test_silent_when_marker_fresh`,
   `TestWatchdog::test_alerts_when_marker_stale`,
   `TestWatchdog::test_task_write_is_visible_to_watchdog` — падают на любом
   коммите, включая чистый `main` (проверено 06.09.2026 отдельным
   `git worktree`). Причина: установленная локально версия `redis-py`
   заводит новую фичу «maintenance notifications», которая на подключении
   делает настоящий `socket.getaddrinfo` — `fakeredis` его не подделывает, и
   запрос улетает в сетевой предохранитель тестов
   (`OutboundNetworkBlocked`). В CI версии зафиксированы иначе, там этого
   нет. Не тратить время на разбор при следующей встрече — дефект в паре
   версий пакетов на этой машине, не в проекте.

История: Почему локальная сборка вырезала код за `VITE_*` и как это закрыли (`assert-bundle.mjs`) — [docs/HISTORY-deploy.md](docs/HISTORY-deploy.md).

   ⚠️ **Маркеры в манифесте — только строковые литералы.** Минификатор
   переименовывает идентификаторы: собственные функции в бандле выглядят как
   `m6` и `v6`. Имя функции в манифесте даст вечно красную сборку. Подробнее —
   в шапке `assert-bundle.mjs`.

   Локально, если нужно собрать «как на проде»:
   `VITE_GOOGLE_CLIENT_ID=$(grep -E '^VITE_GOOGLE_CLIENT_ID=' deploy/opt-astro/frontend.env | cut -d= -f2-) npm run build`
2. **Claude Code**: `git push` в `main`. **Автодеплоя нет** — пуш запускает
   только тесты (job `deploy` требует `workflow_dispatch`, см. раздел
   «Деплой» выше и почему так). Чтобы код уехал на прод, запустить джоб
   вручную: Actions → CI/CD → Run workflow (ветка `main`), либо
   `gh workflow run "CI/CD" --ref main`.
3. **Claude Code**: следить за GitHub Actions до конца, не отчитываться
   «запушено» раньше, чем отработает job `deploy` — включая ручной запуск
   из пункта 2.
4. **Владелец**: если деплой упал на `git pull` — выполнить блок из пункта
   «Файлы, которые портятся сами», затем сказать Claude Code перезапустить:
   `gh run rerun <ID> --failed`.
5. **Владелец**: проверить результат по чек-листу приёмки.

`05-update.sh`, `04-frontend-deploy.sh`, `docker-compose.yml` и
`07-backup-cron.sh` больше не нужно
копировать руками — `05-update.sh` сам синхронизирует их из
`app/deploy/opt-astro/` поверх рабочих копий в `/opt/astro` сразу после
`git pull`, при каждом запуске (обычном и `--backend-only`). Исключение:
`--frontend-only` не делает `git pull` для `app/`, поэтому и синхронизацию
не запускает. Добавлено 14.08.2026 после падения ночного бэкапа — фикс в
`07-backup-cron.sh` был закоммичен, но не доезжал до сервера этим же путём,
что раньше случалось с `05-update.sh` и `docker-compose.yml`.

### Доступ сервера к GitHub

Пароли для git отключены с 2021 года. Сервер тянет код по SSH через deploy key
`~/.ssh/astro_deploy_key`, публичная часть добавлена в
Settings → Deploy keys репозитория (только чтение). Настроено 13.08.2026 после
падения деплоя с `Authentication failed`.

Если снова появится запрос логина — ключ или конфиг `~/.ssh/config` потерян,
проверять: `cd /opt/astro/app && git remote -v` должен показывать
`git@github.com:...`, а не `https://`.

### Бэкапы: что уже проверено, что нет

Прогон на проде 13.08.2026 прошёл полностью — дамп создаётся, шифруется,
расшифровывается ключом и читается как валидный архив.

- `age` ставится через `apt-get install -y age`, на чистом сервере его нет.
- Приватный ключ `/opt/astro/backups/astro-backup.key` **обязан** храниться
  вне сервера. Без него бэкапы бесполезны.
- `pg_restore -l` не читает дамп формата `-Fc` из потока — нужен файл с
  возможностью перемотки. Проверка внутри контейнера пишет во временный файл.
  Не «чинить» обратно на pipe.
- `BACKUP_S3_TARGET` не задан — копии только на этом сервере. Открытый риск:
  отказ диска уносит базу и бэкапы разом.

### Прод-гварды платежей — вернулись под ЮKassa

История: История прод-гвардов (Robokassa → ЮKassa, переход к обязательным переменным) — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

| Состояние `.env` | Поведение |
|---|---|
| обе переменные пусты или пустые строки | **старт и деплой падают** |
| задана ровно одна из двух | старт и деплой падают |
| обе заданы, ключ `test_`, прод | старт и деплой падают (иначе подписки выдавались бы без реальной оплаты) |
| обе заданы боевыми | норма |
| `DEBUG=true` или `TESTING=true` | проверка не применяется — иначе локальная разработка и CI не запустятся |

⚠️ **Практическое следствие: деплой невозможен, если переменных нет на
сервере.** Обе обязаны быть в `/opt/astro/.env` — именно оттуда их читает и
`_required` в `05-update.sh` (`grep` по файлу в рабочем каталоге), и
приложение через `Settings` (`env_file=".env"` в контейнере). Имена в коде,
скрипте и `.env.example` совпадают до символа. Магазин: `shopId = 1442186`,
модерация пройдена, боевой ключ выпущен.

Проверка «ровно одна из двух» после этого стала недостижимой (пустая половина
падает раньше) и **намеренно оставлена** в обоих местах — страхует, если
обязательность когда-нибудь смягчат, и держит отдельную внятную формулировку.
Не удалять как мёртвый код.

Синхронность двух мест проверяется тестом
`TestDeployScriptStaysInSync` (`test_yookassa_required_guard.py`): он читает
`05-update.sh` и требует обе переменные в списке `_required`. Добавляя новый
прод-гвард в `main.py`, не забыть дописать переменную в `PROD_STARTUP_ENV`
(`backend/tests/prod_env.py`) — иначе все subprocess-тесты прод-гвардов начнут
падать по чужой причине.

---

## Важные соглашения

- Не использовать `sudo` с npm
- Framer Motion — только поверх готовых компонентов
- Новые компоненты брать из 21st.dev или Ponytail, не генерировать с нуля
- Один компонент за раз, не рефакторить всё сразу
- Перед правкой файла — уточнить задачу
- Никогда не редактировать файлы напрямую на сервере — только через репозиторий
- «Готово» означает проверено в работе. Если проверить нельзя (нет браузера,
  Docker, доступа к серверу) — писать «код готов, приёмка не пройдена» и явно
  перечислять, что осталось непроверенным
- Всё, что выясняется по ходу работы, записывать сразу — в этот файл, в
  комментарий или в docstring, туда, где найдёт следующий читающий. Не ждать
  отдельной просьбы и не отдавать владельцу списком «что стоило бы записать».
  Записывать не то, что сделано (это видно из git), а **почему** и **что
  сломается, если сделать иначе**. Особая ценность — у неочевидного:
  намеренные литералы, порядок операций, места, где правильное решение
  выглядит как недоделка. Мысль «выглядит странно, но трогать нельзя,
  потому что…» — это ровно то, что надо записать.

---

## Аудит безопасности 23.08.2026

Полный аудит по коду (прод не трогался). Ниже — итог, чтобы не перепроверять
заново и не искать находки повторно.

История: Аудит 23.08.2026, раздел «проверено и держится» (чекаут, идемпотентность, `client_ip`, BOLA, CRM, SSE-тикеты, `token_version`) — [docs/HISTORY-security.md](docs/HISTORY-security.md).

### Открытые находки — не чиним пока, но помним

Нумерация — из отчёта аудита 23.08.2026.

| № | Что | Где |
|---|---|---|
| ~~2.1~~ | ✅ Закрыто 23.08.2026: `SubscriptionOwnerMissing` + коммит записи о платеже + уведомление владельцу. Подробности — «Платёж, которому некому выдать подписку» в [docs/HISTORY-payments.md](docs/HISTORY-payments.md) | `payments/common.py` |
| 2.2 | `refund.succeeded` — единственное место, где телу вебхука верят: сумма и id берутся из запроса, а не перечитываются из API. Противоречит докстрингу модуля | `payments/yookassa_router.py` (`_handle_refund`) |
| ~~2.3~~ | ✅ Закрыто 23.08.2026: валюта сверяется вместе с суммой в `_handle_succeeded`, не-RUB → 400, подписка не выдаётся | `payments/yookassa_router.py` |
| ~~2.9~~ | ✅ Закрыто 23.08.2026 вместе с непригодной metadata: постоянные причины отвечают 200, платёж записывается, владелец уведомляется. Подробности — «Код ответа вебхука ЮKassa» в [docs/HISTORY-payments.md](docs/HISTORY-payments.md) | `payments/yookassa_router.py` |
| ~~2.4~~ | ✅ Закрыто 14.09.2026 (`33cfd35`): чтение строки токена идёт с `with_for_update()`, блокировка держится до того же `db.commit()`, которым пишется `used=True` и выдаётся premium — они и раньше были в одной транзакции, переносить ничего не потребовалось. ⚠️ Проверка гонки возможна ТОЛЬКО на Postgres: диалект SQLite `FOR UPDATE` не выдаёт вовсе, и на нём тест был бы зелёным при живом дефекте. Поэтому `test_pilot_claim_race.py` на SQLite пропускается, а рядом стоит исполняемая везде проверка, что блокировка запрошена. Прогон на живом postgres:18 — CI `34818754569`: `test_two_concurrent_claims_give_premium_to_exactly_one` PASSED, premium получил ровно один из двух | `pilot/router.py` (`claim_pilot`) |
| ~~2.5~~ | ✅ Закрыто 23.08.2026: `/admin/coupons/apply` переведён на `require_admin`. Подробности — ниже, раздел «Промокоды не подключены» | `admin/promo_router.py` |
| ~~2.6~~ | ✅ Закрыто 14.09.2026: отключение портала ротирует токен (старый исчезает из БД — строка одна на клиента, `client_id` unique), включение пересчитывает `expires_at` от момента включения. Ссылка отдаётся ровно в одном месте — CRM-карточка тянет её живым `GET /clients/{id}/portal`, поэтому ротация ничего не рвёт в интерфейсе; писем и PDF со ссылкой нет вовсе (грепом по бэкенду). ⚠️ **Правка бесплатна только потому, что CRM ещё не запущена и живых порталов нет.** После запуска отключение будет безвозвратно рвать уже разосланные клиентам ссылки — восстановить их нечем, выдаётся новая; сказать об этом владельцу ДО запуска CRM. Тест `test_portal_revoke.py`, обратный прогон на коде до правки: два кейса из трёх падают (третий — «выключенный портал закрыт сразу» — был зелёным и раньше, его закрывал флаг `enabled`) | `crm/router.py` (`set_portal`) |
| 3.1 | `/payments/checkout` без собственного rate limit (только глобальный 300/мин и nginx-зона `api`), а каждый вызов создаёт платёж на стороне ЮKassa | `payments/yookassa_router.py` |
| ~~2.10~~ | ✅ Закрыто 23.08.2026 (`b2b9bf0`, согласование п. 7 — `b5e3a42`). П. 4.5 больше не обещает чек в момент расчёта: «регистрирует полученный доход в приложении „Мой налог“… Чек о произведённом расчёте предоставляется Заказчику по его запросу». Сверено 09.09.2026 — обе половины пары совпадают дословно (`oferta_final.md:66` ↔ `TermsPage.jsx:82`) | `oferta_final.md` + `frontend/src/pages/TermsPage.jsx` (пара) |
| ~~3.6~~ | ✅ Закрыто 13.09.2026: `react-router-dom` 6.30 → 7.18.3, `npm audit --omit=dev` с 3 moderate до нуля. Приёмка навигации на вебе и устройстве пройдена. Заодно удалён мёртвый SSR-контур (`server.js`, `entry-server.jsx`) — он импортировал исчезнувший в семёрке `react-router-dom/server`, но чинить было нечего: `express` не в зависимостях, а маркера `<!--ssr-outlet-->` нет в `index.html`, то есть контур не работал бы и при запуске | `frontend/package.json` |
| ~~3.7~~ | ✅ Закрыто 14.09.2026: `get_eclipses` пересчитывает момент в GMT+3 по образцу равноденствий (`_jd_to_gmt3`) и отдаёт `"HH:MM GMT+3"` — пояс на ручке снова один. ⚠️ Сдвиг сделан на ВЫХОДЕ, а не в `_scan_eclipses`: сканер общий с лентой (`feed/builder.py`), которая разбирает его строку как UTC и склеивает затмение с фазой по порогу ровно в 3 часа — сдвиг в сканере вернул бы дубли. Фронт не трогали: `fmtPhaseTime` срезает обе метки одинаково. Проверено на трёх реальных затмениях около полуночи (20.02.2027 23:12 UTC → 21.02 02:12) плюс сверка, что выдача ленты не изменилась побайтно; прогон CI `34818754569` (`f12b23b`) — три кейса `test_lunar_eclipses.py` PASSED | `calendar/lunar_engine.py` |

Требует действий владельца (не Claude Code): `key_githab.txt` и
`ключ для бэкап.txt`. **Сверка 09.09.2026: обоих файлов в рабочем дереве
больше нет** (`ls` не находит), то есть немедленного риска «`git add -A` их
закоммитит» сегодня нет.

⚠️ **Но записана здесь была не пропажа файлов, а дыра в `.gitignore`, и она
на месте:** `git check-ignore` по обоим именам по-прежнему не даёт ничего,
gitleaks в CI untracked-файлы не видит, — значит файл с таким именем,
положенный в корень снова, снова окажется в одном `git add -A` от коммита.
Закрывать пункт по признаку «файлов нет» нельзя. Отдельно и независимо: ключ
от бэкапов по правилам обязан храниться вне сервера.

---

## Правовые документы — пара .md + .jsx, и она обязана совпадать

`oferta_final.md` ↔ `TermsPage.jsx` и `privacy_final.md` ↔ `PrivacyPage.jsx`.
`.md` программно **не читается** ничем — ни сборкой, ни тестами; синхронизация
ручная, по конвенции из шапки `.jsx`. Правишь одно — правишь оба в том же
коммите.

Что тянется за правкой текста оферты:

1. **`CURRENT_TERMS_VERSION`** (`backend/auth/consent.py`) — дата редакции.
   Используется ТОЛЬКО при создании аккаунта: штампует
   `user.consent_terms_version`. Сравнения с ней нигде нет — ни повторного
   согласия, ни баннера, ни разлогина, механизма не существует. Подъём версии
   ничего не ломает и никого не трогает: старые пользователи сохраняют старый
   штамп, новые получают новый. Читается только в выгрузке `/profile/export`.
2. **Строка «Дата публикации» в `.jsx`** — обязана совпадать с константой,
   это проверяет `test_consent_versions.py`. Разойдутся — упадёт тест.
3. **Комментарий в шапке `.jsx`** с датой редакции исходника.

Меняются все три разом, иначе документ утверждает неправду о самом себе.
Приватность и оферта версионируются **независимо** (две константы) — правка
одной не поднимает вторую.

⚠️ **Версия — это календарная дата, поэтому две редакции одного дня получают
одну версию.** 23.08.2026 так и вышло: утром правился п. 4.3 (сгорание
остатка), вечером п. 4.5 (чек) — обе редакции записаны пользователям как
`2026-08-23`, различить их по `consent_terms_version` нельзя. Ставить завтрашнюю
дату, чтобы «развести» версии, нельзя: документ будет утверждать, что
опубликован в будущем. Если понадобится точность внутри дня — менять сам формат
версии (например добавить порядковый номер редакции), а не подкручивать дату.

**П. 10.1 оферты требует уведомить пользователей за 14 дней** до вступления
изменений в силу, публикацией и письмом. Механизма рассылки в коде нет.
23.08.2026 уведомление не запускалось сознательно: правило про сгорание
остатка работало в коде с самого начала, ничьи условия не ухудшались —
менялась полнота информирования, а не сами условия; плюс пользователей на
проде ещё нет. **Для правки, которая реально меняет условия, это решение не
переиспользовать.**

---

История: Смена тарифа сжигает остаток: правило, три места текста и `TestOfferDocumentsTheRule` — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

---

История: Код ответа вебхука ЮKassa: постоянная причина — 200, временная — 500 — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

---

История: Платёж, которому некому выдать подписку (`SubscriptionOwnerMissing`, 200 вместо 500) — [docs/HISTORY-payments.md](docs/HISTORY-payments.md).

---

## Промокоды не подключены — целиком, и в чекауте это намеренно

Фича существует кусками, ни один из которых не работает. Проверено по коду и
истории 23.08.2026. Не «чинить UI», не «дописать вызов» — сначала прочитать это.

| Звено | Состояние |
|---|---|
| Поле ввода промокода, `PaywallModal.jsx` (~175-192) | Есть, живое |
| `validatePromoCode` → `GET /api/v1/payments/promo-validate` | **Эндпоинта в бэкенде нет вовсе** → всегда 404 → поле ВСЕГДА показывает «Промокод не найден или истёк» |
| `createCheckoutSession` шлёт `promo_code` | Бэкенд принимает и игнорирует: `CheckoutRequest` объявлен с `extra: "ignore"` |
| `POST /admin/coupons/apply` | Вызывающих нет. С 23.08.2026 — `require_admin` |
| `record_promo_usage` | Вызывающих не было **никогда** (`git log --all -S`, функция родилась как `def` в `26ef160`) |

**Игнорирование `promo_code` в чекауте — решение, а не недоделка**
(`yookassa_router.py`, комментарий в `CheckoutRequest`): промокод — вторая
точка, где сумма платежа могла бы разойтись с тем, что реально списала
ЮKassa. Провайдер уведомления не подписывает, сумму мы и так перечитываем из
API и сверяем с `TIER_PRICES_RUB` — вводить в эту сверку ещё одну переменную
без отдельного решения нельзя.

Поэтому «поле промокода не работает» чинится не в UI: пока нет
`promo-validate` и решения по сумме, любой ремонт интерфейса даст
полурабочее состояние. Полный список того, что нужно сделать разом, — в
докстринге `record_promo_usage`.
